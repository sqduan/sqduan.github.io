<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>一隅</title>
  
  <subtitle>记录生活</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2021-06-28T15:51:24.838Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>Shiqi Duan</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Make基本语法</title>
    <link href="http://yoursite.com/2021/06/28/%E5%B7%A5%E5%85%B7/Make/Make%E5%9F%BA%E6%9C%AC%E8%AF%AD%E6%B3%95/"/>
    <id>http://yoursite.com/2021/06/28/工具/Make/Make基本语法/</id>
    <published>2021-06-28T15:03:57.923Z</published>
    <updated>2021-06-28T15:51:24.838Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>本文将针对Make的基本语法进行讲解</p><a id="more"></a><h2 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h2><h3 id="赋值运算符"><a href="#赋值运算符" class="headerlink" title="赋值运算符"></a>赋值运算符</h3><p>Make 中有<code>:=</code>、<code>?=</code>、<code>+=</code>以及<code>=</code>四个赋值运算符，其中<code>=</code>和<code>?=</code>比较常用，这里总结一下两者的区别：</p><ul><li>对于<code>=</code>，变量的值将会是整个Makefile中最后被指定的值，例如：</li></ul><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">x = foo</span><br><span class="line">y = <span class="variable">$(x)</span> bar</span><br><span class="line">x = xyz</span><br></pre></td></tr></table></figure><p>最终，<code>y</code>的值是<code>xyz bar</code></p><ul><li>对于<code>:=</code>，变量的值决定于它在Makefile中的位置，而不是整个Makefile展开后的值</li></ul><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">x := foo</span><br><span class="line">y := <span class="variable">$(x)</span> bar</span><br><span class="line">x := xyz</span><br></pre></td></tr></table></figure><p>最终，<code>y</code>的值为<code>foo bar</code></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文将针对Make的基本语法进行讲解&lt;/p&gt;
    
    </summary>
    
      <category term="工具" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/"/>
    
      <category term="Make" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/Make/"/>
    
    
  </entry>
  
  <entry>
    <title>MIT6828-Lab4-抢占式多线程</title>
    <link href="http://yoursite.com/2021/06/13/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/MIT6828-Lab4/"/>
    <id>http://yoursite.com/2021/06/13/计算机/操作系统/课程/MIT6828-Lab4/</id>
    <published>2021-06-13T14:31:27.485Z</published>
    <updated>2021-06-28T15:03:35.703Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>剪不断，理还乱</p></blockquote><a id="more"></a><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>In this lab you will implement preemptive multitasking among multiple simultaneously active user-mode environments.</p><p>In part A you will add multiprocessor support to JOS, implement round-robin scheduling, and add basic environment management system calls (calls that create and destroy environments, and allocate/map memory).</p><p>In part B, you will implement a Unix-like <code>fork()</code>, which allows a user-mode environment to create copies of itself.</p><p>Finally, in part C you will add support for inter-process communication (IPC), allowing different user-mode environments to communicate and synchronize with each other explicitly. You will also add support for hardware clock interrupts and preemption.</p><h3 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">athena% cd ~/<span class="number">6.828</span>/lab</span><br><span class="line">athena% add git</span><br><span class="line">athena% git pull</span><br><span class="line">Already up-to-date.</span><br><span class="line">athena% git checkout -b lab4 origin/lab4</span><br><span class="line">Branch lab4 <span class="built_in">set</span> up to track remote branch refs/remotes/origin/lab4.</span><br><span class="line">Switched to a <span class="keyword">new</span> branch <span class="string">"lab4"</span></span><br><span class="line">athena% git merge lab3</span><br><span class="line">Merge made by recursive.</span><br><span class="line">...</span><br><span class="line">athena%</span><br></pre></td></tr></table></figure><p>首先，合并lab3，然后切换至lab4，在lab4中，你需要参考及阅读的源码如下：</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><div class="table-container"><table><thead><tr><th>目录</th><th>文件</th><th>功能</th><th>进度</th></tr></thead><tbody><tr><td><code>inc/</code></td><td><code>env.h</code></td><td>Public definitions for user-mode environments</td><td></td></tr><tr><td></td><td><code>trap.h</code></td><td>Public definitions for trap handling</td><td></td></tr><tr><td></td><td><code>syscall.h</code></td><td>Public definitions for system calls from user environments to the kernel</td><td></td></tr><tr><td></td><td><code>lib.h</code></td><td>Public definitions for the user-mode support library</td><td></td></tr><tr><td><code>kern/</code></td><td><code>env.h</code></td><td>Kernel-private definitions for user-mode environments</td><td></td></tr><tr><td></td><td><code>env.c</code></td><td>Kernel code implementing user-mode environments</td><td></td></tr><tr><td></td><td><code>trap.h</code></td><td>Kernel-private trap handling definitions</td><td></td></tr><tr><td></td><td><code>trap.c</code></td><td>Trap handling code</td><td></td></tr><tr><td></td><td><code>trapentry.S</code></td><td>Assembly-language trap handler entry-points</td><td></td></tr><tr><td></td><td><code>syscall.h</code></td><td>Kernel-private definitions for system call handling</td><td></td></tr><tr><td></td><td><code>syscall.c</code></td><td>System call implementation code</td><td></td></tr><tr><td><code>lib/</code></td><td><code>Makefrag</code></td><td>Makefile fragment to build user-mode library, <code>obj/lib/libjos.a</code></td><td></td></tr><tr><td></td><td><code>entry.S</code></td><td>Assembly-language entry-point for user environments</td><td></td></tr><tr><td></td><td><code>libmain.c</code></td><td>User-mode library setup code called from <code>entry.S</code></td><td></td></tr><tr><td></td><td><code>syscall.c</code></td><td>User-mode system call stub functions</td><td></td></tr><tr><td></td><td><code>console.c</code></td><td>User-mode implementations of <code>putchar</code> and <code>getchar</code>, providing console I/O</td><td></td></tr><tr><td></td><td><code>exit.c</code></td><td>User-mode implementation of <code>exit</code></td><td></td></tr><tr><td></td><td><code>panic.c</code></td><td>User-mode implementation of <code>panic</code></td><td></td></tr><tr><td><code>user/</code></td><td><code>*</code></td><td>Various test programs to check kernel lab 3 code</td></tr></tbody></table></div></div></div><h3 id="实验需求"><a href="#实验需求" class="headerlink" title="实验需求"></a>实验需求</h3><p>本练习有三个部分，每个部分耗时一周。你需要完成所有的任务和至少一个挑战。</p><h2 id="第一部分：多处理器支持及协作多任务"><a href="#第一部分：多处理器支持及协作多任务" class="headerlink" title="第一部分：多处理器支持及协作多任务"></a>第一部分：多处理器支持及协作多任务</h2><p>在本实验第一章，你需要在多处理器系统上扩展JOS，并实现一些新的系统调用使用户程序创建新的用户环境。你需要实现合作的round-robin调度算法，从而使内核实现任务切换。在Part C，你需要实现抢占式调度，从而使内核重新获得CPU控制权。</p><h3 id="多处理器支持"><a href="#多处理器支持" class="headerlink" title="多处理器支持"></a>多处理器支持</h3><p>We are going to make JOS support “symmetric multiprocessing” (SMP), a multiprocessor model in which all CPUs have equivalent access to system resources such as memory and I/O buses. While all CPUs are functionally identical in SMP, during the boot process they can be classified into two types: the bootstrap processor (BSP) is responsible for initializing the system and for booting the operating system; and the application processors (APs) are activated by the BSP only after the operating system is up and running. Which processor is the BSP is determined by the hardware and the BIOS. Up to this point, all your existing JOS code has been running on the BSP. （BSP负责启动系统）</p><p>In an SMP system, each CPU has an accompanying local APIC (LAPIC) unit. The LAPIC units are responsible for delivering interrupts throughout the system. The LAPIC also provides its connected CPU with a unique identifier. In this lab, we make use of the following basic functionality of the LAPIC unit (in <code>kern/lapic.c</code>):</p><ul><li>Reading the LAPIC identifier (APIC ID) to tell which CPU our code is currently running on (see <code>cpunum()</code>).</li><li>Sending the <code>STARTUP</code> interprocessor interrupt (IPI) from the BSP to the APs to bring up other CPUs (see <code>lapic_startap()</code>).</li><li>In part C, we program LAPIC’s built-in timer to trigger clock interrupts to support preemptive multitasking (see <code>apic_init()</code>).</li></ul><p>A processor accesses its LAPIC using memory-mapped I/O (MMIO). In MMIO, a portion of <em>physical</em> memory is hardwired to the registers of some I/O devices, so the same load/store instructions typically used to access memory can be used to access device registers. You’ve already seen one IO hole at physical address <code>0xA0000</code> (we use this to write to the VGA display buffer). The LAPIC lives in a hole starting at physical address <code>0xFE000000</code> (32MB short of 4GB), so it’s too high for us to access using our usual direct map at KERNBASE. The JOS virtual memory map leaves a 4MB gap at <code>MMIOBASE</code> so we have a place to map devices like this. Since later labs introduce more MMIO regions, you’ll write a simple function to allocate space from this region and map device memory to it.</p><div class="note default">            <p><strong>练习1</strong>：在<code>kern/pmap.c</code>中编写<code>mmio_map_region</code>。为了搞明白这个是如何工作的，看一下<code>kern/lapic.c</code>中<code>lapic_init</code>的一开始部分。你需要完成下一个练习，才能令<code>mmio_map_region</code>正常工作。</p>          </div><h5 id="函数原型"><a href="#函数原型" class="headerlink" title="函数原型"></a>函数原型</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> *</span><br><span class="line">mmio_map_region(<span class="keyword">physaddr_t</span> pa, <span class="keyword">size_t</span> size)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">uintptr_t</span> base = MMIOBASE;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">void</span>*)<span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="函数功能"><a href="#函数功能" class="headerlink" title="函数功能"></a>函数功能</h5><p>这个函数保留了一段空间，用于MMIO区域，这段区域位于<code>MMIOBASE + size</code>，被映射至<code>[pa, pa+size)</code>，程序不难，照着注释就能写完。</p><h5 id="函数实现"><a href="#函数实现" class="headerlink" title="函数实现"></a>函数实现</h5><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Reserve size bytes in the MMIO region and map [pa,pa+size) at this</span></span><br><span class="line"><span class="comment">// location.  Return the base of the reserved region.  size does *not*</span></span><br><span class="line"><span class="comment">// have to be multiple of PGSIZE.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">void</span> *</span><br><span class="line">mmio_map_region(<span class="keyword">physaddr_t</span> pa, <span class="keyword">size_t</span> size)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Where to start the next region.  Initially, this is the</span></span><br><span class="line">    <span class="comment">// beginning of the MMIO region.  Because this is static, its</span></span><br><span class="line">    <span class="comment">// value will be preserved between calls to mmio_map_region</span></span><br><span class="line">    <span class="comment">// (just like nextfree in boot_alloc).</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">uintptr_t</span> base = MMIOBASE;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">    size = (<span class="keyword">size_t</span>) ROUNDUP((<span class="keyword">uint32_t</span>)(size), PGSIZE);    <span class="comment">// Round size up to a multiple of PGSIZE</span></span><br><span class="line">    <span class="comment">// Handle if this reservation would overflow MMIOLIM</span></span><br><span class="line">    <span class="keyword">if</span>(base + size &gt;= MMIOBASE) panic(<span class="string">"The reservation of MMIO region is overflowed"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Reserve size bytes of virtual memory starting at base and</span></span><br><span class="line">    <span class="comment">// map physical pages [pa,pa+size) to virtual addresses</span></span><br><span class="line">    <span class="comment">// [base,base+size).  Since this is device memory and not</span></span><br><span class="line">    <span class="comment">// regular DRAM, you'll have to tell the CPU that it isn't</span></span><br><span class="line">    <span class="comment">// safe to cache access to this memory. </span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">// PTE_PCD: Cache disable</span></span><br><span class="line">    <span class="comment">// PTE_PWT: write through</span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">// Should I write PTE_P?</span></span><br><span class="line">    boot_map_region(kern_pgdir, base, size, pa, (PTE_P | PTE_W | PTE_PCD | PTE_PWT));</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">void</span>*)base;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h5><p>上面的函数无法通过<code>check_kern_pgdir</code>，注意这里的<code>base</code>用的是<code>static</code>，根据其中的注释，我们每次调用<code>mmio_map_region</code>，都需要更新<code>base</code>，因此我们在函数中使用一个<code>old_base</code>记录更新前的值，然后令<code>base = base + size</code>。代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">uintptr_t</span> old_base = base;</span><br><span class="line">base = base + size;</span><br><span class="line"><span class="keyword">return</span> (<span class="keyword">void</span>*)old_base</span><br></pre></td></tr></table></figure><h4 id="应用处理器启动"><a href="#应用处理器启动" class="headerlink" title="应用处理器启动"></a>应用处理器启动</h4><p>在启动APs之前，BSP将会收集多处理器系统的信息，例如CPU总数，APIC号以及LAPIC的多路IO地址。 <code>kern/mpconfig.c</code>中的 <code>mp_init()</code>函数通过读取位于内存BIOS区域的多处理器配置表获取这些信息。</p><p><code>boot_aps()</code> 函数 (位于<code>kern/init.c</code>) 驱动了应用处理器的启动过程。APs于实模式下启动，和bootloader由<code>boot/boot.S</code>的启动过程类似，因此<code>boot_aps()</code>(位于<code>kern/mpentry.S</code>)将AP的入口（entry）代码复制到了可以在实模式下寻址的一段内存空间中。不同于bootloader，我们对于AP执行代码的位置有了一定控制；我们将入口代码复制到了 <code>0x7000</code> (<code>MPENTRY_PADDR</code>)处。</p><p>在此之后，<code>boot_aps()</code> 将依次激活应用处理器，通过向对应处理器的LAPIC单元发送<code>STARTUP</code>IPIs，以及一个初始化的 <code>CS:IP</code> 地址，应用处理器将会在这个地址执行入口代码 (<code>MPENTRY_PADDR</code>). <code>kern/mpentry.S</code> 中的入口代码和<code>boot/boot.S</code>中的很类似。经过一些简单的启动过程后，这个代码将会令AP进入保护模式，从而使能页映射，并调用 <code>mp_main()</code>进行配置(also in <code>kern/init.c</code>)。 在唤醒下一个AP前，<code>boot_aps()</code> 等待AP发送一个<code>CPU_STARTED</code>标志。这个标志位于结构体<code>struct CpuInfo</code>的<code>cpu_status</code>字段。</p><div class="note default">            <p><strong>练习 2.</strong> Read <code>boot_aps()</code> and <code>mp_main()</code> in <code>kern/init.c</code>, and the assembly code in <code>kern/mpentry.S</code>. Make sure you understand the control flow transfer during the bootstrap of APs. Then modify your implementation of <code>page_init()</code> in <code>kern/pmap.c</code> to avoid adding the page at <code>MPENTRY_PADDR</code> to the free list, so that we can safely copy and run AP bootstrap code at that physical address. Your code should pass the updated <code>check_page_free_list()</code> test (but might fail the updated <code>check_kern_pgdir()</code> test, which we will fix soon).</p>          </div><h5 id="阅读boot-aps、mp-main以及mpentry-S"><a href="#阅读boot-aps、mp-main以及mpentry-S" class="headerlink" title="阅读boot_aps、mp_main以及mpentry.S"></a>阅读boot_aps、mp_main以及<code>mpentry.S</code></h5><p><code>boot_aps</code>的源代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">boot_aps(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">extern</span> <span class="keyword">unsigned</span> <span class="keyword">char</span> mpentry_start[], mpentry_end[];   <span class="comment">// 这两个地址位于`mpentry.S`中</span></span><br><span class="line">    <span class="keyword">void</span> *code;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CpuInfo</span> *<span class="title">c</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Write entry code to unused memory at MPENTRY_PADDR</span></span><br><span class="line">    code = KADDR(MPENTRY_PADDR);</span><br><span class="line">    memmove(code, mpentry_start, mpentry_end - mpentry_start);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Boot each AP one at a time</span></span><br><span class="line">    <span class="keyword">for</span> (c = cpus; c &lt; cpus + ncpu; c++) &#123;</span><br><span class="line">        <span class="comment">// 下面这句不是太理解，是说当前CPU已经启动么？</span></span><br><span class="line">        <span class="keyword">if</span> (c == cpus + cpunum())  <span class="comment">// We've started already.</span></span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Tell mpentry.S what stack to use </span></span><br><span class="line">        mpentry_kstack = percpu_kstacks[c - cpus] + KSTKSIZE;</span><br><span class="line">        <span class="comment">// Start the CPU at mpentry_start</span></span><br><span class="line">        lapic_startap(c-&gt;cpu_id, PADDR(code));</span><br><span class="line">        <span class="comment">// Wait for the CPU to finish some basic setup in mp_main()</span></span><br><span class="line">        <span class="keyword">while</span>(c-&gt;cpu_status != CPU_STARTED)</span><br><span class="line">            ;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>boot_aps</code>的基本过程为首先拷贝入口代码，然后遍历每个CPU，如果CPU已经启动了（cpunum返回当前CPU），那么跳过，否则分配栈（大小为32768），然后启动AP并等待配置完成。</p><p><code>mp_main</code>的源码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Setup code for APs</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">mp_main(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// We are in high EIP now, safe to switch to kern_pgdir </span></span><br><span class="line">    lcr3(PADDR(kern_pgdir));</span><br><span class="line">    cprintf(<span class="string">"SMP: CPU %d starting\n"</span>, cpunum());</span><br><span class="line"></span><br><span class="line">    lapic_init();           <span class="comment">// 初始化local apic</span></span><br><span class="line">    env_init_percpu();      <span class="comment">// 初始化每个CPU的进程</span></span><br><span class="line">    trap_init_percpu();     <span class="comment">// 初始化每个CPU的trap</span></span><br><span class="line">    xchg(&amp;thiscpu-&gt;cpu_status, CPU_STARTED); <span class="comment">// tell boot_aps() we're up</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Now that we have finished some basic setup, call sched_yield()</span></span><br><span class="line">    <span class="comment">// to start running processes on this CPU.  But make sure that</span></span><br><span class="line">    <span class="comment">// only one CPU can enter the scheduler at a time!</span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">// Your code here:</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Remove this after you finish Exercise 6</span></span><br><span class="line">    <span class="keyword">for</span> (;;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>mp_main</code>主要做了一些初始化工作，具体内容我们会在后面进行讲解。至于<code>mpentry.S</code>，其基本功能就是初始化一些寄存器，然后调用<code>mp_main</code>，所以AP的初始化过程可以总结为：<code>boot_aps</code>将<code>mpentry.S</code>拷贝到给定内存空间中，然后<code>mpentry.S</code>调用<code>mp_main</code>进行CPU初始化工作。</p><h5 id="将MPENTRY-PADDR（0x7000）对应的物理页从内存freelist中剔除"><a href="#将MPENTRY-PADDR（0x7000）对应的物理页从内存freelist中剔除" class="headerlink" title="将MPENTRY_PADDR（0x7000）对应的物理页从内存freelist中剔除"></a>将<code>MPENTRY_PADDR</code>（0x7000）对应的物理页从内存freelist中剔除</h5><p>我们之间以链表的形式将内存中空闲页面进行了串联，现在需要将0x7000对应的物理页剔除，只要找到0x7000对应的页在pages的数组中所在下标，并调整链表节点连接关系即可，比较简单，代码如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// LAB 4:</span></span><br><span class="line"><span class="comment">// Change your code to mark the physical page at MPENTRY_PADDR</span></span><br><span class="line"><span class="comment">// as in use</span></span><br><span class="line">i = MPENTRY_PADDR/PGSIZE;</span><br><span class="line">pages[i].pp_ref = <span class="number">1</span>;</span><br><span class="line">pages[i].pp_link = <span class="literal">NULL</span>;</span><br><span class="line">pages[i+<span class="number">1</span>].pp_link = &amp;pages[i<span class="number">-1</span>];</span><br></pre></td></tr></table></figure><h4 id="单个CPU的状态及初始化"><a href="#单个CPU的状态及初始化" class="headerlink" title="单个CPU的状态及初始化"></a>单个CPU的状态及初始化</h4><p>编写多处理器OS时，最重要的一个就是分辨每个CPU的状态（私有）以及系统共享全局状态。 <code>kern/cpu.h</code>定义了最常用的CPU状态，包括 <code>struct CpuInfo</code>，包含了每个CPU中的状态变量. <code>cpunum()</code>总是返回调用它的CPU的序号，因此可以被用来作为CPU数组<code>cpus</code>的索引。同样的， 宏<code>thiscpu</code> 也记录了当前CPU的 <code>struct CpuInfo</code>.</p><p>这里列举了你应当了解的每个CPU的状态：</p><ul><li><p><strong>Per-CPU kernel stack</strong>（每个CPU的内核栈）<br>Because multiple CPUs can trap into the kernel simultaneously, we need a separate kernel stack for each processor to prevent them from interfering with each other’s execution. The array <code>percpu_kstacks[NCPU][KSTKSIZE]</code> reserves space for NCPU’s worth of kernel stacks.（独立的内核栈确保互不打扰）</p><p>In Lab 2, you mapped the physical memory that <code>bootstack</code> refers to as the BSP’s kernel stack just below <code>KSTACKTOP</code>. Similarly, in this lab, you will map each CPU’s kernel stack into this region with guard pages acting as a buffer between them. CPU 0’s stack will still grow down from <code>KSTACKTOP</code>; CPU 1’s stack will start <code>KSTKGAP</code> bytes below the bottom of CPU 0’s stack, and so on. <code>inc/memlayout.h</code> shows the mapping layout.</p></li><li><p><strong>Per-CPU TSS and TSS descriptor</strong>（每个CPU的任务队列）<br>A per-CPU task state segment (TSS) is also needed in order to specify where each CPU’s kernel stack lives. The TSS for CPU <em>i</em> is stored in <code>cpus[i].cpu_ts</code>, and the corresponding TSS descriptor is defined in the GDT entry <code>gdt[(GD_TSS0 &gt;&gt; 3) + i]</code>. The global <code>ts</code> variable defined in <code>kern/trap.c</code> will no longer be useful.</p></li><li><p><strong>Per-CPU current environment pointer</strong>（每个CPU的当前环境指针）<br>Since each CPU can run different user process simultaneously, we redefined the symbol <code>curenv</code> to refer to <code>cpus[cpunum()].cpu_env</code> (or <code>thiscpu-&gt;cpu_env</code>), which points to the environment <em>currently</em> executing on the <em>current</em> CPU (the CPU on which the code is running).</p></li><li><p><strong>Per-CPU system registers</strong>（每个CPU的系统寄存器）<br>All registers, including system registers, are private to a CPU. Therefore, instructions that initialize these registers, such as <code>lcr3()</code>, <code>ltr()</code>, <code>lgdt()</code>, <code>lidt()</code>, etc., must be executed once on each CPU. Functions <code>env_init_percpu()</code> and <code>trap_init_percpu()</code> are defined for this purpose.</p></li><li><p>In addition to this, if you have added any extra per-CPU state or performed any additional CPU-specific initialization (by say, setting new bits in the CPU registers) in your solutions to challenge problems in earlier labs, be sure to replicate them on each CPU here!</p></li></ul><div class="note default">            <p><strong>Exercise 3.</strong> Modify <code>mem_init_mp()</code> (in <code>kern/pmap.c</code>) to map per-CPU stacks starting at <code>KSTACKTOP</code>, as shown in <code>inc/memlayout.h</code>. The size of each stack is <code>KSTKSIZE</code> bytes plus <code>KSTKGAP</code> bytes of unmapped guard pages. Your code should pass the new check in <code>check_kern_pgdir()</code>.</p>          </div><h5 id="函数功能-1"><a href="#函数功能-1" class="headerlink" title="函数功能"></a>函数功能</h5><p>为每个CPU分配栈空间，栈空间分为两部分，一部分为栈区，大小为<code>KSTKSIZE</code>，另一部分为缓冲区，大小为<code>KSTKGAP</code>，缓冲区的作用是判断栈溢出，如果进入缓冲区，说明溢出，要触发异常，栈示意图如下：</p><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/kstacktop.svg" width = "250" alt="图片名称" align=center /></p><h5 id="函数实现-1"><a href="#函数实现-1" class="headerlink" title="函数实现"></a>函数实现</h5><p>根据函数功能，我们需要将栈地址与对应的物理地址进行映射，每个栈的栈顶为<code>kstacktop_i = KSTACKTOP - i * (KSTKSIZE + KSTKGAP)</code>，栈底为<code>kstacktop_i-KSTKSIZE</code>，大小为<code>KSTKSIZE</code>。不难写出映射代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Modify mappings in kern_pgdir to support SMP</span></span><br><span class="line"><span class="comment">//   - Map the per-CPU stacks in the region [KSTACKTOP-PTSIZE, KSTACKTOP)</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">mem_init_mp(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// For CPU i, use the physical memory that 'percpu_kstacks[i]' refers</span></span><br><span class="line">    <span class="comment">// to as its kernel stack. CPU i's kernel stack grows down from virtual</span></span><br><span class="line">    <span class="comment">// address kstacktop_i = KSTACKTOP - i * (KSTKSIZE + KSTKGAP), and is</span></span><br><span class="line">    <span class="comment">// divided into two pieces, just like the single stack you set up in</span></span><br><span class="line">    <span class="comment">// mem_init:</span></span><br><span class="line">    <span class="comment">//     * [kstacktop_i - KSTKSIZE, kstacktop_i)</span></span><br><span class="line">    <span class="comment">//          -- backed by physical memory</span></span><br><span class="line">    <span class="comment">//     * [kstacktop_i - (KSTKSIZE + KSTKGAP), kstacktop_i - KSTKSIZE)</span></span><br><span class="line">    <span class="comment">//          -- not backed; so if the kernel overflows its stack,</span></span><br><span class="line">    <span class="comment">//             it will fault rather than overwrite another CPU's stack.</span></span><br><span class="line">    <span class="comment">//             Known as a "guard page".</span></span><br><span class="line">    <span class="comment">//     Permissions: kernel RW, user NONE</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; NCPU; ++i)&#123;</span><br><span class="line">        <span class="keyword">uintptr_t</span> kstacktop_i = KSTACKTOP - i * (KSTKSIZE + KSTKGAP);</span><br><span class="line">        boot_map_region(kern_pgdir, kstacktop_i - KSTKSIZE, KSTKSIZE, PADDR(percpu_kstacks[i]), (PTE_W | PTE_P));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>完成后，代码应当能够通过<code>check_kern_pgdir</code></p><div class="note default">            <p><strong>Exercise 4.</strong> The code in <code>trap_init_percpu()</code> (<code>kern/trap.c</code>) initializes the TSS and TSS descriptor for the BSP. It worked in Lab 3, but is incorrect when running on other CPUs. Change the code so that it can work on all CPUs. (Note: your new code should not use the global <code>ts</code> variable any more.)</p>          </div><h5 id="TSS和TSS-descriptor"><a href="#TSS和TSS-descriptor" class="headerlink" title="TSS和TSS descriptor"></a>TSS和TSS descriptor</h5><p>为了解决这个问题，我们首先要搞清楚Task Segment Selector（TSS）和TSS descriptor，关于这些内容请参考<a href="/2020/07/12/计算机/操作系统/进程/" title="关于进程的讲解">关于进程的讲解</a>。练习四要求我们修改<code>trap_init_percpu</code>，从而初始化TSS和TSSD。</p><h5 id="原始版本的trap-init-percpu"><a href="#原始版本的trap-init-percpu" class="headerlink" title="原始版本的trap_init_percpu"></a>原始版本的trap_init_percpu</h5><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">trap_init_percpu(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    ts.ts_esp0 = KSTACKTOP;</span><br><span class="line">    ts.ts_ss0 = GD_KD;</span><br><span class="line">    ts.ts_iomb = <span class="keyword">sizeof</span>(struct Taskstate);</span><br><span class="line"></span><br><span class="line">    gdt[GD_TSS0 &gt;&gt; <span class="number">3</span>] = SEG16(STS_T32A, (<span class="keyword">uint32_t</span>) (&amp;ts),</span><br><span class="line">                    <span class="keyword">sizeof</span>(struct Taskstate) - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    gdt[GD_TSS0 &gt;&gt; <span class="number">3</span>].sd_s = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    ltr(GD_TSS0);</span><br><span class="line">    lidt(&amp;idt_pd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码配置了CPU0（Boot CPU）的TSS和TSSD，但是并不通用，我们要将其修改，令其可以自适应地配置每个CPU的TSS和TSSD。</p><h5 id="修改内容"><a href="#修改内容" class="headerlink" title="修改内容"></a>修改内容</h5><p>根据注释，我们需要修改的内容如下：</p><ul><li>首先使用<code>thiscpu-&gt;cpu_ts</code>即当前CPU的TSS段代替<code>ts</code></li><li>将TSS段中的esp0指向当前CPU内核栈栈顶（栈顶计算过程参考练习3）</li><li>修改当前CPU的TSSD，即<code>gdt[(GD_TSS0 &gt;&gt; 3) + cpunum()]</code></li><li>修改当前CPU的TSS selector</li></ul><p>前三个任务根据注释很好完成，主要注意下当前CPU的TSS selector。根据注释，selector的低三位都是0，实际上第四位表示具体的CPU，因此每个CPU的TSS selector可以表示为：<code>GD_TSS0  + (cpunum() &lt;&lt; 3)</code>。</p><h5 id="改进后的trap-init-percpu"><a href="#改进后的trap-init-percpu" class="headerlink" title="改进后的trap_init_percpu"></a>改进后的trap_init_percpu</h5><p>根据上面的分析及注释，可以写出改进的<code>trap_init_percpu</code>如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Initialize and load TSS, TSS descriptor and IDT for all CPUs</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">trap_init_percpu(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// - Calculate current CPU's kern stack top (esp0), thiscpu points to the current CPU's </span></span><br><span class="line">    <span class="comment">// struct CpuInfo, and cpunum() returns current CPU's id</span></span><br><span class="line">    <span class="comment">// - Use "thiscpu-&gt;cpu_ts" as the TSS for the current CPU,</span></span><br><span class="line">    <span class="comment">// rather than the global "ts" variable;</span></span><br><span class="line">    <span class="keyword">uintptr_t</span> cur_cpu_kstacktop = KSTACKTOP - cpunum() * (KSTKSIZE + KSTKGAP);  </span><br><span class="line">    thiscpu-&gt;cpu_ts.ts_esp0 = cur_cpu_kstacktop;</span><br><span class="line">    thiscpu-&gt;cpu_ts.ts_ss0 = GD_KD;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//   - Initialize cpu_ts.ts_iomb to prevent unauthorized environments</span></span><br><span class="line">    <span class="comment">//     from doing IO (0 is not the correct value!)</span></span><br><span class="line">    thiscpu-&gt;cpu_ts.ts_iomb = <span class="keyword">sizeof</span>(struct Taskstate);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Setup a TSS so that we get the right stack when we trap to the kernel.</span></span><br><span class="line">    <span class="comment">// Initialize the TSS slot of the gdt. USE gdt[(GD_TSS0 &gt;&gt; 3) + i] for CPU i's TSS descriptor;</span></span><br><span class="line">    gdt[(GD_TSS0 &gt;&gt; <span class="number">3</span>) + cpunum()] = SEG16(STS_T32A, (<span class="keyword">uint32_t</span>) (&amp;(thiscpu-&gt;cpu_ts)),</span><br><span class="line">                    <span class="keyword">sizeof</span>(struct Taskstate) - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    gdt[(GD_TSS0 &gt;&gt; <span class="number">3</span>) + cpunum()].sd_s = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// ltr sets a 'busy' flag in the TSS selector, so if you</span></span><br><span class="line">    <span class="comment">// accidentally load the same TSS on more than one CPU, you'll</span></span><br><span class="line">    <span class="comment">// get a triple fault.  If you set up an individual CPU's TSS</span></span><br><span class="line">    <span class="comment">// wrong, you may not get a fault until you try to return from</span></span><br><span class="line">    <span class="comment">// user space on that CPU.</span></span><br><span class="line">    <span class="comment">// Load the TSS selector (like other segment selectors, the</span></span><br><span class="line">    <span class="comment">// bottom three bits are special; we leave them 0)</span></span><br><span class="line">    ltr(GD_TSS0  + (cpunum() &lt;&lt; <span class="number">3</span>));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Load the IDT</span></span><br><span class="line">    lidt(&amp;idt_pd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>完成上述内容后，以4核CPU在QEMU中启动JOS（<code>make qemu CPUS=4</code>），可以得到如下输出结果：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line">Physical memory: 66556K available, base = 640K, extended = 65532K</span><br><span class="line">check_page_alloc() succeeded!</span><br><span class="line">check_page() succeeded!</span><br><span class="line">check_kern_pgdir() succeeded!</span><br><span class="line">check_page_installed_pgdir() succeeded!</span><br><span class="line">SMP: CPU 0 found 4 CPU(s)</span><br><span class="line">enabled interrupts: 1 2</span><br><span class="line">SMP: CPU 1 starting</span><br><span class="line">SMP: CPU 2 starting</span><br><span class="line">SMP: CPU 3 starting</span><br><span class="line">[00000000] new env 00001000</span><br><span class="line">EAX=00000000 EBX=00000000 ECX=ef804000 EDX=f022a094</span><br><span class="line">... <span class="comment">#中间省略一堆寄存器的信息</span></span><br><span class="line">EFER=0000000000000000</span><br><span class="line">Triple fault.  Halting <span class="keyword">for</span> inspection via QEMU monitor.</span><br></pre></td></tr></table></figure><p>可以看到CPU都启动了，但新启动的进程遇到了一个Triple fault，原因见上一个实验。</p><h4 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h4><p>我们现在的代码将在<code>mp_main()</code>初始化AP后陷入死循环。为了让AP能够继续运行，我们首先要解决多CPU同时运行内核代码时的竞态条件。最简单的解决方式是使用一个大的全局内核锁，当有进程进入内核态后，需要对内核进行上锁；从内核态返回用户态时，需要解锁。在这个模型中，用户态进程可以在任意CPU上并发执行，但是只有一个进程能够运行于内核态，任何一个其他的想要进入内核态的进程必须等待。</p><p><code>kern/spinlock.h</code> declares the big kernel lock, namely <code>kernel_lock</code>. It also provides <code>lock_kernel()</code> and <code>unlock_kernel()</code>, shortcuts to acquire and release the lock. You should apply the big kernel lock at four locations:</p><ul><li>In <code>i386_init()</code>, acquire the lock before the BSP wakes up the other CPUs. 避免其他CPU执行内核代码。</li><li>In <code>mp_main()</code>, acquire the lock after initializing the AP, and then call <code>sched_yield()</code> to start running environments on this AP. 在该CPU上执行用户态进程</li><li>In <code>trap()</code>, acquire the lock when trapped from user mode. To determine whether a trap happened in user mode or in kernel mode, check the low bits of the <code>tf_cs</code>. 从用户态进入内核态，占有内核锁。</li><li>In <code>env_run()</code>, release the lock <em>right before</em> switching to user mode. Do not do that too early or too late, otherwise you will experience races or deadlocks. 从内核态进入用户态，释放内核锁</li></ul><p>从上面的过程中可以看出，我们有三处位置进行上锁，一处位置进行解锁。实际上，我们在所有可能发生内核态到用户态以及用户态到内核态转变的过程中，都对内核锁进行了操作。</p><div class="note default">            <p><strong>Exercise 5.</strong> Apply the big kernel lock as described above, by calling <code>lock_kernel()</code> and <code>unlock_kernel()</code> at the proper locations.</p>          </div><h5 id="在指定位置为内核上锁-解锁"><a href="#在指定位置为内核上锁-解锁" class="headerlink" title="在指定位置为内核上锁/解锁"></a>在指定位置为内核上锁/解锁</h5><p>根据上面的提示，我们需要在特定位置上对内核锁进行加锁与解锁的操作。代码并不难，直接给出：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// - 1. Acquire the lock before the BSP wakes up the other CPUs</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">i386_init(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// Acquire the big kernel lock before waking up APs</span></span><br><span class="line">    <span class="comment">// Your code here:</span></span><br><span class="line">    lock_kernel();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Starting non-boot CPUs</span></span><br><span class="line">    boot_aps();</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// Schedule and run the first user environment!</span></span><br><span class="line">    sched_yield();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>目前我们尚无法验证是否正确，但是随着后面练习的推进，我们会验证锁的正确性</p><div class="note default">            <p><strong>Question</strong></p><p>It seems that using the big kernel lock guarantees that only one CPU can run the kernel code at a time. Why do we still need separate kernel stacks for each CPU? Describe a scenario in which using a shared kernel stack will go wrong, even with the protection of the big kernel lock.</p>          </div><p>如果CPU只是对内核栈进行读操作，那么倒是不会有什么影响，但是如果两个CPU都对内核栈进行写操作，最后那个CPU写入的内容会留下。尽管有锁保证两个CPU的操作顺序，但是前一个CPU的写入依然会被覆盖。</p><div class="note danger">            <p><strong>Challenge!</strong> </p><p>粗颗粒锁简单易用，但是限制了内核态的所有并发，效率较低。现代操作系统使用细颗粒锁来保护共享态中的不同的部分。细颗粒锁提高了并发性能，但是更困难，同时也更容易出错。请尝试设计合理的细颗粒锁，实现对于内核更加精细的控制。</p><p>提示：你可以考虑针对如下的共享部分设计细颗粒锁，并实现内核的访问控制：</p><ul><li>The page allocator.</li><li>The console driver.</li><li>The scheduler.</li><li>The inter-process communication (IPC) state that you will implement in the part C.</li></ul>          </div><h5 id="定义细颗粒锁以及加锁-解锁函数"><a href="#定义细颗粒锁以及加锁-解锁函数" class="headerlink" title="定义细颗粒锁以及加锁/解锁函数"></a>定义细颗粒锁以及加锁/解锁函数</h5><p>根据上面的提示，我们首先要针对共享部分，定义新的细颗粒锁。</p><h3 id="Round-Robin-调度（轮转调度）"><a href="#Round-Robin-调度（轮转调度）" class="headerlink" title="Round-Robin 调度（轮转调度）"></a>Round-Robin 调度（轮转调度）</h3><p>下一个任务是实施轮转调度，令内核轮转调度多个进程，调度过程如下：</p><ul><li>函数<code>sched_yield()</code> （在新的<code>kern/sched.c</code>中）选择一个新的要运行的进程。该函数在进程队列<code>envs[]</code>中环形顺序搜索， 搜索位置开始于前一个运行的进程之后（如果没有前一时刻的运行进程，那么就从进程队列起始位置搜索），并选择第一个状态为 <code>ENV_RUNNABLE</code> (see <code>inc/env.h</code>)的进程，并调用<code>env_run()</code>执行该进程</li><li><code>sched_yield()</code> 永远不应于同一时刻在两个CPU上运行同一个进程。根据进程状态<code>ENV_RUNNING</code>，我们可以判定当前进程是否在某个CPU（或当前CPU）上运行.</li><li>我们定义了一个新系统调用<code>sys_yield()</code>，用户可以通过该调用来调用内核<code>sched_yield()</code>函数，从而主动将CPU让给另一个进程。</li></ul><div class="note default">            <p><strong>Exercise 6.</strong> Implement round-robin scheduling in <code>sched_yield()</code> as described above. Don’t forget to modify <code>syscall()</code> to dispatch <code>sys_yield()</code>.</p><p>在<code>mp_main</code>中调用<code>sched_yield()</code>，修改<code>kern/init.c</code> 并创建三个以上的进程，这些进程都运行程序 <code>user/yield.c</code>。现在，运行<code>make qemu CPUS=2</code>，你应当能看到多个进程交互运行，每个执行了五次，然后退出</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line">Hello, I am environment 00001000.</span><br><span class="line">Hello, I am environment 00001001.</span><br><span class="line">Hello, I am environment 00001002.</span><br><span class="line">Back in environment 00001000, iteration 0.</span><br><span class="line">Back in environment 00001001, iteration 0.</span><br><span class="line">Back in environment 00001002, iteration 0.</span><br><span class="line">Back in environment 00001000, iteration 1.</span><br><span class="line">Back in environment 00001001, iteration 1.</span><br><span class="line">Back in environment 00001002, iteration 1.</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>After the <code>yield</code> programs exit, there will be no runnable environment in the system, the scheduler should invoke the JOS kernel monitor. If any of this does not happen, then fix your code before proceeding.</p>          </div><h5 id="框架编写"><a href="#框架编写" class="headerlink" title="框架编写"></a>框架编写</h5><p>任务6主要是实现轮询调度，然后在合适的地方调用该函数，找到下一个调度的进程。我们先完成框架的编写，在<code>mp_main</code>中调用<code>sched_yield()</code>，改进后的<code>mp_main</code>为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Setup code for APs</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">mp_main(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// We are in high EIP now, safe to switch to kern_pgdir </span></span><br><span class="line">    lcr3(PADDR(kern_pgdir));</span><br><span class="line">    cprintf(<span class="string">"SMP: CPU %d starting\n"</span>, cpunum());</span><br><span class="line"></span><br><span class="line">    lapic_init();           <span class="comment">// 初始化local apic</span></span><br><span class="line">    env_init_percpu();      <span class="comment">// 初始化每个CPU的进程</span></span><br><span class="line">    trap_init_percpu();     <span class="comment">// 初始化每个CPU的trap</span></span><br><span class="line">    xchg(&amp;thiscpu-&gt;cpu_status, CPU_STARTED); <span class="comment">// tell boot_aps() we're up</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Now that we have finished some basic setup, call sched_yield()</span></span><br><span class="line">    <span class="comment">// to start running processes on this CPU.  But make sure that</span></span><br><span class="line">    <span class="comment">// only one CPU can enter the scheduler at a time!</span></span><br><span class="line">    lock_kernel();</span><br><span class="line">    sched_yield();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后修改<code>kern/syscall.c</code>，在<code>syscall()</code>中添加对<code>sys_yield</code>的响应。</p><h5 id="轮询算法实现"><a href="#轮询算法实现" class="headerlink" title="轮询算法实现"></a>轮询算法实现</h5><div class="note default">            <p><strong>Question</strong></p><ol><li>In your implementation of <code>env_run()</code> you should have called <code>lcr3()</code>. Before and after the call to <code>lcr3()</code>, your code makes references (at least it should) to the variable <code>e</code>, the argument to <code>env_run</code>. Upon loading the <code>%cr3</code> register, the addressing context used by the MMU is instantly changed. But a virtual address (namely <code>e</code>) has meaning relative to a given address context—the address context specifies the physical address to which the virtual address maps. Why can the pointer <code>e</code> be dereferenced both before and after the addressing switch?</li><li>Whenever the kernel switches from one environment to another, it must ensure the old environment’s registers are saved so they can be restored properly later. Why? Where does this happen?</li></ol>          </div><div class="note danger">            <p><strong>Challenge!</strong> Add a less trivial scheduling policy to the kernel, such as a fixed-priority scheduler that allows each environment to be assigned a priority and ensures that higher-priority environments are always chosen in preference to lower-priority environments. If you’re feeling really adventurous, try implementing a Unix-style adjustable-priority scheduler or even a lottery or stride scheduler. (Look up “lottery scheduling” and “stride scheduling” in Google.)</p><p>Write a test program or two that verifies that your scheduling algorithm is working correctly (i.e., the right environments get run in the right order). It may be easier to write these test programs once you have implemented <code>fork()</code> and IPC in parts B and C of this lab.</p>          </div><div class="note danger">            <p><strong>Challenge!</strong> The JOS kernel currently does not allow applications to use the x86 processor’s x87 floating-point unit (FPU), MMX instructions, or Streaming SIMD Extensions (SSE). Extend the <code>Env</code> structure to provide a save area for the processor’s floating point state, and extend the context switching code to save and restore this state properly when switching from one environment to another. The <code>FXSAVE</code> and <code>FXRSTOR</code> instructions may be useful, but note that these are not in the old i386 user’s manual because they were introduced in more recent processors. Write a user-level test program that does something cool with floating-point.</p>          </div><h3 id="用于进程创建的系统调用"><a href="#用于进程创建的系统调用" class="headerlink" title="用于进程创建的系统调用"></a>用于进程创建的系统调用</h3><p>Although your kernel is now capable of running and switching between multiple user-level environments, it is still limited to running environments that the <em>kernel</em> initially set up. You will now implement the necessary JOS system calls to allow <em>user</em> environments to create and start other new user environments.</p><p>Unix provides the <code>fork()</code> system call as its process creation primitive. Unix <code>fork()</code> copies the entire address space of calling process (the parent) to create a new process (the child). The only differences between the two observable from user space are their process IDs and parent process IDs (as returned by <code>getpid</code> and <code>getppid</code>). In the parent, <code>fork()</code> returns the child’s process ID, while in the child, <code>fork()</code> returns 0. By default, each process gets its own private address space, and neither process’s modifications to memory are visible to the other.</p><p>You will provide a different, more primitive set of JOS system calls for creating new user-mode environments. With these system calls you will be able to implement a Unix-like <code>fork()</code> entirely in user space, in addition to other styles of environment creation. The new system calls you will write for JOS are as follows:</p><ul><li><p><code>sys_exofork</code>:</p><p>This system call creates a new environment with an almost blank slate: nothing is mapped in the user portion of its address space, and it is not runnable. The new environment will have the same register state as the parent environment at the time of the <code>sys_exofork</code> call. In the parent, <code>sys_exofork</code> will return the <code>envid_t</code> of the newly created environment (or a negative error code if the environment allocation failed). In the child, however, it will return 0. (Since the child starts out marked as not runnable, <code>sys_exofork</code> will not actually return in the child until the parent has explicitly allowed this by marking the child runnable using….)</p></li><li><p><code>sys_env_set_status</code>:</p><p>Sets the status of a specified environment to <code>ENV_RUNNABLE</code> or <code>ENV_NOT_RUNNABLE</code>. This system call is typically used to mark a new environment ready to run, once its address space and register state has been fully initialized.</p></li><li><p><code>sys_page_alloc</code>:</p><p>Allocates a page of physical memory and maps it at a given virtual address in a given environment’s address space.</p></li><li><p><code>sys_page_map</code>:</p><p>Copy a page mapping (<em>not</em> the contents of a page!) from one environment’s address space to another, leaving a memory sharing arrangement in place so that the new and the old mappings both refer to the same page of physical memory.</p></li><li><p><code>sys_page_unmap</code>:</p><p>Unmap a page mapped at a given virtual address in a given environment.</p></li></ul><p>For all of the system calls above that accept environment IDs, the JOS kernel supports the convention that a value of 0 means “the current environment.” This convention is implemented by <code>envid2env()</code> in <code>kern/env.c</code>.</p><p>We have provided a very primitive implementation of a Unix-like <code>fork()</code> in the test program <code>user/dumbfork.c</code>. This test program uses the above system calls to create and run a child environment with a copy of its own address space. The two environments then switch back and forth using <code>sys_yield</code> as in the previous exercise. The parent exits after 10 iterations, whereas the child exits after 20.</p><div class="note default">            <p><strong>Exercise 7.</strong> Implement the system calls described above in <code>kern/syscall.c</code> and make sure <code>syscall()</code> calls them. You will need to use various functions in <code>kern/pmap.c</code> and <code>kern/env.c</code>, particularly <code>envid2env()</code>. For now, whenever you call <code>envid2env()</code>, pass 1 in the <code>checkperm</code> parameter. Be sure you check for any invalid system call arguments, returning <code>-E_INVAL</code> in that case. Test your JOS kernel with <code>user/dumbfork</code> and make sure it works before proceeding.</p>          </div><div class="note danger">            <p><strong>Challenge!</strong> Add the additional system calls necessary to <em>read</em> all of the vital state of an existing environment as well as set it up. Then implement a user mode program that forks off a child environment, runs it for a while (e.g., a few iterations of <code>sys_yield()</code>), then takes a complete snapshot or <em>checkpoint</em> of the child environment, runs the child for a while longer, and finally restores the child environment to the state it was in at the checkpoint and continues it from there. Thus, you are effectively “replaying” the execution of the child environment from an intermediate state. Make the child environment perform some interaction with the user using <code>sys_cgetc()</code> or <code>readline()</code> so that the user can view and mutate its internal state, and verify that with your checkpoint/restart you can give the child environment a case of selective amnesia, making it “forget” everything that happened beyond a certain point.</p><p>This completes Part A of the lab; make sure it passes all of the Part A tests when you run make grade, and hand it in using make handin as usual. If you are trying to figure out why a particular test case is failing, run ./grade-lab4 -v, which will show you the output of the kernel builds and QEMU runs for each test, until a test fails. When a test fails, the script will stop, and then you can inspect <code>jos.out</code> to see what the kernel actually printed.</p>          </div><h2 id="第二部分：写复制"><a href="#第二部分：写复制" class="headerlink" title="第二部分：写复制"></a>第二部分：写复制</h2><p>As mentioned earlier, Unix provides the <code>fork()</code> system call as its primary process creation primitive. The <code>fork()</code> system call copies the address space of the calling process (the parent) to create a new process (the child).</p><p>xv6 Unix implements <code>fork()</code> by copying all data from the parent’s pages into new pages allocated for the child. This is essentially the same approach that <code>dumbfork()</code> takes. The copying of the parent’s address space into the child is the most expensive part of the <code>fork()</code> operation.</p><p>However, a call to <code>fork()</code> is frequently followed almost immediately by a call to <code>exec()</code> in the child process, which replaces the child’s memory with a new program. This is what the the shell typically does, for example. In this case, the time spent copying the parent’s address space is largely wasted, because the child process will use very little of its memory before calling <code>exec()</code>.</p><p>For this reason, later versions of Unix took advantage of virtual memory hardware to allow the parent and child to <em>share</em> the memory mapped into their respective address spaces until one of the processes actually modifies it. This technique is known as <em>copy-on-write</em>. To do this, on <code>fork()</code> the kernel would copy the address space <em>mappings</em> from the parent to the child instead of the contents of the mapped pages, and at the same time mark the now-shared pages read-only. When one of the two processes tries to write to one of these shared pages, the process takes a page fault. At this point, the Unix kernel realizes that the page was really a “virtual” or “copy-on-write” copy, and so it makes a new, private, writable copy of the page for the faulting process. In this way, the contents of individual pages aren’t actually copied until they are actually written to. This optimization makes a <code>fork()</code> followed by an <code>exec()</code> in the child much cheaper: the child will probably only need to copy one page (the current page of its stack) before it calls <code>exec()</code>.</p><p>In the next piece of this lab, you will implement a “proper” Unix-like <code>fork()</code> with copy-on-write, as a user space library routine. Implementing <code>fork()</code> and copy-on-write support in user space has the benefit that the kernel remains much simpler and thus more likely to be correct. It also lets individual user-mode programs define their own semantics for <code>fork()</code>. A program that wants a slightly different implementation (for example, the expensive always-copy version like <code>dumbfork()</code>, or one in which the parent and child actually share memory afterward) can easily provide its own.</p><h3 id="用户态页错误处理"><a href="#用户态页错误处理" class="headerlink" title="用户态页错误处理"></a>用户态页错误处理</h3><p>一个用户态的写时拷贝函数 <code>fork()</code> needs to know about page faults on write-protected pages, so that’s what you’ll implement first. Copy-on-write is only one of many possible uses for user-level page fault handling.</p><p>It’s common to set up an address space so that page faults indicate when some action needs to take place. For example, most Unix kernels initially map only a single page in a new process’s stack region, and allocate and map additional stack pages later “on demand” as the process’s stack consumption increases and causes page faults on stack addresses that are not yet mapped. A typical Unix kernel must keep track of what action to take when a page fault occurs in each region of a process’s space. For example, a fault in the stack region will typically allocate and map new page of physical memory. A fault in the program’s BSS region will typically allocate a new page, fill it with zeroes, and map it. In systems with demand-paged executables, a fault in the text region will read the corresponding page of the binary off of disk and then map it.</p><p>This is a lot of information for the kernel to keep track of. Instead of taking the traditional Unix approach, you will decide what to do about each page fault in user space, where bugs are less damaging. This design has the added benefit of allowing programs great flexibility in defining their memory regions; you’ll use user-level page fault handling later for mapping and accessing files on a disk-based file system.</p><h2 id="第三部分：抢占式多任务与进程间通信"><a href="#第三部分：抢占式多任务与进程间通信" class="headerlink" title="第三部分：抢占式多任务与进程间通信"></a>第三部分：抢占式多任务与进程间通信</h2><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev10.pdf">xv6 bookc</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/labs/lab3/">Lab3</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li><li id="fn:3"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">3.</span><span style="display: inline-block; vertical-align: top;"><a href="https://www.cnblogs.com/gatsby123/p/9838304.html">Lab3实验答案</a></span><a href="#fnref:3" rev="footnote"> ↩</a></li><li id="fn:4"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">4.</span><span style="display: inline-block; vertical-align: top;"><a href="https://yangminz.github.io/images/OperatingSys/Lab3Report.html">Yangminz Blog</a></span><a href="#fnref:4" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;剪不断，理还乱&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
      <category term="课程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>内联汇编</title>
    <link href="http://yoursite.com/2021/04/28/%E5%B7%A5%E5%85%B7/Assembly/%E5%86%85%E8%81%94%E6%B1%87%E7%BC%96/"/>
    <id>http://yoursite.com/2021/04/28/工具/Assembly/内联汇编/</id>
    <published>2021-04-28T05:30:48.545Z</published>
    <updated>2021-04-28T06:05:51.235Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>asm asm-qualifiers ( AssemblerTemplate<br>                 : OutputOperands<br>                 [ : InputOperands<br>                 [ : Clobbers ] ])</p></blockquote><a id="more"></a><p>内联汇编允许我们在C或C++代码中插入汇编语句，本文将对内联汇编的相关使用方法进行总结。</p><h2 id="基本汇编——不带有运算数"><a href="#基本汇编——不带有运算数" class="headerlink" title="基本汇编——不带有运算数"></a>基本汇编——不带有运算数</h2><h3 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h3><p>一个基本内联汇编语句语法如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">asm</span> [qualifiers] ( instructions )</span><br></pre></td></tr></table></figure><h3 id="修饰定语-Qualifiers"><a href="#修饰定语-Qualifiers" class="headerlink" title="修饰定语(Qualifiers)"></a>修饰定语(Qualifiers)</h3><p>修饰定语可添加inline，那么asm的语句将被优化，尽可能占用少的空间</p><h3 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h3><p>asm的参数为汇编语句，需要采用””括起来，并使用<code>\n\t</code>换行</p><h3 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">asm (&quot;.globl func\n\t&quot;</span><br><span class="line">        &quot;.type func, @function\n\t&quot;</span><br><span class="line">        &quot;func:\n\t&quot;</span><br><span class="line">        &quot;.cfi_startproc\n\t&quot;</span><br><span class="line">        &quot;movl $7, %eax\n\t&quot;</span><br><span class="line">        &quot;ret\n\t&quot;</span><br><span class="line">        &quot;.cfi_endproc&quot;);</span><br></pre></td></tr></table></figure><h2 id="扩展汇编"><a href="#扩展汇编" class="headerlink" title="扩展汇编"></a>扩展汇编</h2><h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><p>相比基本汇编，扩展汇编更加灵活高效，但是必须放在函数中。扩展汇编的基本语法如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">asm</span> ( assembler <span class="keyword">template</span> </span><br><span class="line">    : output operands                  <span class="comment">/* optional 输出操作数*/</span>  </span><br><span class="line">    : input operands                   <span class="comment">/* optional 输入操作数*/</span></span><br><span class="line">    : <span class="built_in">list</span> of clobbered registers      <span class="comment">/* optional 破坏的寄存器*/</span></span><br><span class="line">    );</span><br></pre></td></tr></table></figure><h3 id="assembler-template"><a href="#assembler-template" class="headerlink" title="assembler template"></a>assembler template</h3><p>assembler template由汇编指令组成</p><h3 id="输入-输出操作数"><a href="#输入-输出操作数" class="headerlink" title="输入/输出操作数"></a>输入/输出操作数</h3><h4 id="操作数顺序"><a href="#操作数顺序" class="headerlink" title="操作数顺序"></a>操作数顺序</h4><p>我们在内联汇编中能看到<code>%0, %1, %2...</code>，这些数表示传入的第1个、第2个、…、第<code>n</code>个操作数，顺序即输出、输入操作数在内联汇编中的排列顺序。</p><h4 id="操作数限制"><a href="#操作数限制" class="headerlink" title="操作数限制"></a>操作数限制</h4><p>我们可以用限制词限制输入输出操作数的位置，限制词的语法为：</p><ul><li><code>: &quot;=c&quot; (output)</code></li><li><code>: &quot;c&quot; (input)</code></li></ul><p>常用的限制词如下：</p><ul><li><code>r</code>：表示使用任意寄存器保存操作数</li></ul><h3 id="例子2"><a href="#例子2" class="headerlink" title="例子2"></a>例子<sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup></h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> a=<span class="number">10</span>, b;</span><br><span class="line"><span class="keyword">asm</span> (<span class="string">"movl %1, %%eax; </span></span><br><span class="line"><span class="string">     movl %%eax, %0;"</span></span><br><span class="line">     :<span class="string">"=r"</span>(b)        <span class="comment">/* output */</span></span><br><span class="line">     :<span class="string">"r"</span>(a)         <span class="comment">/* input */</span></span><br><span class="line">     :<span class="string">"%eax"</span>         <span class="comment">/* clobbered register */</span></span><br><span class="line">    );</span><br></pre></td></tr></table></figure><p>这段代码解释如下：</p><ul><li><code>b</code>是输出操作数，由<code>%0</code>表示第一个传入的表示输入或输出操作数的变量</li><li><code>a</code>是输入操作数，由<code>%1</code>表示第二个传入的表示输入或输出操作数的变量</li><li><code>r</code>为操作数限制，表示使用任意寄存器保存操作数</li><li>寄存器前有两个%%，这是为了区分操作数和寄存器，操作数只有一个%</li><li>被破坏的寄存器是<code>%eax</code>，告知GCC<code>eax</code>的值发生了改变，GCC将不会用这些寄存器保存值</li></ul><p>当上面的代码运行完成后，<code>b</code>将保存更新后的输出值</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://gcc.gnu.org/onlinedocs/gcc/Basic-Asm.html#Basic-Asm">Basic Asm (Using the GNU Compiler Collection (GCC))</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://www.ibiblio.org/gferg/ldp/GCC-Inline-Assembly-HOWTO.html#s1">GCC-Inline-Assembly-HOWTO (ibiblio.org)</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;asm asm-qualifiers ( AssemblerTemplate&lt;br&gt;                 : OutputOperands&lt;br&gt;                 [ : InputOperands&lt;br&gt;                 [ : Clobbers ] ])&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="工具" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/"/>
    
      <category term="Assembly" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/Assembly/"/>
    
    
  </entry>
  
  <entry>
    <title>Unity学习资料总结</title>
    <link href="http://yoursite.com/2021/04/27/%E5%B7%A5%E5%85%B7/Unity/Unity%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%80%BB%E7%BB%93/"/>
    <id>http://yoursite.com/2021/04/27/工具/Unity/Unity学习资料总结/</id>
    <published>2021-04-27T12:44:16.429Z</published>
    <updated>2021-04-27T13:59:51.311Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>本文对Unity相关学习资料进行了总结。</p><a id="more"></a><h2 id="视频资料"><a href="#视频资料" class="headerlink" title="视频资料"></a>视频资料</h2><h3 id="How-to-make-a-2D-Game-in-Unity-YouTube"><a href="#How-to-make-a-2D-Game-in-Unity-YouTube" class="headerlink" title="How to make a 2D Game in Unity - YouTube"></a><a href="https://www.youtube.com/watch?v=on9nwbZngyw&amp;list=RDCMUCYbK_tjZ2OrIZFBvU6CCMiA&amp;start_radio=1&amp;t=2&amp;ab_channel=Brackeys">How to make a 2D Game in Unity - YouTube</a></h3><div class="table-container"><table><thead><tr><th>课程</th><th>内容</th><th></th></tr></thead><tbody><tr><td><a href="https://www.youtube.com/watch?v=on9nwbZngyw&amp;list=RDCMUCYbK_tjZ2OrIZFBvU6CCMiA&amp;start_radio=1&amp;t=2&amp;ab_channel=Brackeys">How to make a 2D Game in Unity - YouTube</a></td><td>介绍Unity工程创建、资源下载（Ctrl + 9），Asset导入</td><td></td></tr><tr><td></td><td></td><td></td></tr><tr><td></td><td></td></tr></tbody></table></div><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文对Unity相关学习资料进行了总结。&lt;/p&gt;
    
    </summary>
    
      <category term="工具" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/"/>
    
      <category term="Unity" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/Unity/"/>
    
    
  </entry>
  
  <entry>
    <title>减少编程错误</title>
    <link href="http://yoursite.com/2021/04/27/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/%E5%87%8F%E5%B0%91%E7%BC%96%E7%A8%8B%E9%94%99%E8%AF%AF/"/>
    <id>http://yoursite.com/2021/04/27/计算机/软件工程/减少编程错误/</id>
    <published>2021-04-27T03:42:17.769Z</published>
    <updated>2021-04-27T05:34:57.287Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>if(connection == nullptr)</p><p>​    goto fail;</p><p>​    goto fail;</p></blockquote><a id="more"></a><h2 id="安全编码与未定义行为"><a href="#安全编码与未定义行为" class="headerlink" title="安全编码与未定义行为"></a>安全编码与未定义行为</h2><h3 id="未定义行为"><a href="#未定义行为" class="headerlink" title="未定义行为"></a>未定义行为</h3><h4 id="缓冲区溢出"><a href="#缓冲区溢出" class="headerlink" title="缓冲区溢出"></a>缓冲区溢出</h4><p>查看下面的代码，其输出结果是什么？</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">int</span> a[<span class="number">4</span>] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt;= <span class="number">4</span>; i++)&#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>产生了越界访问，导致了未定义行为。建议：</p><ul><li>不要使用超出边界的指针或数组下标（这不是废话吗）</li><li>缓冲区溢出问题在CWE的数据库中排名第二</li></ul><h4 id="访问已经销毁的局部空间（悬空指针）"><a href="#访问已经销毁的局部空间（悬空指针）" class="headerlink" title="访问已经销毁的局部空间（悬空指针）"></a>访问已经销毁的局部空间（悬空指针）</h4><p>这种最常见的就是在一个函数中定义一段空间，然后返回这段空间的首地址。由于这段空间在函数返回后就被销毁，因此这个地址也成为了悬空指针。</p><h4 id="超出类型能容纳的范围"><a href="#超出类型能容纳的范围" class="headerlink" title="超出类型能容纳的范围"></a>超出类型能容纳的范围</h4><p>查看下面的代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int32_t</span> Average(<span class="keyword">int32_t</span> x, <span class="keyword">int32_t</span> y)&#123;</span><br><span class="line">    <span class="keyword">return</span> (x+y)/<span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>两个整型相加，可能会溢出。建议：</p><ul><li>确保有符号整数运算不溢出，避免无符号整数运算产生的回绕</li></ul><h4 id="有符号和无符号混用"><a href="#有符号和无符号混用" class="headerlink" title="有符号和无符号混用"></a>有符号和无符号混用</h4><p>建议：</p><ul><li>用来表示数值时，不要用char</li><li>避免混用有符号和无符号数，如果不得不一起用，显式指定转换类型</li><li>类型转换导致的溢出问题在CWE中排11位，有超过800条漏洞</li></ul><h3 id="未定义行为后果"><a href="#未定义行为后果" class="headerlink" title="未定义行为后果"></a>未定义行为后果</h3><p>未定义行为的一个典型后果是，由于访问了非法内存，所以可以通过精心设计构造的输入，修改栈中保存的返回地址，从而跳转到黑客想要跳转的区域。</p><h2 id="表达式与变量"><a href="#表达式与变量" class="headerlink" title="表达式与变量"></a>表达式与变量</h2><h3 id="全局变量初始化"><a href="#全局变量初始化" class="headerlink" title="全局变量初始化"></a>全局变量初始化</h3><p>假设我们在<code>a.cpp</code>中有一个全局变量</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">int g_x = 1;</span><br></pre></td></tr></table></figure><p>而在<code>b.cpp</code>中，也有一个全局变量，且使用<code>g_x</code>进行定义</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> g_x;</span><br><span class="line"><span class="keyword">int</span> g_y = <span class="number">10</span> + g_x;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; g_y &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里就有了问题，两个不同文件定义的全局变量，其初始化顺序是不确定的。建议：</p><ul><li>避免全局变量出现初始化顺序依赖的情况</li></ul><h2 id="代码度量"><a href="#代码度量" class="headerlink" title="代码度量"></a>代码度量</h2><h3 id="代码的组织"><a href="#代码的组织" class="headerlink" title="代码的组织"></a>代码的组织</h3><ul><li>程序必须为阅读者编写，只是顺便用于机器执行</li><li>短小的函数总是更简洁、更容易阅读</li><li>功能单一的函数更容易复用，简洁明了的代码更容易维护</li></ul><h2 id="业界指南、规范与工具"><a href="#业界指南、规范与工具" class="headerlink" title="业界指南、规范与工具"></a>业界指南、规范与工具</h2><h3 id="业界指南"><a href="#业界指南" class="headerlink" title="业界指南"></a>业界指南</h3><ul><li>谷歌C++风格指南<sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>，以风格约定与习惯引导为主，不涉及消除安全问题</li><li>SEI CERT C++ Coding Standard<sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>，以消除安全隐患为目的，不涉及风格约定，内容太多，建议作为字典</li><li>C++ Core Guideline<sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>，重点为如何合理应用现代C++特性，这个非常值得一读</li></ul><h3 id="工具"><a href="#工具" class="headerlink" title="工具"></a>工具</h3><h4 id="编译器"><a href="#编译器" class="headerlink" title="编译器"></a>编译器</h4><p>gcc等编译器本身就能检查很多问题，且几乎不会误报，我们应当做到保持编译器警告清零</p><h4 id="辅助检查工具"><a href="#辅助检查工具" class="headerlink" title="辅助检查工具"></a>辅助检查工具</h4><ul><li>PC-link Plus：以CERT、MISRA等业界规范作为准则，能检查上千条规则，但是有误报和漏报</li><li>CPPCheck：开源静态检查工具，主要检查未定义行为，以减少误报为设计目标</li><li>华为云代码检查：<a href="https://www.huaweicloud.com/product/codecheck.html">代码检查CodeCheck_精准定位_代码缺陷_安全检查_华为云 (huaweicloud.com)</a></li></ul><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://zh-google-styleguide.readthedocs.io/en/latest/google-cpp-styleguide/contents/">C++ 风格指南 - 内容目录 — Google 开源项目风格指南 (zh-google-styleguide.readthedocs.io)</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://resources.sei.cmu.edu/downloads/secure-coding/assets/sei-cert-cpp-coding-standard-2016-v01.pdf">SEI CERT C++ Coding Standard (2016 Edition) (cmu.edu)</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li><li id="fn:3"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">3.</span><span style="display: inline-block; vertical-align: top;"><a href="https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines">C++ Core Guidelines (isocpp.github.io)</a></span><a href="#fnref:3" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;if(connection == nullptr)&lt;/p&gt;
&lt;p&gt;​    goto fail;&lt;/p&gt;
&lt;p&gt;​    goto fail;&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="软件工程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E8%BD%AF%E4%BB%B6%E5%B7%A5%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>QML定位与布局</title>
    <link href="http://yoursite.com/2021/04/26/%E5%B7%A5%E5%85%B7/Qt/%E5%AE%9A%E4%BD%8D%E4%B8%8E%E5%B8%83%E5%B1%80/"/>
    <id>http://yoursite.com/2021/04/26/工具/Qt/定位与布局/</id>
    <published>2021-04-26T14:06:32.235Z</published>
    <updated>2021-04-27T03:38:26.579Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>本文将针对QML组件中定位与布局的方法进行讲解</p><a id="more"></a><h2 id="anchor"><a href="#anchor" class="headerlink" title="anchor"></a>anchor</h2><p>在讲解定位与布局之前，我们先来看看锚(anchor)，利用锚可以方便地实现相对位置排布。每一个item都包含有七个不可见锚线</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文将针对QML组件中定位与布局的方法进行讲解&lt;/p&gt;
    
    </summary>
    
      <category term="工具" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/"/>
    
      <category term="Qt" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/Qt/"/>
    
    
  </entry>
  
  <entry>
    <title>Qt知识库导航</title>
    <link href="http://yoursite.com/2021/04/26/%E5%B7%A5%E5%85%B7/Qt/Qt%E7%9F%A5%E8%AF%86%E5%BA%93%E5%AF%BC%E8%88%AA/"/>
    <id>http://yoursite.com/2021/04/26/工具/Qt/Qt知识库导航/</id>
    <published>2021-04-26T13:08:06.743Z</published>
    <updated>2021-04-26T14:04:08.885Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>花里胡哨的，但是我喜欢</p></blockquote><a id="more"></a><h2 id="课程视频"><a href="#课程视频" class="headerlink" title="课程视频"></a>课程视频</h2><h3 id="qml-tutorial-for-beginners"><a href="#qml-tutorial-for-beginners" class="headerlink" title="qml tutorial for beginners"></a>qml tutorial for beginners</h3><div class="table-container"><table><thead><tr><th>章节</th><th>内容</th><th>完成情况</th></tr></thead><tbody><tr><td>01 Introduction</td><td></td><td>完成</td></tr><tr><td>07 Positioning</td><td>Anchors</td><td>完成</td></tr><tr><td></td><td></td></tr></tbody></table></div><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;花里胡哨的，但是我喜欢&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="工具" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/"/>
    
      <category term="Qt" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/Qt/"/>
    
    
  </entry>
  
  <entry>
    <title>致谢</title>
    <link href="http://yoursite.com/2021/04/24/%E7%94%9F%E6%B4%BB/%E9%9A%8F%E7%AC%94/%E8%87%B4%E8%B0%A2/"/>
    <id>http://yoursite.com/2021/04/24/生活/随笔/致谢/</id>
    <published>2021-04-24T07:49:19.376Z</published>
    <updated>2021-04-24T07:50:06.175Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>合肥的春天多雨，那雨是细碎的，残留着一丝冬日的清凉，像是拂面而来的雾，在我的身上笼罩着一层湿润的茧。早樱被这雨水滋润，在一夜之间拥簇着开了，花骨朵细密地拥挤在枝头，成了一片淡红的轻云。我曾经在北方生活，习惯了那里的干燥，初来南城，总觉得潮气很重。后来也渐渐适应了，觉得这气候就像是也西湖里的水，温润平淡，让人一点点放松起来。</p><a id="more"></a><p>我住在学校东南角的一隅，楼下通往系楼的小道上，齐整地栽种着两排银杏，我便每日顺着这银杏小路走去实验室。夏天的时候，叶子变成了浓郁的绿色，正午太阳浓烈，我走在路上，被银杏的叶子遮蔽着，总算在焦热的气氛里获得了一丝清凉。那一阵是写论文的日子，丛老师的办公室在二楼，窗外是一片厚实的绿荫，平日里她便一个人在这里办公。她会把要写的内容交代给我，然后每个星期给她看一回。我交出去的论文她看过后，过几日便叫我过去，她已将论文从头到末细细地添加了许多批注，再一一给我讲解订正。现在回想起来，我们平素之间的交流大多都是这样的形式吧。</p><p>其余的时间我基本在实验室中度过，汪涛师弟向来比我早到，等我推门进来，他已经开始了一天的工作，随后其他人便也陆陆续续来了。平日里实验室是安静的，偶尔能听到一阵敲击键盘的清脆的噼啪声，又或者角落里隐隐约约的风扇响动，窗外不时传来一两声悠远的鸟鸣。夏日的暴雨来得急促，这时周围便淹没在喧嚣的雨声里，我便停下手里的工作，看着楼下那一方浅浅的池塘，在大雨中激起层层碧波。</p><p>银杏在秋天里结满了白果，起初果皮是光滑圆润的，待果子熟透了，那表面便皱缩起来。一阵风过去，果子便掉落下来，若被人不小心踩上了，就会散发出白果特有的青涩的味道。桂花也开了，空气中弥散着桂花淡淡的清甜的香气，如果凑近了闻，那芬芳萦绕在了肺腑里，让人一天都觉得清爽。正是吃蟹的季节，丁娇总会给我和张坤带上几只自家鱼塘养的螃蟹，蟹黄浓郁地如同融化了一般，轻抿一口，鲜味便久久地弥留在唇齿之间。蟹膏金黄时，最宜饮美酒。这便是秋天里一份值得留恋的味道了。</p><p>十月过后，天气渐渐冷了下来，合肥的冬天少不了连绵的阴雨，寒意沾着雨水透过衣服，渗入了身体的最深处。这个季节，妈妈在电话里便多了几分叮嘱，总是提醒我记得多穿一些。听着她的声音，我想念起在家里的感觉了。我想起厨房里明亮的灯，还有锅里沸滚的粥，一家人坐在餐桌前，吃着热腾腾的饭菜，讲讲今天的趣事。求学在外，曾经稀松平常的日子，此刻却也成为一心一意的期待。冬季难得的晴日里，天空是清澈而冷冽的，夜晚来临，几分透亮的星辰开始点缀在这浓黑的苍穹下。送晓敏回宿舍的时候，我总是指着天上的星星问她，“你知道那是什么星星吗？”“我知道，你都问过我好多次了，是木星！”她笑着说。我看着她的眼睛，路灯的光倒映在她的眼眸里，像是有一条星河在缓缓流淌。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;合肥的春天多雨，那雨是细碎的，残留着一丝冬日的清凉，像是拂面而来的雾，在我的身上笼罩着一层湿润的茧。早樱被这雨水滋润，在一夜之间拥簇着开了，花骨朵细密地拥挤在枝头，成了一片淡红的轻云。我曾经在北方生活，习惯了那里的干燥，初来南城，总觉得潮气很重。后来也渐渐适应了，觉得这气候就像是也西湖里的水，温润平淡，让人一点点放松起来。&lt;/p&gt;
    
    </summary>
    
      <category term="生活" scheme="http://yoursite.com/categories/%E7%94%9F%E6%B4%BB/"/>
    
      <category term="随笔" scheme="http://yoursite.com/categories/%E7%94%9F%E6%B4%BB/%E9%9A%8F%E7%AC%94/"/>
    
    
  </entry>
  
  <entry>
    <title>MIT6828-HW8-User level threads</title>
    <link href="http://yoursite.com/2021/04/21/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/MIT6828-HW8/"/>
    <id>http://yoursite.com/2021/04/21/计算机/操作系统/课程/MIT6828-HW8/</id>
    <published>2021-04-21T12:45:17.876Z</published>
    <updated>2021-04-21T12:47:39.792Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>在这个作业中，我们将实现一个用户层线程包，实现线程间切换。</p><a id="more"></a><h2 id="线程切换"><a href="#线程切换" class="headerlink" title="线程切换"></a>线程切换</h2><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev10.pdf">xv6 book</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-syscall.html">Homework: xv6 system calls (mit.edu)</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在这个作业中，我们将实现一个用户层线程包，实现线程间切换。&lt;/p&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
      <category term="课程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>缺页异常</title>
    <link href="http://yoursite.com/2021/04/20/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E7%BC%BA%E9%A1%B5%E5%BC%82%E5%B8%B8/"/>
    <id>http://yoursite.com/2021/04/20/计算机/操作系统/缺页异常/</id>
    <published>2021-04-20T12:19:25.255Z</published>
    <updated>2021-04-20T12:58:20.867Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>她轻轻地把鱼儿放在那池清水里，鱼儿摇了摇尾巴，游走了，只剩下她随风摇动的裙摆和水面微微泛起的涟漪</p></blockquote><a id="more"></a><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><h3 id="缺页异常种类"><a href="#缺页异常种类" class="headerlink" title="缺页异常种类"></a>缺页异常种类</h3><p>缺页异常一般由如下两种情况触发：</p><ul><li>编程错误引起的异常</li><li>由于懒分配导致的引用属于进程但是尚未分配物理页框的页</li></ul><h2 id="缺页异常处理程序"><a href="#缺页异常处理程序" class="headerlink" title="缺页异常处理程序"></a>缺页异常处理程序</h2><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href=""></a></span><a href="#fnref:1" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;她轻轻地把鱼儿放在那池清水里，鱼儿摇了摇尾巴，游走了，只剩下她随风摇动的裙摆和水面微微泛起的涟漪&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
    
  </entry>
  
  <entry>
    <title>如何成为一名优秀的软件工程师</title>
    <link href="http://yoursite.com/2021/04/20/%E5%B7%A5%E5%85%B7/C++/%E8%A7%84%E8%8C%83/%E4%BB%A3%E7%A0%81%E7%AE%80%E6%B4%81%E4%B9%8B%E9%81%93/"/>
    <id>http://yoursite.com/2021/04/20/工具/C++/规范/代码简洁之道/</id>
    <published>2021-04-20T08:38:14.310Z</published>
    <updated>2021-05-10T11:50:09.318Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>普天之下，哪怕有一个也好，必须寻找出能俘获自己这颗心的伟大的东西，美丽的东西，或是慈祥的东西。</p></blockquote><a id="more"></a><h2 id="重构的作用"><a href="#重构的作用" class="headerlink" title="重构的作用"></a>重构的作用</h2><p>绝大多数的人，都无法做到一下就写出漂亮、完美的代码，因此我们才需要重构，重构使得我们能够逐步地对代码进行反省，从而令其更加简洁，逐渐趋于完美。可以把代码创造比作雕刻的过程，如果第一次的编写是开模，那么后续的重构就是一点点地精细化，最终创造出精美的艺术品。</p><h2 id="优秀特质"><a href="#优秀特质" class="headerlink" title="优秀特质"></a>优秀特质</h2><h3 id="良好习惯"><a href="#良好习惯" class="headerlink" title="良好习惯"></a>良好习惯</h3><p>作为一个工程师，我们需要保持如下良好的习惯：</p><ul><li>专注可用性：以代码功能作为终极追求，最终软件是给用户使用的，一定要将用户放在心中</li><li>善用现有的好代码，一个好的工程师，会专注于三种基本重用，不要重复造轮子，除非以学习为目的<ul><li>内部基础架构重用</li><li>第三方函数库</li><li>大型软件服务商提供的服务</li></ul></li><li>代码高可维护性，代码每一行都必须恰如其分地展现其目的，不要写糟糕的代码，哪怕时间再紧张</li><li>编写测试：测试的目的就是为了创建能够正常工作的系统</li></ul><h3 id="持续学习"><a href="#持续学习" class="headerlink" title="持续学习"></a>持续学习</h3><p>愿意学习新语言、新函数库和新方法，用最新的武器武装自己。</p><h3 id="如何变得优秀"><a href="#如何变得优秀" class="headerlink" title="如何变得优秀"></a>如何变得优秀</h3><ul><li>精益求精：不要妥协，写烂代码</li><li>甘于磨练：自己解决一个问题，不要一开始就求助别人，反复磨练，不断提升自己的能力</li><li>不断学习：</li><li>分享知识：</li><li>乐于助人：</li><li>从容耐心：欲速则不达，成神之路是一条漫长的路</li></ul><h2 id="扩展学习资料"><a href="#扩展学习资料" class="headerlink" title="扩展学习资料"></a>扩展学习资料</h2><p>C++程序设计语言</p><p>C++ primer</p><p>C++语言的设计和演化</p><p>C++编程思想</p><p>More Effective C++</p><p>C++编程艺术</p><p>A philosophy of software design</p><p>设计原则&amp;设计模式</p><p>重构：改善既有代码的设计</p><p>零信任架构</p>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;普天之下，哪怕有一个也好，必须寻找出能俘获自己这颗心的伟大的东西，美丽的东西，或是慈祥的东西。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="工具" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/"/>
    
      <category term="C++" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/C/"/>
    
      <category term="规范" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/C/%E8%A7%84%E8%8C%83/"/>
    
    
  </entry>
  
  <entry>
    <title>C++命名规范</title>
    <link href="http://yoursite.com/2021/04/20/%E5%B7%A5%E5%85%B7/C++/%E8%A7%84%E8%8C%83/C++%E5%91%BD%E5%90%8D%E8%A7%84%E8%8C%83/"/>
    <id>http://yoursite.com/2021/04/20/工具/C++/规范/C++命名规范/</id>
    <published>2021-04-20T08:17:56.340Z</published>
    <updated>2021-04-26T12:04:40.447Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>如果脏了还用，就莫不如一开始就用带颜色的。白的就要纯白才行。</p></blockquote><a id="more"></a><p>本文根据<a href="https://google.github.io/styleguide/cppguide.html">Google C++ Style Guide</a>，对其中相关命名规范进行总结。</p><p>基本的命名规则为：</p><ul><li>尽量不要用缩写，除非人尽皆知</li><li>变量和类型用名词，函数用动词</li><li>尽量使用描述性词语，做到见名知意</li></ul><div class="table-container"><table><thead><tr><th>项目</th><th>子项目</th><th>命名方式</th><th>例子</th></tr></thead><tbody><tr><td></td><td>头文件宏定义</td><td>全部大写，PROJECT_PATH_FILE_H_</td><td>JOS_KERN_ENV_H_</td></tr><tr><td></td><td>命名空间</td><td>用小写，且基于项目进行命名</td><td></td></tr><tr><td></td><td>文件</td><td>文件名全部小写，且用_分割</td><td>url_table</td></tr><tr><td></td><td>类型</td><td>类名采用每个单词首字母大写的方式</td><td>MyClass</td></tr><tr><td>变量</td><td>普通变量</td><td>变量名全部小写，且用_分割</td><td>table_name</td></tr><tr><td></td><td>类成员变量</td><td>变量名全部小写，且用_分割，最后加一个下划线</td><td>table_name_</td></tr><tr><td></td><td>全局变量</td><td>在变量名前加<code>g_</code></td><td>g_table_name</td></tr><tr><td></td><td>常量</td><td>k起头，后面为每个单词首字母大写</td><td>kDaysInAWeek</td></tr><tr><td>函数</td><td>普通函数</td><td>每个单词首字母大写</td><td>AddTableEntry()</td></tr><tr><td></td><td>getter/setter函数</td><td>与要获得的变量名相匹配</td><td>get_num_entries/set_num_entries</td></tr></tbody></table></div><h2 id="reference"><a href="#reference" class="headerlink" title="reference"></a>reference</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://google.github.io/styleguide/cppguide.html">Google C++ Style Guide</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;如果脏了还用，就莫不如一开始就用带颜色的。白的就要纯白才行。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="工具" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/"/>
    
      <category term="C++" scheme="http://yoursite.com/categories/%E5%B7%A5%E5%85%B7/C/"/>
    
    
  </entry>
  
  <entry>
    <title>MIT 6006 课程大纲</title>
    <link href="http://yoursite.com/2021/04/05/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E7%AE%97%E6%B3%95/%E8%AF%BE%E7%A8%8B/MIT6006-%E8%AF%BE%E7%A8%8B%E5%A4%A7%E7%BA%B2/"/>
    <id>http://yoursite.com/2021/04/05/计算机/算法/课程/MIT6006-课程大纲/</id>
    <published>2021-04-05T06:44:40.791Z</published>
    <updated>2021-04-05T06:58:21.566Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>人的新鲜感总是难以为继，我们总是会不经意间开始厌倦已经熟悉的人和事</p></blockquote><a id="more"></a><p>&lt;iframe src=”<a href="https://www.youtube.com/v/Nz1KZXbg">https://www.youtube.com/v/Nz1KZXbg</a></p><h2 id="课程安排"><a href="#课程安排" class="headerlink" title="课程安排"></a>课程安排</h2><div class="table-container"><table><thead><tr><th style="text-align:left">LEC #</th><th style="text-align:left">TOPICS</th><th style="text-align:left">KEY DATES</th></tr></thead><tbody><tr><td style="text-align:left"><strong>Unit 1: Introduction</strong></td><td style="text-align:left"></td><td style="text-align:left"></td></tr><tr><td style="text-align:left">1</td><td style="text-align:left">Algorithmic thinking, peak finding</td><td style="text-align:left">Problem set 1 out</td></tr><tr><td style="text-align:left">2</td><td style="text-align:left">Models of computation, Python cost model, document distance</td><td style="text-align:left"></td></tr><tr><td style="text-align:left"><strong>Unit 2: Sorting and Trees</strong></td><td style="text-align:left"></td><td style="text-align:left"></td></tr><tr><td style="text-align:left">3</td><td style="text-align:left">Insertion sort, merge sort</td><td style="text-align:left"></td></tr><tr><td style="text-align:left">4</td><td style="text-align:left">Heaps and heap sort</td><td style="text-align:left"></td></tr><tr><td style="text-align:left">5</td><td style="text-align:left">Binary search trees, BST sort</td><td style="text-align:left"></td></tr><tr><td style="text-align:left">6</td><td style="text-align:left">AVL trees, AVL sort</td><td style="text-align:left"></td></tr><tr><td style="text-align:left">7</td><td style="text-align:left">Counting sort, radix sort, lower bounds for sorting and searching</td><td style="text-align:left"></td></tr><tr><td style="text-align:left"><strong>Unit 3: Hashing</strong></td><td style="text-align:left"></td><td style="text-align:left"></td></tr><tr><td style="text-align:left">8</td><td style="text-align:left">Hashing with chaining</td><td style="text-align:left"></td></tr><tr><td style="text-align:left">9</td><td style="text-align:left">Table doubling, Karp-Rabin</td><td style="text-align:left"></td></tr><tr><td style="text-align:left">10</td><td style="text-align:left">Open addressing, cryptographic hashing</td></tr></tbody></table></div><h2 id="课程阅读"><a href="#课程阅读" class="headerlink" title="课程阅读"></a>课程阅读</h2><div class="table-container"><table><thead><tr><th style="text-align:left">课程</th><th style="text-align:left">内容</th><th style="text-align:center">完成情况</th></tr></thead><tbody><tr><td style="text-align:left">LEC1</td><td style="text-align:left">阅读：课程大纲</td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">观看：Unix系统介绍</td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left">LEC2</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-x86.pdf">PC hardware and x86 programming slides</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-x86.html">notes</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">Bootstrap/PC hardware appendices and the related xv6 source files</a></td><td style="text-align:center">完成，但是剩下三个题目不会</td></tr><tr><td style="text-align:left">LEC3</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/gdb_slides.pdf">gdb (mit.edu)</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：K&amp;R2.9、5.1-5.5、6.4章节</td><td style="text-align:center">完成，2.9节三个练习没做</td></tr><tr><td style="text-align:left">LEC4</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-shell.txt">Shell &amp; OS organization</a></td><td style="text-align:center">剩下三个挑战问题和两个问答题没做</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">chapter 0 of the xv6 book</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left">LEC5</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-internal.txt">Isolation mechanisms</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">Chapter 1: Operating system organization” and the related xv6 source files</a></td><td style="text-align:center">读到了22页</td></tr><tr><td style="text-align:left">LEC6</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-vm.md">Virtual memory (1)</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/x86_translation_and_registers.pdf">page table translation and registers</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-vm.pdf">slides</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">Chapter 2: Page Tables</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left">LEC7</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-usevm.md">Virtual memory (2)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-josmem.html">JOS virtual memory layout</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-usingvm.pdf">slides</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC8</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-interrupt.txt">System calls, interrupts, exceptions</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/x86_idt.pdf">IDT</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">Traps, interrupts, and drivers” and the related xv6 source files</a></td><td style="text-align:center">大致浏览了一遍，代码还没看</td></tr><tr><td style="text-align:left">LEC9</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-lockv2.txt">Multiprocessors and locking (mit.edu)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-locks.pdf">slides (mit.edu)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读： <a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">“Locking”</a> with spinlock.c and skim mp.c</td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC10</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-threads.txt">Processes and switching</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">“Scheduling”</a> up to “Sleep and wakeup” and proc.c, swtch.S</td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC11</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-coordination.txt">sleep&amp;wakeup</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：remainder of <a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">“Scheduling”</a>, and corresponding parts of proc.c</td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC12</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-fs.txt">File systems</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：bio.c, fs.c, sysfile.c, file.c and “File system” except for the logging sections</td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC13</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-crash.txt">Crash recovery</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：log.c and the logging sections of “File system”</td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC14</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-journal.txt">File system performance and fast crash recovery</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/homework/journal-ext2fs.html">Journaling the Linux ext2fs Filesystem (1998)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC15</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-vm-again.txt">Virtual Memory (3)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-vm-again.pdf">slides</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/readings/appel-li.pdf">Virtual Memory Primitives for User Programs (1991)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC16</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-organization.txt">OS Organization</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/readings/engler95exokernel.pdf">Exokernel (1995)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC17</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-biscuit.txt">Kernels and HLL</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/readings/biscuit.pdf">Biscuit paper (2018)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC18</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-scalable-lock.md">Scalable locks</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-mcs.pdf">slides</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/scalable-lock-code.c">code</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/readings/linux-lock.pdf">Non-scalable locks paper (2012)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC19</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-rcu.pdf">Scaling OSes slides</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/readings/rcu-decade-later.pdf">RCU (2013)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC20</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-vmware.txt">Virtual Machines (1)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-vmware.pdf">slides</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC21</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/readings/adams06vmware.pdf"><a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-vmm.md">Virtual Machines (2)</a></a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-virt2.pdf">slides</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/readings/belay-dune.pdf">Dune: Safe User-level Access to Privileged CPU Features (2012)</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">LEC22</td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/lec/l-net.txt">High-performance networking</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left">阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/readings/osdi14-paper-belay.pdf">IX: A Protected Dataplane Operating System for High Throughput and Low Latency (2014)</a></td></tr></tbody></table></div><h2 id="课程视频"><a href="#课程视频" class="headerlink" title="课程视频"></a>课程视频</h2><p>由于本课程视频较为模糊，因此我们以其他的视频进行替代</p><div class="table-container"><table><thead><tr><th style="text-align:left">课程</th><th style="text-align:left">内容</th><th style="text-align:center">完成情况</th></tr></thead><tbody><tr><td style="text-align:left">第一讲</td><td style="text-align:left"><a href="https://ocw.mit.edu/courses/electrical-engineering-and-computer-science/6-006-introduction-to-algorithms-fall-2011/lecture-videos/lecture-1-algorithmic-thinking-peak-finding">lecture-1-algorithmic-thinking-peak-finding</a></td><td style="text-align:center">完成</td></tr><tr><td style="text-align:left">第二讲</td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">第三讲</td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">第四讲</td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">第五讲</td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">第六讲</td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">第七讲</td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left">第八讲</td><td style="text-align:left"><a href="https://ocw.mit.edu/courses/electrical-engineering-and-computer-science/6-006-introduction-to-algorithms-fall-2011/lecture-videos/lecture-8-hashing-with-chaining/">Hashing with Chaining</a></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left"></td><td style="text-align:center"></td></tr><tr><td style="text-align:left"></td><td style="text-align:left"></td></tr></tbody></table></div><h2 id="作业"><a href="#作业" class="headerlink" title="作业"></a>作业</h2><div class="table-container"><table><thead><tr><th>作业</th><th>内容</th><th style="text-align:center">完成情况</th></tr></thead><tbody><tr><td><a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-boot.html">boot xv6 (mit.edu)</a></td><td>Boot xv6</td><td style="text-align:center">完成</td></tr><tr><td></td><td>Finding and breaking at an address</td><td style="text-align:center">完成</td></tr><tr><td></td><td>Exercise: What is on the stack?</td><td style="text-align:center"></td></tr><tr><td><a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-shell.html">shell (mit.edu)</a></td><td>完成：<a href="http://web.mit.edu/6.033/2004/wwwdocs/handouts/handson/unix.html">6.033: Hands-on Assignment (UNIX) (mit.edu)</a></td><td style="text-align:center"></td></tr><tr><td></td><td>阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">chapter 0 of the xv6 book</a></td><td style="text-align:center">完成</td></tr><tr><td></td><td>阅读：<a href="https://pdos.csail.mit.edu/6.828/2018/homework/sh.c">6.828 shell</a></td><td style="text-align:center">完成</td></tr><tr><td></td><td>完成：实验环境搭建</td><td style="text-align:center">完成</td></tr><tr><td></td><td>完成：执行简单命令</td><td style="text-align:center">完成</td></tr><tr><td></td><td>完成：IO重定向</td><td style="text-align:center">完成</td></tr><tr><td></td><td>完成：实施管道</td><td style="text-align:center">完成</td></tr><tr><td></td><td>完成：挑战练习</td><td style="text-align:center"></td></tr><tr><td><a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-syscall.html">xv6 system calls (mit.edu)</a></td><td>完成：syscall函数</td><td style="text-align:center">完成</td></tr><tr><td></td><td>完成：date函数</td><td style="text-align:center">完成</td></tr><tr><td><a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-zero-fill.html">lazy page allocation</a></td><td>完成：修改sbrk函数</td><td style="text-align:center">完成</td></tr><tr><td></td><td>完成：lazy allocation</td><td style="text-align:center">框架搭好了，但尚未实现</td></tr><tr><td><a href="https://pdos.csail.mit.edu/6.828/2018/homework/lock.html">Threads and Locking</a></td><td>完成：对put进行加锁保护</td><td style="text-align:center">完成，但是Hash表原理不太懂</td></tr></tbody></table></div><h2 id="实-验"><a href="#实-验" class="headerlink" title="实     验"></a>实     验</h2><div class="table-container"><table><thead><tr><th>实验</th><th>内容</th><th>完成情况</th></tr></thead><tbody><tr><td><a href="https://pdos.csail.mit.edu/6.828/2018/labs/lab1/">Lab1</a></td><td></td><td>完成</td></tr><tr><td><a href="https://pdos.csail.mit.edu/6.828/2018/labs/lab2/">Lab2</a></td><td>阅读：Introduction</td><td>完成</td></tr><tr><td></td><td>完成：实验环境搭建</td><td>完成</td></tr><tr><td></td><td>阅读：第一部分，物理页管理</td><td>完成</td></tr><tr><td></td><td>阅读：第二部分，虚拟内存</td><td>完成</td></tr><tr><td></td><td>阅读：第三部分，内核地址空间</td><td>完成</td></tr><tr><td></td><td>编程：编写一个物理内存分配 boot_alloc</td><td>完成</td></tr><tr><td></td><td>第一部分：编写mem_init()</td><td>完成</td></tr><tr><td></td><td>第一部分：编写page_init</td><td>完成</td></tr><tr><td></td><td>第一部分：编写page_alloc和page_free</td><td>完成</td></tr><tr><td></td><td>第二部分：编写pgdir_walk</td><td>完成</td></tr><tr><td></td><td>第二部分：</td><td></td></tr><tr><td></td><td></td></tr></tbody></table></div><h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h2><ul><li>测试一：<a href="https://pdos.csail.mit.edu/6.828/2018/quiz.html">previous years’ quizzes</a>.</li><li>测试二：<a href="https://pdos.csail.mit.edu/6.828/2018/quiz.html">previous years’ quizzes</a>.</li></ul><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://www.bilibili.com/video/av15896196/">MIT6828课程视频链接</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/schedule.html">6.828 / Fall 2018 (mit.edu)</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;人的新鲜感总是难以为继，我们总是会不经意间开始厌倦已经熟悉的人和事&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="算法和数据结构" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E7%AE%97%E6%B3%95%E5%92%8C%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
      <category term="课程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E7%AE%97%E6%B3%95%E5%92%8C%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E8%AF%BE%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>MIT6828-HW7-locking</title>
    <link href="http://yoursite.com/2021/04/04/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/MIT6828-HW7/"/>
    <id>http://yoursite.com/2021/04/04/计算机/操作系统/课程/MIT6828-HW7/</id>
    <published>2021-04-04T13:23:13.456Z</published>
    <updated>2021-06-23T15:24:36.675Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>在这个作业中，我们将探究中断和锁之间的一些关系。</p><a id="more"></a><h2 id="Don’t-do-this"><a href="#Don’t-do-this" class="headerlink" title="Don’t do this"></a>Don’t do this</h2><p>当<code>xv6</code>执行下面的代码时，会发生什么？</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">struct spinlock lk;</span><br><span class="line">initlock(&amp;lk, &quot;test lock&quot;);</span><br><span class="line">acquire(&amp;lk);</span><br><span class="line">acquire(&amp;lk);</span><br></pre></td></tr></table></figure><p>重复上锁，第二个锁会panic。acquire的代码如下：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Acquire the lock.</span></span><br><span class="line"><span class="comment">// Loops (spins) until the lock is acquired.</span></span><br><span class="line"><span class="comment">// Holding a lock for a long time may cause</span></span><br><span class="line"><span class="comment">// other CPUs to waste time spinning to acquire it.</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">acquire(struct spinlock *lk)</span><br><span class="line">&#123;</span><br><span class="line">  pushcli(); <span class="comment">// disable interrupts to avoid deadlock.</span></span><br><span class="line">  <span class="keyword">if</span>(holding(lk)) <span class="comment">// 如果锁已经被holding了，那么会panic</span></span><br><span class="line">    panic(<span class="string">"acquire"</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// The xchg is atomic.</span></span><br><span class="line">  <span class="keyword">while</span>(xchg(&amp;lk-&gt;locked, <span class="number">1</span>) != <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Tell the C compiler and the processor to not move loads or stores</span></span><br><span class="line">  <span class="comment">// past this point, to ensure that the critical section's memory</span></span><br><span class="line">  <span class="comment">// references happen after the lock is acquired.</span></span><br><span class="line">  __sync_synchronize();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Record info about lock acquisition for debugging.</span></span><br><span class="line">  lk-&gt;cpu = mycpu();</span><br><span class="line">  getcallerpcs(&amp;lk, lk-&gt;pcs);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="ide-c中的中断"><a href="#ide-c中的中断" class="headerlink" title="ide.c中的中断"></a>ide.c中的中断</h2><p>An <code>acquire</code> ensures that interrupts are off on the local processor using the <code>cli</code> instruction (via <code>pushcli()</code>), and that interrupts remain off until the <code>release</code> of the last lock held by that processor (at which point they are enabled using <code>sti</code>).</p><p>Let’s see what happens if we turn on interrupts while holding the <code>ide</code> lock. In <code>iderw</code> in <code>ide.c</code>, add a call to <code>sti()</code> after the <code>acquire()</code>, and a call to <code>cli()</code> just before the <code>release()</code>. Rebuild the kernel and boot it in QEMU. Chances are the kernel will panic soon after boot; try booting QEMU a few times if it doesn’t.</p><h3 id="在iderw中加锁和释放锁部分开启中断"><a href="#在iderw中加锁和释放锁部分开启中断" class="headerlink" title="在iderw中加锁和释放锁部分开启中断"></a>在iderw中加锁和释放锁部分开启中断</h3><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">iderw(struct buf *b)</span><br><span class="line">&#123;</span><br><span class="line"> </span><br><span class="line">  acquire(&amp;idelock);  <span class="comment">//DOC:acquire-lock</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// HW7: Add sti </span></span><br><span class="line">  sti();</span><br><span class="line"></span><br><span class="line">  ...</span><br><span class="line">  <span class="comment">// HW7: add cli just before releasing the lock</span></span><br><span class="line">  cli();</span><br><span class="line">  release(&amp;idelock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在添加了<code>sti</code>和<code>cli</code>后，如果编译并启动内核，系统会panic。</p><h3 id="sti和cli的作用"><a href="#sti和cli的作用" class="headerlink" title="sti和cli的作用"></a><code>sti</code>和<code>cli</code>的作用</h3><p><code>sti</code>开启中断，<code>cli</code>关闭中断，也就是说我们在<code>acquire</code>和<code>release</code>之间开启了中断。</p><h3 id="原因分析"><a href="#原因分析" class="headerlink" title="原因分析"></a>原因分析</h3><p>系统提示的错误信息如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sb: size 1000 nblocks 941 ninodes 200 nlog 30 logstart 2 inodestart 32 bmap start 58</span><br><span class="line">lapicid 1: panic: acquire</span><br><span class="line">801043ed 80102053 80105985 801056fc 80100183 80102ae5 801036c0 801056ff 0 0</span><br></pre></td></tr></table></figure><p>最下方一行数字为系统调用栈情况，根据调用栈，</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev10.pdf">xv6 book</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-lock.html">Homework: xv6 locking (mit.edu)</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在这个作业中，我们将探究中断和锁之间的一些关系。&lt;/p&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
      <category term="课程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>MIT6828-HW6-Threads and Locking</title>
    <link href="http://yoursite.com/2021/04/04/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/MIT6828-HW6/"/>
    <id>http://yoursite.com/2021/04/04/计算机/操作系统/课程/MIT6828-HW6/</id>
    <published>2021-04-04T12:14:16.011Z</published>
    <updated>2021-04-21T12:45:14.902Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>在这个作业中，我们将使用一个Hash Table实现多线程编程，首先<a href="https://pdos.csail.mit.edu/6.828/2018/homework/ph.c">下载这个文件</a>。</p><a id="more"></a><h2 id="作业内容"><a href="#作业内容" class="headerlink" title="作业内容"></a>作业内容</h2><p>下载文件后进行编译并运行：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">$ gcc -g -O2 ph.c -pthread</span><br><span class="line">$ ./a.out <span class="number">2</span></span><br></pre></td></tr></table></figure><p>2表示启动的线程数，运行一段时间后，程序应该有如下输出</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span>: put time = <span class="number">0.003338</span></span><br><span class="line"><span class="number">0</span>: put time = <span class="number">0.003389</span></span><br><span class="line"><span class="number">0</span>: get time = <span class="number">7.684335</span></span><br><span class="line"><span class="number">0</span>: <span class="number">17480</span> keys missing</span><br><span class="line"><span class="number">1</span>: get time = <span class="number">7.684335</span></span><br><span class="line"><span class="number">1</span>: <span class="number">17480</span> keys missing</span><br><span class="line">completion time = <span class="number">7.687856</span></span><br></pre></td></tr></table></figure><p>每个线程运行在两个阶段，put和get，每个阶段大约7.7秒，现在使用一个线程：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">$ ./a.out 1</span><br><span class="line">0: put time = 0.004073</span><br><span class="line">0: get time = 6.929189</span><br><span class="line">0: 0 keys missing</span><br><span class="line">completion time = 6.933433</span><br></pre></td></tr></table></figure><p>可以发现总用时为6.9秒，貌似一个线程速度还快一些，但是多线程做了其两倍的工作，用时只多了不到1秒，还是效率高了一些，但是可以看到，使用两个线程会有很多次哈希miss的情况，画一个图思考一下为何会出现哈希miss。（本质上是因为两个线程对同一个空间进行写操作，第一个线程写了之后，被第二个线程擦除，所以真正有用的是第二个线程的操作）</p><p>为了避免哈希miss，我们需要在对hash table访问时加锁保护，使用下列语句对put和get进行加锁：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">pthread_mutex_t lock;     // declare a lock</span><br><span class="line">pthread_mutex_init(&amp;lock, NULL);   // initialize the lock</span><br><span class="line">pthread_mutex_lock(&amp;lock);  // acquire lock</span><br><span class="line">pthread_mutex_unlock(&amp;lock);  // release lock</span><br></pre></td></tr></table></figure><p>由于get只是读取，因此我们只需要对put进行加锁，即可避免hash miss</p><h2 id="反思"><a href="#反思" class="headerlink" title="反思"></a>反思</h2><h3 id="粗粒锁与细粒锁"><a href="#粗粒锁与细粒锁" class="headerlink" title="粗粒锁与细粒锁"></a>粗粒锁与细粒锁</h3><p>我们可以使用一个锁锁住整个哈希表，也可以对哈希表中的每一个链表单独上一把锁，代码分别如下：</p><h4 id="粗颗粒锁"><a href="#粗颗粒锁" class="headerlink" title="粗颗粒锁"></a>粗颗粒锁</h4><p>粗颗粒锁对整个哈希表上锁，比较简单，但是控制颗粒度很粗，访问不同列的线程也会导致相互阻塞。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">pthread_mutex_t lock;     // declare a lock</span><br><span class="line">pthread_mutex_init(&amp;lock, NULL);   // initialize the lock</span><br><span class="line"></span><br><span class="line">pthread_mutex_lock(&amp;lock);    // acquire lock</span><br><span class="line">    ...                       // 临界区</span><br><span class="line">pthread_mutex_unlock(&amp;lock);  // release lock</span><br></pre></td></tr></table></figure><h4 id="细颗粒锁"><a href="#细颗粒锁" class="headerlink" title="细颗粒锁"></a>细颗粒锁</h4><p>细颗粒锁针对哈希表每一列单独上锁，因此访问不同列的线程不会因为争抢锁而陷入等待，效率高一些。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pthread_mutex_t</span> locks[NBUCKET];     <span class="comment">// declare a lock</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;NBUCKET; i++)</span><br><span class="line">    pthread_mutex_init(&amp;locks[i], <span class="literal">NULL</span>);   <span class="comment">// initialize the lock</span></span><br><span class="line"></span><br><span class="line">pthread_mutex_lock(&amp;locks[i]);    <span class="comment">// acquire lock</span></span><br><span class="line">    ...                       <span class="comment">// 临界区</span></span><br><span class="line">pthread_mutex_unlock(&amp;locks[i]);  <span class="comment">// release lock</span></span><br></pre></td></tr></table></figure><h4 id="两种不同锁对比"><a href="#两种不同锁对比" class="headerlink" title="两种不同锁对比"></a>两种不同锁对比</h4><p>总的来说粗颗粒锁更简单，细颗粒锁更快一些，但是需要锁的数量会上升，如果对象很多的话，使用细颗粒锁并不方便。（其实就我个人的实验，并没有看出两者之间有何差别）</p><h2 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Headers</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Constants</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SOL</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NBUCKET 5</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NKEYS 100000</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Node of a hash table </span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">entry</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> key;</span><br><span class="line">    <span class="keyword">int</span> value;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">entry</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Hash value is the array index, and make linear search </span></span><br><span class="line"><span class="comment">// inside the list</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">entry</span> *<span class="title">table</span>[<span class="title">NBUCKET</span>];</span></span><br><span class="line"><span class="comment">// Average list length = NKEYS / NBUCKET</span></span><br><span class="line"><span class="keyword">int</span> keys[NKEYS];</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> nthread = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">volatile</span> <span class="keyword">int</span> done;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Get current time </span></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">now</span><span class="params">()</span></span>&#123;...&#125;</span><br><span class="line"><span class="comment">// Print the hash table</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">(<span class="keyword">void</span>)</span></span>&#123;...&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Insert at beginning</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> key, <span class="keyword">int</span> value, struct entry **p, struct entry *n)</span></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">entry</span> *<span class="title">e</span> = <span class="title">malloc</span>(<span class="title">sizeof</span>(<span class="title">struct</span> <span class="title">entry</span>));</span></span><br><span class="line">    e-&gt;key = key;</span><br><span class="line">    e-&gt;value = value;</span><br><span class="line">    e-&gt;next = n;</span><br><span class="line">    *p = e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">int</span> key, <span class="keyword">int</span> value)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = key % NBUCKET;</span><br><span class="line">    insert(key, value, &amp;table[i], table[i]);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> struct entry* <span class="title">get</span><span class="params">(<span class="keyword">int</span> key)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">entry</span> *<span class="title">e</span> = 0;</span></span><br><span class="line">    <span class="keyword">for</span> (e = table[key % NBUCKET]; e != <span class="number">0</span>; e = e-&gt;next) &#123;  <span class="comment">// 进入对应哈希列，然后顺序遍历</span></span><br><span class="line">        <span class="keyword">if</span> (e-&gt;key == key) <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// e == NULL: 搜索到末尾，哈希不命中</span></span><br><span class="line">    <span class="keyword">return</span> e;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程函数</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> *<span class="title">thread</span><span class="params">(<span class="keyword">void</span> *xa)</span></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> n = (<span class="keyword">long</span>) xa;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">int</span> b = NKEYS/nthread;    <span class="comment">// 每个线程处理的索引数</span></span><br><span class="line">    <span class="keyword">int</span> k = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">double</span> t1, t0;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//  printf("b = %d\n", b);</span></span><br><span class="line">    t0 = now();</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; b; i++) &#123;</span><br><span class="line">        <span class="comment">// printf("%d: put %d\n", n, b*n+i);</span></span><br><span class="line">        put(keys[b*n + i], n);</span><br><span class="line">    &#125;</span><br><span class="line">    t1 = now();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%ld: put time = %f\n"</span>, n, t1-t0);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Should use pthread_barrier, but MacOS doesn't support it ...</span></span><br><span class="line">    __sync_fetch_and_add(&amp;done, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">while</span> (done &lt; nthread) ;</span><br><span class="line"></span><br><span class="line">    t0 = now();</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NKEYS; i++) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">entry</span> *<span class="title">e</span> = <span class="title">get</span>(<span class="title">keys</span>[<span class="title">i</span>]);</span></span><br><span class="line">        <span class="keyword">if</span> (e == <span class="number">0</span>) k++;</span><br><span class="line">    &#125;</span><br><span class="line">    t1 = now();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%ld: get time = %f\n"</span>, n, t1-t0);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%ld: %d keys missing\n"</span>, n, k);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span>&#123;</span><br><span class="line">    <span class="keyword">pthread_t</span> *tha;</span><br><span class="line">    <span class="keyword">void</span> *value;</span><br><span class="line">    <span class="keyword">long</span> i;</span><br><span class="line">    <span class="keyword">double</span> t1, t0;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 参数处理</span></span><br><span class="line">    <span class="keyword">if</span> (argc &lt; <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"%s: %s nthread\n"</span>, argv[<span class="number">0</span>], argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 创建线程，分配n个线程所需的空间</span></span><br><span class="line">    nthread = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">    tha = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">pthread_t</span>) * nthread);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 设置随机数种子，初始化索引</span></span><br><span class="line">    srandom(<span class="number">0</span>);</span><br><span class="line">    assert(NKEYS % nthread == <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NKEYS; i++) &#123;</span><br><span class="line">        keys[i] = random();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    t0 = now();</span><br><span class="line">    <span class="comment">// 创建线程</span></span><br><span class="line">    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; nthread; i++) &#123;</span><br><span class="line">        assert(pthread_create(&amp;tha[i], <span class="literal">NULL</span>, thread, (<span class="keyword">void</span> *) i) == <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 等待线程执行结束</span></span><br><span class="line">    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; nthread; i++) &#123;</span><br><span class="line">        assert(pthread_join(tha[i], &amp;value) == <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    t1 = now();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"completion time = %f\n"</span>, t1-t0);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在这个作业中，我们将使用一个Hash Table实现多线程编程，首先&lt;a href=&quot;https://pdos.csail.mit.edu/6.828/2018/homework/ph.c&quot;&gt;下载这个文件&lt;/a&gt;。&lt;/p&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
      <category term="课程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>MIT6828-Lab3-用户环境</title>
    <link href="http://yoursite.com/2021/04/02/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/MIT6828-Lab3/"/>
    <id>http://yoursite.com/2021/04/02/计算机/操作系统/课程/MIT6828-Lab3/</id>
    <published>2021-04-02T13:16:08.719Z</published>
    <updated>2021-06-28T08:43:57.148Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>且将杯酒对星河</p></blockquote><a id="more"></a><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>本文将实现使用户环境（即进程）运行的内核功能。在本实验中，你需要：</p><ul><li>建立起跟踪进程的数据结构</li><li>创建一个进程</li><li>载入程序镜像</li><li>让程序运行</li><li>处理进程的系统调用</li></ul><h3 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">athena% cd ~/<span class="number">6.828</span>/lab</span><br><span class="line">athena% add git</span><br><span class="line">athena% git commit -am 'changes to lab2 after handin'</span><br><span class="line">Created commit <span class="number">734f</span>ab7: changes to lab2 after handin</span><br><span class="line"> <span class="number">4</span> files changed, <span class="number">42</span> insertions(+), <span class="number">9</span> deletions(-)</span><br><span class="line">athena% git pull</span><br><span class="line">Already up-to-date.</span><br><span class="line">athena% git checkout -b lab3 origin/lab3</span><br><span class="line">Branch lab3 <span class="built_in">set</span> up to track remote branch refs/remotes/origin/lab3.</span><br><span class="line">Switched to a <span class="keyword">new</span> branch <span class="string">"lab3"</span></span><br><span class="line">athena% git merge lab2</span><br><span class="line">Merge made by recursive.</span><br><span class="line"> kern/pmap.c |   <span class="number">42</span> +++++++++++++++++++</span><br><span class="line"> <span class="number">1</span> files changed, <span class="number">42</span> insertions(+), <span class="number">0</span> deletions(-)</span><br><span class="line">athena%</span><br></pre></td></tr></table></figure><p>首先，合并lab2，然后切换至lab3，在lab3中，你需要参考及阅读的源码如下：</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><div class="table-container"><table><thead><tr><th>目录</th><th>文件</th><th>功能</th><th>进度</th></tr></thead><tbody><tr><td><code>inc/</code></td><td><code>env.h</code></td><td>Public definitions for user-mode environments</td><td>完成</td></tr><tr><td></td><td><code>trap.h</code></td><td>Public definitions for trap handling</td><td>完成</td></tr><tr><td></td><td><code>syscall.h</code></td><td>Public definitions for system calls from user environments to the kernel</td><td></td></tr><tr><td></td><td><code>lib.h</code></td><td>Public definitions for the user-mode support library</td><td></td></tr><tr><td><code>kern/</code></td><td><code>env.h</code></td><td>Kernel-private definitions for user-mode environments</td><td></td></tr><tr><td></td><td><code>env.c</code></td><td>Kernel code implementing user-mode environments</td><td></td></tr><tr><td></td><td><code>trap.h</code></td><td>Kernel-private trap handling definitions</td><td></td></tr><tr><td></td><td><code>trap.c</code></td><td>Trap handling code</td><td></td></tr><tr><td></td><td><code>trapentry.S</code></td><td>Assembly-language trap handler entry-points</td><td></td></tr><tr><td></td><td><code>syscall.h</code></td><td>Kernel-private definitions for system call handling</td><td></td></tr><tr><td></td><td><code>syscall.c</code></td><td>System call implementation code</td><td></td></tr><tr><td><code>lib/</code></td><td><code>Makefrag</code></td><td>Makefile fragment to build user-mode library, <code>obj/lib/libjos.a</code></td><td></td></tr><tr><td></td><td><code>entry.S</code></td><td>Assembly-language entry-point for user environments</td><td></td></tr><tr><td></td><td><code>libmain.c</code></td><td>User-mode library setup code called from <code>entry.S</code></td><td></td></tr><tr><td></td><td><code>syscall.c</code></td><td>User-mode system call stub functions</td><td></td></tr><tr><td></td><td><code>console.c</code></td><td>User-mode implementations of <code>putchar</code> and <code>getchar</code>, providing console I/O</td><td></td></tr><tr><td></td><td><code>exit.c</code></td><td>User-mode implementation of <code>exit</code></td><td></td></tr><tr><td></td><td><code>panic.c</code></td><td>User-mode implementation of <code>panic</code></td><td></td></tr><tr><td><code>user/</code></td><td><code>*</code></td><td>Various test programs to check kernel lab 3 code</td></tr></tbody></table></div></div></div><h3 id="实验需求"><a href="#实验需求" class="headerlink" title="实验需求"></a>实验需求</h3><p>本次实验有AB两个部分，你需要分别完成，并至少完成一个挑战实验。</p><h3 id="内联汇编"><a href="#内联汇编" class="headerlink" title="内联汇编"></a>内联汇编</h3><p>GCC提供了内联汇编功能，参考<a href="https://pdos.csail.mit.edu/6.828/2018/reference.html">该网站获得内联汇编讲解</a></p><h2 id="第一部分：进程和异常处理"><a href="#第一部分：进程和异常处理" class="headerlink" title="第一部分：进程和异常处理"></a>第一部分：进程和异常处理</h2><p><code>inc/env.h</code>包含了进程的一些定义，内核使用<code>Env</code>追踪进程，在<code>kern/env.c</code>中，包含如下全局变量：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">envs</span> = <span class="title">NULL</span>;</span>    <span class="comment">// All environments，指向一个进程array</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">curenv</span> = <span class="title">NULL</span>;</span>    <span class="comment">// The current env，正在运行的进程，在第一个进程执行前为NULL</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">env_free_list</span>;</span><span class="comment">// Free environment list</span></span><br></pre></td></tr></table></figure><p>JOS允许的最大活动线程数定义在<code>inc/env.h</code>中，为<code>1&lt;&lt;10</code>即1024个。<code>env_free_list</code>为不活跃的Env链表，在表中添加或删除Env，即可分配或释放进程。</p><h3 id="进程状态"><a href="#进程状态" class="headerlink" title="进程状态"></a>进程状态</h3><p>我们使用<code>Env</code>结构体对进程进行描述，<code>Env</code>结构体如下：</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Trapframe</span> <span class="title">env_tf</span>;</span><span class="comment">// Saved registers 保存进程未执行时的寄存器，即内核或其他进程运行时，trapframe</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">env_link</span>;</span><span class="comment">// Next free Env</span></span><br><span class="line">    <span class="keyword">envid_t</span> env_id;<span class="comment">// Unique environment identifier</span></span><br><span class="line">    <span class="keyword">envid_t</span> env_parent_id;<span class="comment">// env_id of this env's parent</span></span><br><span class="line">    <span class="keyword">enum</span> EnvType env_type;<span class="comment">// Indicates special system environments</span></span><br><span class="line">    <span class="keyword">unsigned</span> env_status;<span class="comment">// Status of the environment</span></span><br><span class="line">    <span class="keyword">uint32_t</span> env_runs;<span class="comment">// Number of times environment has run</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Address space</span></span><br><span class="line">    <span class="keyword">pde_t</span> *env_pgdir;<span class="comment">// Kernel virtual address of page dir</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div></div><p>结构体成员功能如下：</p><p><strong>env_tf</strong>：保存进程未执行时的寄存器，即内核或其他进程运行时的寄存器。发生进程切换时，内核将保存该寄存器。</p><p><strong>env_link</strong>：指向<code>env_free_list</code>中的下一个空闲进程</p><p><strong>env_id</strong>：使用当前<code>Env</code>的进程的id，当进程被回收后，内核可能会将同一个<code>Env</code>分配给其他进程，但是进程号会发生改变</p><p><strong>env_parent_id</strong>：父进程id</p><p><strong>env_type</strong>：进程类型，对于大多数进程，都是用户进程，即<code>ENV_TYPE_USER</code></p><p><strong>env_status</strong>：进程状态，有如下几种：</p><p>​    <code>ENV_FREE</code>：不活跃进程，位于<code>env_free_list</code>中</p><p>​    <code>ENV_RUNNABLE</code>：等待执行的进程</p><p>​    <code>ENV_RUNNING</code>：正在执行的进程</p><p>​    <code>ENV_NOT_RUNNABLE</code>：活跃的进程，但是尚未准备运行，例如等待另一个进程通信的进程</p><p>​    <code>ENV_DYING</code>：僵尸进程，将会在下一次陷入内核时被回收</p><p><strong>env_pgdir</strong>：保存着当前进程页目录的内核虚拟地址</p><h3 id="陷帧"><a href="#陷帧" class="headerlink" title="陷帧"></a>陷帧</h3><p>我一直没有对这个名词找到一个合适的翻译，姑且顾名思义，称其为“陷帧”，因为进程切换是需要陷入内核的。</p><p>陷帧的作用如下，如果把进程的执行比作动画，动画是一帧一帧播放的，相应地，我们的进程也是一帧一帧执行的，我们使用一个结构体对进程关键信息进行描述。这个结构体称为陷帧。在这个帧中，保存有进程执行时的关键寄存器。当我们进行进程或线程切换时，实际就是先将当前进程的陷帧进行保存，然后加载新进程的陷帧</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Trapframe</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">PushRegs</span> <span class="title">tf_regs</span>;</span></span><br><span class="line">    <span class="keyword">uint16_t</span> tf_es;             <span class="comment">// es段寄存器</span></span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding1;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_ds;             <span class="comment">// ds数据段寄存器 pushl </span></span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding2;</span><br><span class="line">    <span class="keyword">uint32_t</span> tf_trapno;</span><br><span class="line">    <span class="comment">/* below here defined by x86 hardware */</span></span><br><span class="line">    <span class="keyword">uint32_t</span> tf_err;            </span><br><span class="line">    <span class="keyword">uintptr_t</span> tf_eip;           <span class="comment">// eip指令指针寄存器</span></span><br><span class="line">    <span class="keyword">uint16_t</span> tf_cs;             <span class="comment">// cs代码段寄存器</span></span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding3;</span><br><span class="line">    <span class="keyword">uint32_t</span> tf_eflags;         </span><br><span class="line">    <span class="comment">/* below here only when crossing rings, such as from user to kernel */</span></span><br><span class="line">    <span class="keyword">uintptr_t</span> tf_esp;           <span class="comment">// 栈顶指针寄存器</span></span><br><span class="line">    <span class="keyword">uint16_t</span> tf_ss;             <span class="comment">// 堆栈段寄存器</span></span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding4;</span><br><span class="line">&#125; __attribute__((packed));</span><br></pre></td></tr></table></figure></div></div><h3 id="分配进程array"><a href="#分配进程array" class="headerlink" title="分配进程array"></a>分配进程array</h3><p>在上一个lab中，我们在<code>mem_init</code>函数中为<code>pages[]</code>分配了空间，类似地，分配一个<code>env[]</code>，用于保存<code>Env</code>结构体，这个比较简单，参考<code>pages</code>分配过程即可，具体代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//////////////////////////////////////////////////////////////////////</span></span><br><span class="line"><span class="comment">// Make 'envs' point to an array of size 'NENV' of 'struct Env'.</span></span><br><span class="line">n = NENV * <span class="keyword">sizeof</span>(struct Env);</span><br><span class="line">envs = (struct Env*) boot_alloc(n);</span><br><span class="line"><span class="built_in">memset</span>(envs, <span class="number">0</span>, n);</span><br><span class="line"></span><br><span class="line"><span class="comment">//////////////////////////////////////////////////////////////////////</span></span><br><span class="line"><span class="comment">// Map the 'envs' array read-only by the user at linear address UENVS</span></span><br><span class="line"><span class="comment">// (ie. perm = PTE_U | PTE_P).</span></span><br><span class="line"><span class="comment">// Permissions:</span></span><br><span class="line"><span class="comment">//    - the new image at UENVS  -- kernel R, user R</span></span><br><span class="line"><span class="comment">//    - envs itself -- kernel RW, user NONE</span></span><br><span class="line">boot_map_region(kern_pgdir, UENVS, PTSIZE, PADDR(envs), PTE_U | PTE_P);</span><br></pre></td></tr></table></figure><p>现在你的代码应该能够通过<code>check_kern_pgdir()</code></p><h3 id="进程创建及运行"><a href="#进程创建及运行" class="headerlink" title="进程创建及运行"></a>进程创建及运行</h3><p>现在编写<code>kern/env.c</code>中的代码，运行一个用户进程。目前我们尚未提供文件系统，因此我们需要进程加载一个写死在内核中的静态二进制镜像作为进程。Lab3中使用了一些手段，将用户程序与内核进行了强绑定，具体实现可以参考参考文献<sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>。现在，请完成如下函数</p><div class="note default">            <p><strong>练习2</strong>：在文件<code>env.c</code>中，完成如下代码：</p><p><code>env_init()</code>：初始化所有<code>Env</code>结构体对象，然后添加至<code>env_free_list</code>中，调用<code>env_init_percpu</code>，配置分段硬件优先级（0为内核，3为用户）</p>          </div><h4 id="env-init"><a href="#env-init" class="headerlink" title="env_init()"></a>env_init()</h4><h5 id="函数原型"><a href="#函数原型" class="headerlink" title="函数原型"></a>函数原型</h5><p>该函数实现了<code>envs</code>以及<code>env_free_list</code>的初始化，其原型如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Make sure the environments are in the free list in the same order</span></span><br><span class="line"><span class="comment">// they are in the envs array (i.e., so that the first call to</span></span><br><span class="line"><span class="comment">// env_alloc() returns envs[0]).</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">env_init(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Set up envs array</span></span><br><span class="line">    <span class="comment">// LAB 3: Your code here.</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Per-CPU part of the initialization</span></span><br><span class="line">    env_init_percpu();   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>根据注释，我们可以总结该函数如下工作：</p><ul><li>将<code>env_id</code>设置为0</li><li>将<code>env</code>按与array一致的顺序插入<code>env_free_list</code>中</li></ul><h5 id="函数实现"><a href="#函数实现" class="headerlink" title="函数实现"></a>函数实现</h5><p>该工作比较简单，实际就是一个链表的插入问题，直接得到代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">env_free_list = envs;</span><br><span class="line"><span class="keyword">size_t</span> i;</span><br><span class="line"><span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; NENV<span class="number">-1</span>; ++i)&#123;</span><br><span class="line">    envs[i].envid = <span class="number">0</span>;</span><br><span class="line">    envs[i].env_link = &amp;envs[i+<span class="number">1</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">envs[i].envid = <span class="number">0</span>;</span><br><span class="line">envs[i].env_link = <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure><h5 id="改进"><a href="#改进" class="headerlink" title="改进"></a>改进</h5><p>上面的实现中，链表是正序插入的，代码比较繁琐，可以采用倒插法实现：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">env_init(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Set up envs array</span></span><br><span class="line">    env_free_list = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = NENV<span class="number">-1</span>; i&gt;= <span class="number">0</span>; --i)&#123;</span><br><span class="line">        <span class="comment">// Mark all environments in 'envs' as free, set their env_ids to 0,</span></span><br><span class="line">        <span class="comment">// and insert them into the env_free_list.</span></span><br><span class="line">        envs[i].env_status = ENV_FREE;</span><br><span class="line">        envs[i].env_id = <span class="number">0</span>;</span><br><span class="line">    envs[i].env_link = env_free_list;</span><br><span class="line">    env_free_list = &amp;envs[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Per-CPU part of the initialization</span></span><br><span class="line">    env_init_percpu();   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="env-setup-vm"><a href="#env-setup-vm" class="headerlink" title="env_setup_vm()"></a>env_setup_vm()</h4><h5 id="函数原型-1"><a href="#函数原型-1" class="headerlink" title="函数原型"></a>函数原型</h5><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Initialize the kernel virtual memory layout for environment e.</span></span><br><span class="line"><span class="comment">// Allocate a page directory, set e-&gt;env_pgdir accordingly,</span></span><br><span class="line"><span class="comment">// and initialize the kernel portion of the new environment's address space.</span></span><br><span class="line"><span class="comment">// Do NOT (yet) map anything into the user portion</span></span><br><span class="line"><span class="comment">// of the environment's virtual address space.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Returns 0 on success, &lt; 0 on error.  Errors include:</span></span><br><span class="line"><span class="comment">//-E_NO_MEM if page directory or table could not be allocated.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span></span><br><span class="line">env_setup_vm(struct Env *e)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">PageInfo</span> *<span class="title">p</span> = <span class="title">NULL</span>;</span></span><br><span class="line">    <span class="keyword">if</span> (!(p = page_alloc(ALLOC_ZERO)))  <span class="comment">//分配一个物理页</span></span><br><span class="line">        <span class="keyword">return</span> -E_NO_MEM;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// LAB 3: Your code here.</span></span><br><span class="line">    </span><br><span class="line">    e-&gt;env_pgdir[PDX(UVPT)] = PADDR(e-&gt;env_pgdir) | PTE_P | PTE_U;  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></div><h5 id="函数功能"><a href="#函数功能" class="headerlink" title="函数功能"></a>函数功能</h5><p>从上面的注释中，我们能抽象出该函数具体的工作，即给进程<code>e</code>分配内核虚拟地址空间。首先我们申请了一张页，该页要作为<code>e-&gt;env_pgdir</code>的页目录，我们要将这段空间的虚拟地址给了<code>env_pgdir</code>。此外，还需要手动将该页的引用递增一下。</p><h5 id="具体实现"><a href="#具体实现" class="headerlink" title="具体实现"></a>具体实现</h5><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">int</span></span><br><span class="line">env_setup_vm(struct Env *e)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">PageInfo</span> *<span class="title">p</span> = <span class="title">NULL</span>;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Allocate a page for the page directory</span></span><br><span class="line">    <span class="keyword">if</span> (!(p = page_alloc(ALLOC_ZERO)))  <span class="comment">//分配一个物理页</span></span><br><span class="line">        <span class="keyword">return</span> -E_NO_MEM;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// In general, pp_ref is not maintained for</span></span><br><span class="line">    <span class="comment">// physical pages mapped only above UTOP, but env_pgdir</span></span><br><span class="line">    <span class="comment">// is an exception -- you need to increment env_pgdir's</span></span><br><span class="line">    <span class="comment">// pp_ref for env_free to work correctly.</span></span><br><span class="line">    p-&gt;pp_ref++;</span><br><span class="line">    <span class="comment">// Now, set e-&gt;env_pgdir and initialize the page directory.</span></span><br><span class="line">    e-&gt;env_pgdir = (<span class="keyword">pde_t</span> *) page2kva(p);         <span class="comment">// 将分配的页用作页目录</span></span><br><span class="line">    <span class="comment">// The VA space of all envs is identical above UTOP</span></span><br><span class="line">    <span class="comment">// (except at UVPT, which we've set below).</span></span><br><span class="line">    <span class="built_in">memcpy</span>(e-&gt;env_pgdir, kern_pgdir, PGSIZE);     <span class="comment">// * 重要，将内核页目录拷贝给每个进程</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// UVPT maps the env's own page table read-only.</span></span><br><span class="line">    <span class="comment">// Permissions: kernel R, user R</span></span><br><span class="line">    e-&gt;env_pgdir[PDX(UVPT)] = PADDR(e-&gt;env_pgdir) | PTE_P | PTE_U;   <span class="comment">// UVPT指向用户页目录地址</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></div><h5 id="改进-1"><a href="#改进-1" class="headerlink" title="改进"></a>改进</h5><p>在上面的实现中，我们还需要对<code>env_pgdir</code>指向的页进行初始化，初始化内核部分，因此还需要下面一句</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">memcpy(e-&gt;env_pgdir, kern_pgdir, PGSIZE);</span><br></pre></td></tr></table></figure><p>这一句说明，每个进程都了解内核的页目录。</p><h4 id="region-alloc-struct-Env-e-void-va-size-t-len"><a href="#region-alloc-struct-Env-e-void-va-size-t-len" class="headerlink" title="region_alloc(struct Env e, void va, size_t len)"></a>region_alloc(struct Env <em>e, void </em>va, size_t len)</h4><p>为进程<code>e</code>分配长度为<code>len</code>的物理内存，然后映射至<code>va</code></p><h5 id="具体实现-1"><a href="#具体实现-1" class="headerlink" title="具体实现"></a>具体实现</h5><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">region_alloc(struct Env *e, <span class="keyword">void</span> *va, <span class="keyword">size_t</span> len)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// It is easier to use region_alloc if the caller can pass</span></span><br><span class="line">    <span class="comment">// 'va' and 'len' values that are not page-aligned.</span></span><br><span class="line">    <span class="comment">// You should round va down, and round (va + len) up.</span></span><br><span class="line">    <span class="comment">// (Watch out for corner-cases!)</span></span><br><span class="line">    <span class="keyword">void</span>* start = (<span class="keyword">void</span> *)ROUNDDOWN((<span class="keyword">uint32_t</span>)va, PGSIZE); <span class="comment">//对齐操作</span></span><br><span class="line">    <span class="keyword">void</span>* end   = (<span class="keyword">void</span> *)ROUNDUP((<span class="keyword">uint32_t</span>)(va + len), PGSIZE);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(; start &lt;= end; start += PGSIZE)&#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">PageInfo</span> *<span class="title">pp</span> = <span class="title">page_alloc</span>(0);</span></span><br><span class="line">        <span class="keyword">if</span>(pp == <span class="literal">NULL</span>)&#123;</span><br><span class="line">            panic(<span class="string">"region_alloc: page_alloc failed\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(page_insert(e-&gt;env_pgdir, pp, start, PTE_W | PTE_U) == -E_NO_MEM)&#123;</span><br><span class="line">            panic(<span class="string">"region_alloc: page_insert failed, no enough room\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></div><h4 id="load-icode"><a href="#load-icode" class="headerlink" title="load_icode()"></a>load_icode()</h4><h5 id="函数原型-2"><a href="#函数原型-2" class="headerlink" title="函数原型"></a>函数原型</h5><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Set up the initial program binary, stack, and processor flags</span></span><br><span class="line"><span class="comment">// for a user process.    设置程序二进制文件</span></span><br><span class="line"><span class="comment">// This function is ONLY called during kernel initialization,</span></span><br><span class="line"><span class="comment">// before running the first user-mode environment.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// This function loads all loadable segments from the ELF binary image</span></span><br><span class="line"><span class="comment">// into the environment's user memory, starting at the appropriate</span></span><br><span class="line"><span class="comment">// virtual addresses indicated in the ELF program header.</span></span><br><span class="line"><span class="comment">// At the same time it clears to zero any portions of these segments</span></span><br><span class="line"><span class="comment">// that are marked in the program header as being mapped</span></span><br><span class="line"><span class="comment">// but not actually present in the ELF file - i.e., the program's bss section.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// All this is very similar to what our boot loader does, except the boot</span></span><br><span class="line"><span class="comment">// loader also needs to read the code from disk.  Take a look at</span></span><br><span class="line"><span class="comment">// boot/main.c to get ideas.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// load_icode panics if it encounters problems.</span></span><br><span class="line"><span class="comment">//  - How might load_icode fail?  What might be wrong with the given input?</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">load_icode(struct Env *e, <span class="keyword">uint8_t</span> *binary)</span><br><span class="line">&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="函数功能-1"><a href="#函数功能-1" class="headerlink" title="函数功能"></a>函数功能</h5><p>这个函数将会为一个用户进程设置初始程序的二进制文件、栈以及处理器标志位，该函数只在内核初始化阶段执行。</p><h5 id="具体实现-2"><a href="#具体实现-2" class="headerlink" title="具体实现"></a>具体实现</h5><p>为了实现该函数，我们需要解决如下问题：</p><ul><li>将ELF格式的二进制文件载入用户进程内存空间</li><li>将程序的<code>.bss</code>段置零</li><li>映射一个内存页给用户的进程栈</li></ul><p>函数具体实现如下：</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">load_icode(struct Env *e, <span class="keyword">uint8_t</span> *binary)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span>(e == <span class="literal">NULL</span> || binary == <span class="literal">NULL</span>)&#123;</span><br><span class="line">        panic(<span class="string">"load_icode: wrong pointer!\n"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Proghdr</span> *<span class="title">ph</span>, *<span class="title">eph</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ELF</span> *<span class="title">ELFHDR</span> = (<span class="title">struct</span> <span class="title">ELF</span> *)<span class="title">binary</span>;</span></span><br><span class="line">    <span class="keyword">if</span>(ELFHDR-&gt;e_magic != ELF_MAGIC)</span><br><span class="line">        panic(<span class="string">"load_icode: wrong Elf format!\n"</span>);</span><br><span class="line">    ph = (struct Proghdr *)((<span class="keyword">uint8_t</span> *)ELFHDR + ELFHDR-&gt;e_phoff);</span><br><span class="line">    eph = ph + ELFHDR-&gt;e_phnum;</span><br><span class="line">    <span class="comment">//  Load each program segment into virtual memory</span></span><br><span class="line">    <span class="comment">//  at the address specified in the ELF section header.</span></span><br><span class="line">    <span class="keyword">for</span>(; ph &lt; eph; ph++)&#123;</span><br><span class="line">        <span class="comment">//  You should only load segments with ph-&gt;p_type == ELF_PROG_LOAD.</span></span><br><span class="line">        <span class="keyword">if</span>(ph-&gt;p_type == ELF_PROG_LOAD)&#123;</span><br><span class="line">            <span class="comment">// 在ph-&gt;p_va分配能够容纳p_memsz的物理页</span></span><br><span class="line">            <span class="comment">//  Each segment's virtual address can be found in ph-&gt;p_va</span></span><br><span class="line">            <span class="comment">//  and its size in memory can be found in ph-&gt;p_memsz.</span></span><br><span class="line">            <span class="comment">//  All page protection bits should be user read/write for now.</span></span><br><span class="line">            <span class="comment">//  ELF segments are not necessarily page-aligned, but you can</span></span><br><span class="line">            <span class="comment">//  assume for this function that no two segments will touch</span></span><br><span class="line">            <span class="comment">//  the same virtual page.</span></span><br><span class="line">            region_alloc(e, ph-&gt;p_va, ph-&gt;p_memsz);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">//  Loading the segments is much simpler if you can move data</span></span><br><span class="line">            <span class="comment">//  directly into the virtual addresses stored in the ELF binary.</span></span><br><span class="line">            <span class="comment">//  So which page directory should be in force during</span></span><br><span class="line">            <span class="comment">//  this function? </span></span><br><span class="line">            </span><br><span class="line">            <span class="comment">//  The ph-&gt;p_filesz bytes from the ELF binary, starting at</span></span><br><span class="line">            <span class="comment">//  'binary + ph-&gt;p_offset', should be copied to virtual address</span></span><br><span class="line">            <span class="comment">//  ph-&gt;p_va.  </span></span><br><span class="line">            <span class="built_in">memcpy</span>(ph-&gt;p_va, binary + ph-&gt;p_offset, ph-&gt;p_filesz);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">//  Any remaining memory bytes should be cleared to zero.</span></span><br><span class="line">            <span class="comment">//  (The ELF header should have ph-&gt;p_filesz &lt;= ph-&gt;p_memsz.)</span></span><br><span class="line">            <span class="comment">//  Use functions from the previous lab to allocate and map pages.</span></span><br><span class="line">            <span class="built_in">memset</span>(ph-&gt;p_va + ph-&gt;p_filesz, <span class="number">0</span>, ph-&gt;p_memsz - ph-&gt;p_filesz);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Now map one page for the program's initial stack </span></span><br><span class="line">    <span class="comment">// at virtual address USTACKTOP-PGSIZE</span></span><br><span class="line">    region_alloc(e, USTACKTOP-PGSIZE, PGSIZE);  <span class="comment">// 用户栈的虚拟地址都是一样的</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">//  You must also do something with the program's entry point,</span></span><br><span class="line">    <span class="comment">//  to make sure that the environment starts executing there.</span></span><br><span class="line">    <span class="comment">//  What?  (See env_run() and env_pop_tf() below.)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></div><h5 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h5><p>这个函数算是partA中最难的函数实现，里面有很多细节一开始没注意到，导致调试时卡在了这里，现在总结一下这个函数中遇到的问题。</p><p>首先是内存分配的问题，这里有一个小技巧，要先清空大块内存，再对其中的局部进行拷贝，所以需要将上面的两句内存拷贝和设置语句重新排序</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 将下面这两句</span></span><br><span class="line"><span class="built_in">memcpy</span>(ph-&gt;p_va, binary + ph-&gt;p_offset, ph-&gt;p_memsz);</span><br><span class="line"><span class="built_in">memset</span>(ph-&gt;p_va + ph-&gt;p_filesz, <span class="number">0</span>, ph-&gt;p_memsz - ph-&gt;p_filesz);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 改为下面这两句</span></span><br><span class="line"><span class="built_in">memset</span>(ph-&gt;p_va, <span class="number">0</span>, ph-&gt;p_memsz);   <span class="comment">//先全部清空</span></span><br><span class="line"><span class="built_in">memcpy</span>(ph-&gt;p_va, binary + ph-&gt;p_offset, ph-&gt;p_filesz);  <span class="comment">//再进行局部拷贝</span></span><br></pre></td></tr></table></figure><p>然后是内存目录管理，上面的代码遗漏了一个非常重要的部分，即内存目录的切换，由于我们是对用户进程的内存空间进行拷贝，所以必须通知CPU，页目录在<code>e-&gt;env_pgdir</code>中，否则CPU还是会继续访问内核内存空间，当访问了不可写的内存区域，内核会发送一个信号停止操作，代码修改如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Change </span></span><br><span class="line">lcr3(PADDR(e-&gt;env_pgdir));  <span class="comment">// 切换至进程页目录</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; ph_num; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (ph[i].p_type == ELF_PROG_LOAD) &#123;</span><br><span class="line">        region_alloc(e, (<span class="keyword">void</span> *)ph[i].p_va, ph[i].p_memsz);</span><br><span class="line">        <span class="built_in">memset</span>((<span class="keyword">void</span> *)ph[i].p_va, <span class="number">0</span>, ph[i].p_memsz);</span><br><span class="line">        <span class="built_in">memcpy</span>((<span class="keyword">void</span> *)ph[i].p_va, binary + ph[i].p_offset, ph[i].p_filesz); </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">lcr3(PADDR(kern_pgdir));     <span class="comment">// 切换至内核页目录</span></span><br></pre></td></tr></table></figure><p>现在再来理解一下注释中的这段话</p><blockquote><p>Loading the segments is much simpler if you can move data directly into the virtual addresses stored in the ELF binary. So which page directory should be in force during this function? </p></blockquote><p>这段话的意思就是让我们切换页目录。</p><p>最后，我们需要记录一下可执行文件的入口，即第一条语句所在的位置，第一条语句地址是<code>ELFHDR-&gt;e_entry</code>，进程<code>e</code>中应当有一个字段专门保存执行时的地址。我们知道CPU是根据<code>eip</code>寄存器找到下一条语句执行的位置的，所以我们要将<code>ELFHDR-&gt;e_entry</code>保存于新进程<code>e</code>的陷帧中，即</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">e-&gt;env_tf.tf_eip = ELFHDR-&gt;e_entry;</span><br></pre></td></tr></table></figure><h4 id="env-create"><a href="#env-create" class="headerlink" title="env_create()"></a>env_create()</h4><h5 id="函数功能-2"><a href="#函数功能-2" class="headerlink" title="函数功能"></a>函数功能</h5><p>创建一个新的进程，然后载入进程的可执行文件</p><h5 id="具体实现-3"><a href="#具体实现-3" class="headerlink" title="具体实现"></a>具体实现</h5><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// This function is ONLY called during kernel initialization,</span></span><br><span class="line"><span class="comment">// before running the first user-mode environment.</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">env_create(<span class="keyword">uint8_t</span> *binary, <span class="keyword">enum</span> EnvType type)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Allocates a new env with env_alloc, the new env's parent ID is set to 0.</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">e</span>;</span></span><br><span class="line">    env_alloc(&amp;e, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Loads the named elf binary into it with load_icode </span></span><br><span class="line">    load_icode(e, binary);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Set the env type to type</span></span><br><span class="line">    e-&gt;env_type = type;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="env-run"><a href="#env-run" class="headerlink" title="env_run()"></a>env_run()</h4><h5 id="函数功能-3"><a href="#函数功能-3" class="headerlink" title="函数功能"></a>函数功能</h5><p>运行进程，同时实现内核态到用户态的转换</p><h5 id="具体实现-4"><a href="#具体实现-4" class="headerlink" title="具体实现"></a>具体实现</h5><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Context switch from curenv to env e.</span></span><br><span class="line"><span class="comment">// Note: if this is the first call to env_run, curenv is NULL.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// This function does not return.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">env_run(struct Env *e)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Hint: This function loads the new environment's state from</span></span><br><span class="line">    <span class="comment">//e-&gt;env_tf.  Go back through the code you wrote above</span></span><br><span class="line">    <span class="comment">//and make sure you have set the relevant parts of</span></span><br><span class="line">    <span class="comment">//e-&gt;env_tf to sensible values.</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Step 1: If this is a context switch (a new environment is running)</span></span><br><span class="line">    <span class="comment">// 1. Set the current environment (if any) back to</span></span><br><span class="line">    <span class="comment">//  ENV_RUNNABLE if it is ENV_RUNNING (think about</span></span><br><span class="line">    <span class="comment">//  what other states it can be in)</span></span><br><span class="line">    <span class="keyword">if</span>(curenv != <span class="literal">NULL</span> &amp;&amp; curenv-&gt;env_status == ENV_RUNNING)&#123;</span><br><span class="line">        curenv-&gt;env_status = ENV_RUNNABLE;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 2. Set 'curenv' to the new environment</span></span><br><span class="line">    curenv = e;</span><br><span class="line">    <span class="comment">// 3. Set its status to ENV_RUNNING</span></span><br><span class="line">    curenv-&gt;env_status = ENV_RUNNING;</span><br><span class="line">    <span class="comment">// 4. Update its 'env_runs' counter</span></span><br><span class="line">    ++curenv-&gt;env_runs;</span><br><span class="line">    <span class="comment">// 5. Use lcr3() to switch to its address space</span></span><br><span class="line">    lcr3(PADDR(curenv-&gt;env_pgdir));   <span class="comment">// 切换页目录至当前进程的页目录</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Step 2: Use env_pop_tf() to restore the environment's</span></span><br><span class="line">    <span class="comment">//   registers and drop into user mode in the</span></span><br><span class="line">    <span class="comment">//   environment.</span></span><br><span class="line">    env_pop_tf(&amp;curenv-&gt;env_tf);      <span class="comment">// 进入内核态</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></div><p>至此，我们完成了进程管理的几个关键函数，当执行一个进程时，内核会调用<code>load_icode</code>加载并执行<code>hello</code>程序，直到使用系统调用后，通过<code>int</code>在进入内核中。但是这里会出现问题，OS尚未配置硬件实现用户态到内核态的转换。因此会触发保护异常，但是依然无法处理异常，于是又会触发一个保护异常的保护异常（开始套娃）。最终放弃，并产生一个<code>triple fault</code>然后重启系统。</p><p>这里我们进行一些调试，以gdb模式启动内核，然后在<code>env_pop_tf</code>处设置断点。这个是进入用户态之前的最后一个函数，在对现场进行了一些保护后，进程跳转至了<code>0x800020</code>这个地址，然后开始执行用户进程。查看<code>obj/user/hello.asm</code>获得<code>hello</code>中调用的系统调用<code>sys_cputs()</code>中<code>int</code>的地址（说明系统调用是中断触发的）。找到<code>int $0x30</code>所在地址（0x800a9b），设置断点执行，到这一句前应该都没有问题。如果有问题，那一定是你的问题。</p><h3 id="中断和异常处理"><a href="#中断和异常处理" class="headerlink" title="中断和异常处理"></a>中断和异常处理</h3><p>现在操作系统已经能够实现内核态到用户态的切换，由于中断会进入内核态，所以我们还需要完成用户态到内核态的切换，实现中断和异常。首先先熟悉x86的中断和异常机制</p><div class="note default">            <p><strong>练习3</strong>：阅读如下材料，学习中断和异常背后的硬件知识</p><p><a href="https://pdos.csail.mit.edu/6.828/2018/readings/ia32/IA32-3A.pdf">IA-32 Developer’s Manual 第五章</a></p>          </div><h3 id="受保护的控制转移"><a href="#受保护的控制转移" class="headerlink" title="受保护的控制转移"></a>受保护的控制转移</h3><p>异常和中断实际上都是受保护的控制转移，即在内核和用户之间的切换。按照英特尔的术语，中断是一种异步控制转移，而异常是同步的。受保护是指：<strong>当中断或异常发生后，当前运行的代码只能以指定的方式进入内核</strong>。在x86中，保护是由两种机制提供的：</p><p>1 <strong>中断描述表（IDT）</strong>：处理器保证进程只能由内核提供的进入点进入内核。x86提供了256个进入点，即256个中断向量（0-255）。中断向量由中断来源决定。CPU根据中断向量，到中断描述表指定的位置寻找中断描述符，并加载如下内容：</p><ul><li>将中断服务函数入口载入EIP中</li><li>将服务函数所在的代码段保存在CS寄存器中</li></ul><p>2 <strong>任务状态段（TSS）</strong>：在处理中断和异常前，处理器需要一个空间保存旧的状态，例如CS和EIP寄存器中的值，以便后续恢复现场。保存这些状态的空间必须被严格保护，禁止低权限的用户进程访问。因此当发生用户态到内核态的切换时，OS会切换至一个位于内核内存空间的堆栈段，并对关键数据进行保存。TSS即设置了这个堆栈的段选择符和地址。处理器会将<code>SS</code>, <code>ESP</code>, <code>EFLAGS</code>, <code>CS</code>, <code>EIP</code>和一些错误码保存在堆栈中，并从中断描述符中加载CS和EIP，并设置指向新堆栈的ESP和SS。</p><p>在JOS中，我们只利用TSS来保存内核堆栈的位置，实际的操作系统中TSS还有许多其他功能。</p><h3 id="异常和中断的种类"><a href="#异常和中断的种类" class="headerlink" title="异常和中断的种类"></a>异常和中断的种类</h3><p>这一节详见<a href="/2020/09/13/计算机/操作系统/中断、异常和系统调用/" title="关于中断和异常区别的讲解">关于中断和异常区别的讲解</a>。本节我们将会处理0-31号中断。下一节我们会处理48号软中断。在Lab4中，我们还会添加一些外部中断，例如定时器中断等。</p><h4 id="一个例子"><a href="#一个例子" class="headerlink" title="一个例子"></a>一个例子</h4><p>假设处理器正在执行一个用户进程，结果遇到了除零异常，处理器会这样处理：</p><p>1 根据TSS中的<code>SS0</code>和<code>ESP0</code>字段，跳转至内核栈字段，在JOS中，<code>SS0</code>和<code>ESP0</code>的值分别为<code>GD_KD</code>和<code>KSTACKTOP</code></p><p>2 将异常参数压入内核栈中，栈顶地址为<code>KSTACKTOP</code>，压完后内核栈如下所示：</p><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/kernelstack.svg" width = "350" alt="图片名称" align=center /></p><p>3 由于我们在处理除零错误，其中断向量号为0，因此处理器读取IDT的入口0，并设置<code>CS:EIP</code>至中断服务函数的入口地址</p><p>4 处理函数将会接管并处理异常，例如退出用户程序等</p><p>对于特定型号的x86处理器，除了上面的五个标准字段，还会向栈中压入一个错误码（一般是32位），关于错误码详见<a href="https://pdos.csail.mit.edu/6.828/2018/readings/ia32/IA32-3A.pdf">IA-32 Developer’s Manual 第五章第13节</a>。有了错误码后，在返回时必须弹出错误码，否则会返回错误的位置执行程序。</p><h3 id="中断-异常嵌套"><a href="#中断-异常嵌套" class="headerlink" title="中断/异常嵌套"></a>中断/异常嵌套</h3><p>中断可能是在内核或者用户态产生，只有从用户态进入内核态时，才需要进行堆栈的切换，即对用户态堆栈的地址进行保存。如果已经位于内核中，那么内核就不需要进行栈切换，直接保存旧的<code>CS</code>和<code>EIP</code>即可。此外，对于内核产生的中断，我们可以很轻易地进行嵌套处理。因为内核处理自己的中断，可以简单地理解为函数调用。</p><p>极端情况下，当中断嵌套过多后，内核栈会爆掉，这种情况下内核只能重启。一个设计良好的内核应该确保这种极端情况永远不发生。</p><h3 id="设置IDT"><a href="#设置IDT" class="headerlink" title="设置IDT"></a>设置IDT</h3><p>现在，我们开始设置IDT，处理中断向量号为0-31的中断，首先，阅读 <a href="/downloads/inc_trap.h"><code>inc/trap.h</code></a>and<a href="/downloads/kern_trap.h"><code>kern/trap.h</code></a>，这两个文件包含了一些和中断、异常相关的重要定义。其中，<code>kern/trap.h</code>包含着和内核严格私有的代码，而<code>inc/trap.h</code>包含着和内核和用户态相关的代码。</p><p>注意，0-31号向量中有一些是保留的，这些不需要处理。我们实现的中断控制流应当如下所示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">// 通过IDT，在trapentry中找到中断服务函数入口，然后跳转至trap中</span><br><span class="line">       IDT                   trapentry.S         trap.c</span><br><span class="line">   </span><br><span class="line">+----------------+                        </span><br><span class="line">|   &amp;handler1    |---------&gt; handler1:          trap (struct Trapframe *tf)</span><br><span class="line">|                |             // do stuff      &#123;</span><br><span class="line">|                |             call trap          // handle the exception/interrupt</span><br><span class="line">|                |             // ...           &#125;</span><br><span class="line">+----------------+</span><br><span class="line">|   &amp;handler2    |--------&gt; handler2:</span><br><span class="line">|                |            // do stuff</span><br><span class="line">|                |            call trap</span><br><span class="line">|                |            // ...</span><br><span class="line">+----------------+</span><br><span class="line">       .</span><br><span class="line">       .</span><br><span class="line">       .</span><br><span class="line">+----------------+</span><br><span class="line">|   &amp;handlerX    |--------&gt; handlerX:</span><br><span class="line">|                |             // do stuff</span><br><span class="line">|                |             call trap</span><br><span class="line">|                |             // ...</span><br><span class="line">+----------------+</span><br></pre></td></tr></table></figure><p>每一个异常或中断都需要有自己的中断服务函数（在<code>trapentry.S</code>中），同时<code>trap_init()</code>将对IDT进行初始化，写入这些服务函数的地址。每个服务函数应当在栈上建立一个<code>struct TrapFrame</code>，然后调用<code>trap()</code>并传入建立的陷帧 (在 <code>trap.c</code>) 。<code>trap</code>将会使用特定的服务函数处理中断和异常。</p><div class="note default">            <p><strong>练习4</strong>：编辑<code>trapentry.S</code>和<code>trap.c</code>，实现上述功能。<code>trapentry.S</code>中的 <code>TRAPHANDLER</code> 和<code>TRAPHANDLER_NOEC</code> 宏以及<code>inc/trap.c</code>中的<code>T_*</code>能够帮助你。你需要在<code>trapentry.S</code>中为<code>inc/trap.h</code>中的每一个trap添加一个入口，并提供<code>_alltraps</code>作为<code>TRAPHANDLER</code>的参考。同时，你需要修改<code>trap_init()</code>，初始化<code>idt</code>，令其指向<code>trapentry.S</code>中定义的每一个入口，此处请使用<code>SETGATE</code>宏。</p><p>你的<code>_alltraps</code>应当：</p><ol><li>将对应的值压入栈中，使栈看起来像一个陷帧</li><li>将<code>GD_KD</code>载入<code>%ds</code>和<code>%es</code></li><li><code>pushl %esp</code> ，向<code>trap()</code>传递一个指向陷帧的指针</li><li><code>call trap</code>（<code>trap</code>可以返回吗？）</li></ol><p>使用<code>pushal</code>，这个指令符合<code>struct Trapframe</code>的布局，在完成上述内容后，<code>make grade</code>应当能够通过Part A</p>          </div><p>练习4的要求挺多的，我们将问题一个一个拆解，分而治之。经过拆分后，练习4一共需要完成如下功能：</p><ul><li>在<code>trapentry.S</code>中为<code>inc/trap.h</code>中的每一个trap添加一个入口</li><li>编写<code>_alltraps</code>函数</li><li>修改<code>trap_init()</code>，初始化<code>idt</code>，令其指向<code>trapentry.S</code>中定义的每一个入口</li></ul><h4 id="实现顺序"><a href="#实现顺序" class="headerlink" title="实现顺序"></a>实现顺序</h4><p>为了解决这个问题，我们考虑如下实现顺序：</p><ul><li>首先，在<code>trapentry.S</code>中添加入口，但是不实现</li><li>修改<code>trap_init()</code>，初始化<code>idt</code>，令其指向每一个入口</li></ul><h4 id="在trapentry-S中为inc-trap-h中的每一个trap添加一个入口"><a href="#在trapentry-S中为inc-trap-h中的每一个trap添加一个入口" class="headerlink" title="在trapentry.S中为inc/trap.h中的每一个trap添加一个入口"></a>在<code>trapentry.S</code>中为<code>inc/trap.h</code>中的每一个trap添加一个入口</h4><h5 id="TRAPHANDLER-和TRAPHANDLER-NOEC"><a href="#TRAPHANDLER-和TRAPHANDLER-NOEC" class="headerlink" title="TRAPHANDLER 和TRAPHANDLER_NOEC"></a><code>TRAPHANDLER</code> 和<code>TRAPHANDLER_NOEC</code></h5><p>我们首先来看一下这两个宏函数，其定义如下：</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">/* TRAPHANDLER defines a globally-visible function for handling a trap.</span></span><br><span class="line"><span class="comment"> * It pushes a trap number onto the stack, then jumps to _alltraps.</span></span><br><span class="line"><span class="comment"> * Use TRAPHANDLER for traps where the CPU automatically pushes an error code.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * You shouldn't call a TRAPHANDLER function from C, but you may</span></span><br><span class="line"><span class="comment"> * need to _declare_ one in C (for instance, to get a function pointer</span></span><br><span class="line"><span class="comment"> * during IDT setup).  You can declare the function with</span></span><br><span class="line"><span class="comment"> *   void NAME();</span></span><br><span class="line"><span class="comment"> * where NAME is the argument passed to TRAPHANDLER.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TRAPHANDLER(name, num)                      \</span></span><br><span class="line">    .globl name;        <span class="comment">/* define global symbol for 'name' */</span>   \</span><br><span class="line">    .type name, @function;  <span class="comment">/* symbol type is function */</span>       \</span><br><span class="line">    .align <span class="number">2</span>;       <span class="comment">/* align function definition 令函数对齐，即其起始地址为2的倍数*/</span>     \ </span><br><span class="line">    name:           <span class="comment">/* function starts here */</span>      \</span><br><span class="line">                    <span class="comment">// 在此处CPU会自动压入一个错误码</span></span><br><span class="line">    pushl $(num);   <span class="comment">/* 将num地址压栈 */</span>               \</span><br><span class="line">    jmp _alltraps</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Use TRAPHANDLER_NOEC for traps where the CPU doesn't push an error code.</span></span><br><span class="line"><span class="comment"> * It pushes a 0 in place of the error code, so the trap frame has the same</span></span><br><span class="line"><span class="comment"> * format in either case.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TRAPHANDLER_NOEC(name, num)                 \</span></span><br><span class="line">    .globl name;                            \</span><br><span class="line">    .type name, @function;                      \</span><br><span class="line">    .align <span class="number">2</span>;                           \</span><br><span class="line">    name:                               \</span><br><span class="line">    pushl $<span class="number">0</span>;         <span class="comment">/* 错误码占位 */</span>      \</span><br><span class="line">    pushl $(num);                         \</span><br><span class="line">    jmp _alltraps</span><br></pre></td></tr></table></figure></div></div><p>这两个宏函数能够帮助我们定义中断服务函数入口，例如我们想定义vector0作为中断0的入口，那么只需要写入下面的代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">TRAPHANDLER_NOEC(vector0, <span class="number">0</span>)    <span class="comment">// 中断0 入口为 vector0，向量号为0</span></span><br></pre></td></tr></table></figure><p>这个宏会被扩展为：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">.globl vector0;                            </span><br><span class="line">.type vector0, @function;                      </span><br><span class="line">.align <span class="number">2</span>;                           </span><br><span class="line">vector0:                               </span><br><span class="line">pushl $<span class="number">0</span>;         <span class="comment">/* 错误码占位 */</span>      </span><br><span class="line">pushl $<span class="number">0</span>;                         </span><br><span class="line">jmp _alltraps</span><br></pre></td></tr></table></figure><h5 id="具体实现-5"><a href="#具体实现-5" class="headerlink" title="具体实现"></a>具体实现</h5><p>根据上面的两个宏，我们能够写出入口函数的定义如下所示，需要注意的是我们要查询硬件手册，看哪些中断要记录Error Code，哪些不需要。同时，中断向量号已经给出了我们宏定义，具体实现如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* Set up the entries for traps, care about whether</span></span><br><span class="line"><span class="comment"> * the interrupt has error code, if not, use TRAPHANDLER_NOEC</span></span><br><span class="line"><span class="comment"> * otherwise use the TRAPHANDLER</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">TRAPHANDLER_NOEC(divide_entry, T_DIVIDE)          # vector0:  divide zero, NOEC</span><br><span class="line">TRAPHANDLER_NOEC(debug_entry, T_DEBUG)            # vector1:  debug exception, NOEC</span><br><span class="line">TRAPHANDLER_NOEC(nmi_entry, T_NMI)                # vector2:  non-maskable interrupt, NOEC</span><br><span class="line">TRAPHANDLER_NOEC(breakpoint_entry, T_BRKPT)       # vector3:  breakpoint, NOEC</span><br><span class="line">TRAPHANDLER_NOEC(overflow_entry, T_OFLOW)         # vector4:  overflow, NOEC</span><br><span class="line">TRAPHANDLER_NOEC(bound_entry, T_BOUND)            # vector5:  bounds check, NOEC</span><br><span class="line">TRAPHANDLER_NOEC(illop_entry, T_ILLOP)            # vector6:  illegal opcode</span><br><span class="line">TRAPHANDLER_NOEC(device_entry, T_DEVICE)          # vector7:  device <span class="keyword">not</span> available</span><br><span class="line">TRAPHANDLER(dblflt_entry, T_DBLFLT)               # vector8:  <span class="keyword">double</span> fault</span><br><span class="line"># TRAPHANDLER_NOEC(coproc_entry, T_COPROC)        # vector9:  reserved</span><br><span class="line">TRAPHANDLER(tss_entry, T_TSS)                     # vector10: invalid task <span class="keyword">switch</span> segment</span><br><span class="line">TRAPHANDLER(segnp_entry, T_SEGNP)                 # vector11: segment <span class="keyword">not</span> present</span><br><span class="line">TRAPHANDLER(stack_entry, T_STACK)                 # vector12: <span class="built_in">stack</span> exception</span><br><span class="line">TRAPHANDLER(gpflt_entry, T_GPFLT)                 # vector13: general protection fault</span><br><span class="line">TRAPHANDLER(pgflt_entry, T_PGFLT)                 # vector14: page fault</span><br><span class="line"># TRAPHANDLER_NOEC(res_entry, T_RES)             # vector15: reserved</span><br><span class="line">TRAPHANDLER_NOEC(fperr_entry, T_FPERR)            # vector16: floating point error</span><br><span class="line">TRAPHANDLER(align_entry, T_ALIGN)                 # vector17: aligment check</span><br><span class="line">TRAPHANDLER_NOEC(mchk_entry, T_MCHK)              # vector18: machine check</span><br><span class="line">TRAPHANDLER_NOEC(simderr_entry, T_SIMDERR)        # vector19: SIMD floating point error</span><br></pre></td></tr></table></figure><h4 id="编写-alltraps函数"><a href="#编写-alltraps函数" class="headerlink" title="编写_alltraps函数"></a>编写<code>_alltraps</code>函数</h4><h5 id="函数功能-4"><a href="#函数功能-4" class="headerlink" title="函数功能"></a>函数功能</h5><p>通过查看<a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev11.pdf">xv6 手册中关于alltrap的讲解</a>，可知这个函数完成了如下几件事请：</p><ul><li>将<code>%ds</code>、<code>%es</code>、<code>%fs</code>、<code>%gs</code>进行保存</li><li>然后将<code>eax</code> 、<code>ecx</code>、 <code>edx</code>、 <code>ebx</code>、 <code>oesp</code>、 <code>ebp</code>、 <code>esi</code>、 <code>edi</code>这些寄存器进行保存，这些操作可以用<code>pushal</code>一次性实现</li><li>将<code>GD_KD</code>载入<code>%ds</code>和<code>%es</code></li><li><code>pushl %esp</code> ，向<code>trap()</code>传递一个指向陷帧的指针</li><li><code>call trap</code>（<code>trap</code>可以返回吗？）</li></ul><h5 id="具体实现-6"><a href="#具体实现-6" class="headerlink" title="具体实现"></a>具体实现</h5><p>根据上面总结的功能，我们能够得到<code>_alltraps</code>的实现如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">.globl _alltraps</span><br><span class="line">_alltraps:</span><br><span class="line">    # Step1: push ds, es, fs and gs separately</span><br><span class="line">    pushl %ds      # check the trap frame and get the length of the registers</span><br><span class="line">    pushl %es</span><br><span class="line">    pushl %fs</span><br><span class="line">    pushl %gs</span><br><span class="line">        </span><br><span class="line">    # Step2: push general purpose registers using pushal</span><br><span class="line">    pushal</span><br><span class="line">    </span><br><span class="line">    # Step3: load GD_KD in %ds and %es using movw</span><br><span class="line">    movw $(GD_KD), %ax  </span><br><span class="line">    movw %ax, %ds</span><br><span class="line">    movw %ax, %es</span><br><span class="line">    </span><br><span class="line">    # Step4: call trap(tf), where tf=%esp</span><br><span class="line">    pushl %esp</span><br><span class="line">    call trap</span><br></pre></td></tr></table></figure><h4 id="修改trap-init-，初始化idt，令其指向trapentry-S中定义的每一个入口"><a href="#修改trap-init-，初始化idt，令其指向trapentry-S中定义的每一个入口" class="headerlink" title="修改trap_init()，初始化idt，令其指向trapentry.S中定义的每一个入口"></a>修改<code>trap_init()</code>，初始化<code>idt</code>，令其指向<code>trapentry.S</code>中定义的每一个入口</h4><h5 id="函数功能-5"><a href="#函数功能-5" class="headerlink" title="函数功能"></a>函数功能</h5><p><code>trap_init()</code>函数的功能就是初始化<code>idt</code>，将<code>idt</code>数组中第$i$个值<code>idt[i]</code>设置为<code>trapentry.S</code>定义的入口。这里需要我们了解<code>idt</code>的具体格式，参考<a href="https://pdos.csail.mit.edu/6.828/2018/lec/x86_idt.pdf">x86_idt.pdf (mit.edu)</a>。在<code>mmu.h</code>中定义了<code>struct Gatedesc</code>对中断描述符进行描述，具体即每一位的作用</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Gatedesc</span> &#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_off_15_0 : <span class="number">16</span>;   <span class="comment">// low 16 bits of offset in segment</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_sel : <span class="number">16</span>;        <span class="comment">// segment selector</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_args : <span class="number">5</span>;        <span class="comment">// # args, 0 for interrupt/trap gates</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_rsv1 : <span class="number">3</span>;        <span class="comment">// reserved(should be zero I guess)</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_type : <span class="number">4</span>;        <span class="comment">// type(STS_&#123;TG,IG32,TG32&#125;)</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_s : <span class="number">1</span>;           <span class="comment">// must be 0 (system)</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_dpl : <span class="number">2</span>;         <span class="comment">// descriptor(meaning new) privilege level</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_p : <span class="number">1</span>;           <span class="comment">// Present</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_off_31_16 : <span class="number">16</span>;  <span class="comment">// high bits of offset in segment</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>我们使用<code>SETGATE</code>宏函数实现<code>idt</code>到入口的绑定，该函数见本文附录，为了正确调用这个函数，需要依次考虑如下问题：</p><ul><li>中断服务函数入口的代码段在哪里？在内核的代码段，查看<code>memlayout.h</code>可知，为<code>GD_KT</code>(0x08)</li><li>是什么类型的，中断？异常？查看中断描述符手册</li><li>中断服务函数入口的偏移量又是多少 ？即<code>TRAPHANDLER</code>和<code>TRAPHANDLER_NOEC</code>定义的函数的偏移量</li></ul><h5 id="具体实现-7"><a href="#具体实现-7" class="headerlink" title="具体实现"></a>具体实现</h5><p>具体绑定过程代码如下：</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">trap_init</span><span class="params">(<span class="keyword">void</span>)</span></span>&#123;</span><br><span class="line">    <span class="keyword">extern</span> <span class="class"><span class="keyword">struct</span> <span class="title">Segdesc</span> <span class="title">gdt</span>[];</span></span><br><span class="line">    </span><br><span class="line">    SETGATE(idt[T_DIVIDE], <span class="number">0</span>, GD_KT, divide_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_DEBUG], <span class="number">1</span>, GD_KT, debug_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_NMI], <span class="number">1</span>, GD_KT, nmi_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_BRKPT], <span class="number">0</span>, GD_KT, breakpoint_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_OFLOW], <span class="number">0</span>, GD_KT, overflow_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_BOUND], <span class="number">0</span>, GD_KT, bound_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_ILLOP], <span class="number">0</span>, GD_KT, illop_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_DEVICE], <span class="number">0</span>, GD_KT, device_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_DBLFLT], <span class="number">0</span>, GD_KT, dblflt_entry, <span class="number">0</span>)</span><br><span class="line">    <span class="comment">//SETGATE(idt[T_COPROC], 0, GD_KT, coproc_entry, 0)</span></span><br><span class="line">    SETGATE(idt[T_TSS], <span class="number">0</span>, GD_KT, tss_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_SEGNP], <span class="number">0</span>, GD_KT, segnp_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_STACK], <span class="number">0</span>, GD_KT, stack_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_GPFLT], <span class="number">0</span>, GD_KT, gpflt_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_PGFLT], <span class="number">0</span>, GD_KT, pgflt_entry, <span class="number">0</span>)</span><br><span class="line">    <span class="comment">//SETGATE(idt[T_RES], 0, GD_KT, res_entry, 0)</span></span><br><span class="line">    SETGATE(idt[T_FPERR], <span class="number">0</span>, GD_KT, fperr_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_ALIGN], <span class="number">0</span>, GD_KT, align_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_MCHK], <span class="number">0</span>, GD_KT, mchk_entry, <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_SIMDERR], <span class="number">0</span>, GD_KT, simderr_entry, <span class="number">0</span>)</span><br><span class="line">        </span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></div><p>结果写完之后一运行，提示这些函数入口都没定义，这个就很难受了，参考xv6的源码，我们还需要在<code>trapentry.S</code>中手动添加<code>*_entry</code>的入口地址，在<code>trapentry.S</code>中设置一个代码段，创建<code>vectors</code>数组，将每一个<code>*_entry</code>对应的地址进行保存：</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">.data</span><br><span class="line">.globl vectors</span><br><span class="line">vectors:</span><br><span class="line">  .long divide_entry</span><br><span class="line">  .long debug_entry</span><br><span class="line">  .long nmi_entry</span><br><span class="line">  .long breakpoint_entry</span><br><span class="line">  .long overflow_entry</span><br><span class="line">  .long bound_entry</span><br><span class="line">  .long illop_entry</span><br><span class="line">  .long device_entry</span><br><span class="line">  .long dblflt_entry</span><br><span class="line">#  .long coproc_entry</span><br><span class="line">  .long tss_entry</span><br><span class="line">  .long segnp_entry</span><br><span class="line">  .long stack_entry</span><br><span class="line">  .long gpflt_entry</span><br><span class="line">  .long pgflt_entry</span><br><span class="line">#  .long res_entry</span><br><span class="line">  .long fperr_entry</span><br><span class="line">  .long align_entry</span><br><span class="line">  .long mchk_entry</span><br><span class="line">  .long simderr_entry</span><br></pre></td></tr></table></figure></div></div><p>在有了<code>vectors</code>数组后，我们还可以采用循环的方式对<code>idt</code>进行初始化，代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">trap_init(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">extern</span> <span class="class"><span class="keyword">struct</span> <span class="title">Segdesc</span> <span class="title">gdt</span>[];</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// LAB 3: Your code here.</span></span><br><span class="line">    <span class="keyword">uint16_t</span> i;</span><br><span class="line">    <span class="comment">// Initialize the first 19 idt by using a loop</span></span><br><span class="line">    <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; <span class="number">19</span>; ++i)&#123;</span><br><span class="line">        SETGATE(idt[i], <span class="number">0</span>, GD_KT, vectors[i], <span class="number">0</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    SETGATE(idt[T_DEBUG], <span class="number">1</span>, GD_KT, vectors[T_DEBUG], <span class="number">0</span>)</span><br><span class="line">    SETGATE(idt[T_BRKPT], <span class="number">0</span>, GD_KT, vectors[T_BRKPT], <span class="number">3</span>)    <span class="comment">// Int 3's DPL is 3</span></span><br><span class="line">    SETGATE(idt[T_NMI], <span class="number">1</span>, GD_KT, vectors[T_NMI], <span class="number">0</span>)</span><br><span class="line">    <span class="comment">// Per-CPU setup </span></span><br><span class="line">    trap_init_percpu();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里我们只针对前19个<code>idt</code>进行初始化，其他的先忽略。</p><h4 id="调试与总结"><a href="#调试与总结" class="headerlink" title="调试与总结"></a>调试与总结</h4><p>在完成上面的相关代码后，我们现在应该能够运行用户程序，然后对异常进行处理，运行<code>make grade</code>应该能够通过 <code>divzero</code>、<code>softint</code>、 <code>badsegment</code>三个测试，通过part A，然后获得三十分。然而我的代码有问题，需要进行调试。</p><p>我们先针对<code>divzero</code>进行调试，查看<code>divzero</code>的输出日志，截取其中的栈帧部分，可以看到如下内容：</p><div><div class="fold_hider"><div class="close hider_title">点击显/隐内容</div></div><div class="fold"><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">TRAP frame at <span class="number">0xefffffb4</span></span><br><span class="line">  edi  <span class="number">0x00000000</span></span><br><span class="line">  esi  <span class="number">0x00000000</span></span><br><span class="line">  ebp  <span class="number">0xeebfdfd0</span></span><br><span class="line">  oesp <span class="number">0xefffffd4</span></span><br><span class="line">  ebx  <span class="number">0x00000000</span></span><br><span class="line">  edx  <span class="number">0x00000000</span></span><br><span class="line">  ecx  <span class="number">0x00000000</span></span><br><span class="line">  eax  <span class="number">0x00000001</span></span><br><span class="line">  es   <span class="number">0</span>x---<span class="number">-0023</span></span><br><span class="line">  ds   <span class="number">0</span>x---<span class="number">-0023</span></span><br><span class="line">  trap <span class="number">0x00000023</span> (unknown trap)</span><br><span class="line">  err  <span class="number">0x00000023</span></span><br><span class="line">  eip  <span class="number">0x00000000</span></span><br><span class="line">  cs   <span class="number">0</span>x---<span class="number">-0000</span></span><br><span class="line">  flag <span class="number">0x0080004e</span></span><br></pre></td></tr></table></figure></div></div><p>可以看到trap一行显示的是 unknown trap。除零应当是除零中断，怎么能是unknown trap呢，说明我们在处理栈帧的时候出现了问题，最有可能的是_alltraps写错了，导致栈布局不对。经过对比，因为我直接参考了xv6的代码，所以多压入了两个寄存器，直接导致栈布局错误，将下面两行删除即可：</p><pre><code># pushl %fs# pushl %gs  </code></pre><p>现在运行可以通过<code>divzero</code>，然后此处我又犯了一个小bug，由于我使用循环的方式对前19个中断进行处理，然而上面的代码中我注释掉了两个保留的中断向量入口，这就导致<code>idt</code>和<code>vectors</code>的映射关系错误了，后面的中断都往前移动了一个，所以如果不是采用一一赋值的方式，上面的代码就不能注释掉，并检查对应关系。修改之后即可通过Part A。</p><div class="note default">            <p><strong>问题</strong>：回答下面的问题</p><ol><li>为何每个中断/异常都要有自己独立的服务函数，如果所有的中断/异常都被送入同一个入口，那么上面的什么机制无法实现？</li><li>是否需要做一些修改，使得<code>user/softint</code>表现正常？打分脚本期望产生一个通用保护错误(trap 13)，但是<code>softint</code>的代码中写了<code>int $14</code>。为何触发了中断向量13？如果内核允许<code>softint</code>的<code>int $14</code>命令触发一个缺页异常，会发生什么？</li></ol><p>回答：</p><ol><li>错误码无法实现，因为有的中断保存错误码，有的不保存，所以必须要不同的入口</li><li>在这里我们首先要明白什么情况会触发trap 13，通过查阅80386手册9.8.13节可知，所有的不触发其他中断的错误，都被归类为GP，其中第14条说：如果在非内核态(privilege 0)中触发中断，那么就会产生这个异常。所以我们不需要修改，操作系统的处理是正确的。如果引发了一个缺页异常，就是用户态直接调用了中断指令，这样做有悖于其优先级。</li></ol>          </div><h2 id="第二部分：页错误、断点异常、系统调用"><a href="#第二部分：页错误、断点异常、系统调用" class="headerlink" title="第二部分：页错误、断点异常、系统调用"></a>第二部分：页错误、断点异常、系统调用</h2><h3 id="处理页错误"><a href="#处理页错误" class="headerlink" title="处理页错误"></a>处理页错误</h3><p>页错误（中断号14：T_PGFLT）是一个非常重要的异常。当处理器触发页错误后，会在CR2中保存触发页错误的指令的地址。 在<code>trap.c</code> 我们提供了 <code>page_fault_handler()</code>处理页错误。</p><div class="note default">            <p><strong>练习5</strong>：修改 <code>trap_dispatch()</code> ，向<code>page_fault_handler()</code>发送页错误。现在<code>make grade</code>应该能通过<code>faultread</code>、<code>faultreadkernel</code>、<code>faultwrite</code>以及<code>faultwritekernel</code>。你可以使用<code>make run-x</code>命令令JOS启动后执行对应的用户程序，例如<code>make run-divzero</code></p>          </div><h4 id="trap-dispatch"><a href="#trap-dispatch" class="headerlink" title="trap_dispatch()"></a>trap_dispatch()</h4><p>这个函数的功能是根据不同的中断号，调用具体的服务函数，dispatch的意思是派遣。那么为了解决page fault，我们只需要写下下面的代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span></span><br><span class="line">trap_dispatch(struct Trapframe *tf)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Handle processor exceptions.</span></span><br><span class="line">    <span class="comment">// Handle the trap according to the trap num</span></span><br><span class="line">    <span class="keyword">switch</span> (tf-&gt;tf_trapno)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// Handle the page fault (vector 14)</span></span><br><span class="line">        <span class="keyword">case</span> T_PGFLT:</span><br><span class="line">            page_fault_handler(tf);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Unexpected trap: The user process or the kernel has a bug.</span></span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            print_trapframe(tf);</span><br><span class="line">            <span class="keyword">if</span> (tf-&gt;tf_cs == GD_KT)&#123;</span><br><span class="line">                panic(<span class="string">"unhandled trap in kernel"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                env_destroy(curenv);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在应该能够通过题目中说的几个函数，再拿到20分</p><h3 id="断点异常"><a href="#断点异常" class="headerlink" title="断点异常"></a>断点异常</h3><p>断点异常(T_BRKPT=3)允许调试器在代码中插入断点，这个异常一般用于调试器插入断点，通过替换相关语句为一个1 byte的<code>int3</code>软中断指令。在JOS中，我们将针对该指令做一些调整，令其变为一个伪系统调用供用户程序使用。实际上，<code>lib/panic.c</code>中的<code>panic()</code>函数就通过调用<code>int3</code>实现了中断过程。</p><div class="note default">            <p><strong>练习6</strong>：修改 <code>trap_dispatch()</code> ，添加断点异常并激活kernel monitor</p>          </div><p>这个任务比较简单，直接给出代码，先写一个breakpoint的handler函数，然后在trap_dispatch中添加对应的路径即可</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> </span><br><span class="line">breakpoint_handler(struct Trapframe *tf)</span><br><span class="line">&#123;</span><br><span class="line">    monitor(tf);   <span class="comment">// invoke the kernel monitor</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里需要注意的是<code>int3</code>的DPL为3，即SETGATE的最后一个参数是3，否则用户进程无法激活该中断。</p><div class="note danger">            <p><strong>挑战任务！</strong>：修改 <code>trap_dispatch()</code> ，添加断点异常，使得程序能够从当前位置继续执行（即在由断点异常引发的<code>int3</code>语句执行后）。实现真正的单步调试功能。你需要掌握<code>EFLAGS</code>中每一位的作用。</p>          </div><div class="note default">            <p><strong>问题</strong>：</p><ol><li>根据<code>IDT</code>初始化的过程，breakpoint中断会产生通用保护错误或者断点异常。为什么？你需要如何设置<code>IDT</code>，使得breakpoint产生断点异常？</li><li>你认为这个机制的目的是什么？</li></ol><p>回答：</p><ol><li>我们需要设置breakpoint中断<code>idt</code>的DPL为3，这样就允许用户进程激活该中断。</li><li>这个机制一方面可以保护内核，让用户进程不能随意动用中断来进入内核态；另一方面也开放了一些中断，方便用户执行一些特定的功能，例如断点调试等。</li></ol>          </div><h3 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h3><p>用户进程通过系统调用向内核请求服务。当用户进程激活系统调用后，处理器会进入内核态，处理器和内核共同合作保存用户进程的状态，随后内核执行系统调用，然后返回用户进程。</p><p>在JOS中，我们使用<code>int</code>指令产生处理器中断。我们将<code>int $0x30</code>用作系统调用中断，中断号为<code>T_SYSCALL</code>。设置系统调用中断的<code>idt</code>，使得用户能够触发该中断。</p><p>应用会将系统调用号和系统调用参数放置在寄存器中。这样，内核不需要在用户进程的栈或者指令流中读取。系统调用号将被放入<code>%eax</code>中，参数（最多五个）将分别进入 <code>%edx</code>、<code>%ecx</code>、<code>%ebx</code>、<code>%edi</code>和<code>%esi</code>。内核将返回值放入<code>%eax</code>。在<code>lib/syscall.c</code>的<code>syscall()</code>函数中提供了激活系统调用的代码，阅读并理解其中的内容（<code>lib</code>中的<code>syscall</code>是给用户使用的，通过<code>int</code>指令激活系统调用）。</p><div class="note default">            <p><strong>练习7</strong>：添加系统调用中断<code>T_SYSCALL</code>的服务函数，编辑<code>kern/trapentry.S</code>和<code>kern/trap.c</code>的<code>trap_init()</code>。修改<code>trap_dispatch()</code>，通过调用<code>kern/syscall.c</code>的<code>syscall()</code>，并根据适当的参数，处理系统调用。将返回值保存在<code>%eax</code>中。最后，修改<code>kern/syscall.c</code>的<code>syscall()</code>。请阅读<code>lib/syscall.c</code>，并弄明白里面的内联汇编语句。通过激活对应的内核函数，处理<code>inc/syscall.h</code>中的所有系统调用。</p><p>通过<code>make run-hello</code>运行<code>user/hello</code>，现在应当在控制台打印”hello world”，并触发页错误。同时，<code>make grade</code>应当能通过<code>testbss</code>。</p>          </div><p>练习7可以分为如下几个部分分别完成：</p><h4 id="阅读lib-syscall-c"><a href="#阅读lib-syscall-c" class="headerlink" title="阅读lib/syscall.c"></a>阅读<code>lib/syscall.c</code></h4><p>这个文件中<code>syscall()</code>函数的关键代码是一句内联汇编，关于内联汇编可以参考<a href="/2021/04/28/工具/Assembly/内联汇编/" title="关于内联汇编的讲解">关于内联汇编的讲解</a>。这句内联汇编如下所示：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">"int %1\n"</span>       <span class="comment">// volatile 禁止优化  </span></span></span></span><br><span class="line"><span class="function"><span class="params">                              <span class="comment">// %1 表示第二个参数，即下面的输出和输入顺序中第二个参数，此处为 T_SYSCALL</span></span></span></span><br><span class="line"><span class="function"><span class="params">     : <span class="string">"=a"</span> (ret)         <span class="comment">// 输出，从ax寄存器输出至ret</span></span></span></span><br><span class="line"><span class="function"><span class="params">     : <span class="string">"i"</span> (T_SYSCALL),   <span class="comment">// 输入，将T_SYSCALL输入至i寄存器</span></span></span></span><br><span class="line"><span class="function"><span class="params">       <span class="string">"a"</span> (num),         <span class="comment">// 输入，将num输入至a寄存器</span></span></span></span><br><span class="line"><span class="function"><span class="params">       <span class="string">"d"</span> (a1),          <span class="comment">// ...</span></span></span></span><br><span class="line"><span class="function"><span class="params">       <span class="string">"c"</span> (a2),</span></span></span><br><span class="line"><span class="function"><span class="params">       <span class="string">"b"</span> (a3),</span></span></span><br><span class="line"><span class="function"><span class="params">       <span class="string">"D"</span> (a4),</span></span></span><br><span class="line"><span class="function"><span class="params">       <span class="string">"S"</span> (a5)</span></span></span><br><span class="line"><span class="function"><span class="params">     : <span class="string">"cc"</span>, <span class="string">"memory"</span>)</span></span>;</span><br></pre></td></tr></table></figure><h4 id="添加系统调用中断的框架"><a href="#添加系统调用中断的框架" class="headerlink" title="添加系统调用中断的框架"></a>添加系统调用中断的框架</h4><p>和上面的几个中断类似，这里直接给出代码。首先，在<code>trap.h</code>和<code>trap.c</code>中分别加入下面的代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*在 trap.h 中添加服务函数声明*/</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">system_call_handler</span><span class="params">(struct Trapframe *)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*在 trap.c 中添加服务函数定义*/</span></span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * system call (interrupt 48) handler</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> </span><br><span class="line">system_call_handler(struct Trapframe *tf)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后在<code>trapentry.S</code>中添加入口：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">TRAPHANDLER_NOEC(syscall_entry, T_SYSCALL)        # vector48: system call</span><br><span class="line"></span><br><span class="line">.long syscall_entry</span><br></pre></td></tr></table></figure><p>在<code>trap_init()</code>中设置系统调用<code>idt</code></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">SETGATE(idt[T_SYSCALL], <span class="number">0</span>, GD_KT, syscall_entry_, <span class="number">3</span>)  <span class="comment">// 这里因为系统调用号是48，和前面的不相连</span></span><br><span class="line">                                                      <span class="comment">// 能重新定义一个变量保存其服务函数入口</span></span><br></pre></td></tr></table></figure><p>最后在<code>trap_dispatch()</code>中添加系统调用处理分支</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">case</span> T_SYSCALL:</span><br><span class="line">system_call_handler(tf);</span><br><span class="line"><span class="keyword">break</span>;</span><br></pre></td></tr></table></figure><h4 id="编写系统调用服务函数"><a href="#编写系统调用服务函数" class="headerlink" title="编写系统调用服务函数"></a>编写系统调用服务函数</h4><p>系统调用服务函数的主要职责就是获取系统调用参数，并调用<code>syscall</code>函数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span> </span><br><span class="line">system_call_handler(struct Trapframe *tf)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">uint32_t</span> syscallno = tf-&gt;tf_regs.reg_eax;</span><br><span class="line">    <span class="keyword">uint32_t</span> arg1 = tf-&gt;tf_regs.reg_edx;</span><br><span class="line">    <span class="keyword">uint32_t</span> arg2 = tf-&gt;tf_regs.reg_ecx;</span><br><span class="line">    <span class="keyword">uint32_t</span> arg3 = tf-&gt;tf_regs.reg_ebx;</span><br><span class="line">    <span class="keyword">uint32_t</span> arg4 = tf-&gt;tf_regs.reg_edi;</span><br><span class="line">    <span class="keyword">uint32_t</span> arg5 = tf-&gt;tf_regs.reg_esi;</span><br><span class="line">    syscall(syscallno, arg1, arg2, arg3, arg4, arg5);  <span class="comment">// Error! Without a return value</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然而上面这个函数有点问题，我们没有保存系统调用的返回值，根据<code>syscall.c</code>中的内联汇编代码，我们需要将返回值存储至<code>eax</code>寄存器中，所以上面的代码需要稍作修改：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tf-&gt;tf_regs.reg_eax = syscall(syscallno, arg1, arg2, arg3, arg4, arg5);</span><br></pre></td></tr></table></figure><h4 id="编写syscall"><a href="#编写syscall" class="headerlink" title="编写syscall()"></a>编写syscall()</h4><p>在<code>kern/syscall.c</code>中，根据系统调用编号，调用对应的系统调用服务函数。这里主要注意一下输入参数和返回值即可，实现如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Dispatches to the correct kernel function, passing the arguments.</span></span><br><span class="line"><span class="comment">// Returns &gt;= on success, &lt; 0 on error. Errors are:</span></span><br><span class="line"><span class="comment">// -E_INVAL if syscallno is invalid</span></span><br><span class="line"><span class="keyword">int32_t</span></span><br><span class="line">syscall(<span class="keyword">uint32_t</span> syscallno, <span class="keyword">uint32_t</span> a1, <span class="keyword">uint32_t</span> a2, <span class="keyword">uint32_t</span> a3, <span class="keyword">uint32_t</span> a4, <span class="keyword">uint32_t</span> a5)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Call the function corresponding to the 'syscallno' parameter.</span></span><br><span class="line">    <span class="comment">// Return any appropriate return value.</span></span><br><span class="line">    <span class="comment">// LAB 3: Your code here.</span></span><br><span class="line">    <span class="keyword">switch</span> (syscallno) &#123;</span><br><span class="line">    <span class="keyword">case</span> SYS_cgetc:</span><br><span class="line">        <span class="keyword">return</span> sys_cgetc();</span><br><span class="line">    <span class="keyword">case</span> SYS_cputs:</span><br><span class="line">        sys_cputs((<span class="keyword">const</span> <span class="keyword">char</span>*)a1, a2);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">case</span> SYS_getenvid:</span><br><span class="line">        <span class="keyword">return</span> sys_getenvid();</span><br><span class="line">    <span class="keyword">case</span> SYS_env_destroy:</span><br><span class="line">        <span class="keyword">return</span> sys_env_destroy((<span class="keyword">envid_t</span>)a1);</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        <span class="keyword">return</span> -E_INVAL;      </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="改进：使用Python脚本自动生成trapentry-S"><a href="#改进：使用Python脚本自动生成trapentry-S" class="headerlink" title="改进：使用Python脚本自动生成trapentry.S"></a>改进：使用Python脚本自动生成<code>trapentry.S</code></h4><p>自己手写维护<code>trapentry.S</code>中的中断入口不仅耗时耗力，还很容易出错，因此这里用python写一个脚本，自动生成包含256个中断的中断服务函数入口，以及保存这些入口的<code>vectors</code>数组。基本思路是采用循环语句构造中断服务函数入口函数以及入口函数地址数组。然后再对Makefile进行修改。脚本就不在此处列出了。</p><div class="note danger">            <p><strong>Challenge!</strong> Implement system calls using the <code>sysenter</code> and <code>sysexit</code> instructions instead of using <code>int 0x30</code> and <code>iret</code>.</p><p>The <code>sysenter/sysexit</code> instructions were designed by Intel to be faster than <code>int/iret</code>. They do this by using registers instead of the stack and by making assumptions about how the segmentation registers are used. The exact details of these instructions can be found in Volume 2B of the Intel reference manuals.</p><p>The easiest way to add support for these instructions in JOS is to add a <code>sysenter_handler</code> in <code>kern/trapentry.S</code> that saves enough information about the user environment to return to it, sets up the kernel environment, pushes the arguments to <code>syscall()</code> and calls <code>syscall()</code> directly. Once <code>syscall()</code> returns, set everything up for and execute the <code>sysexit</code> instruction. You will also need to add code to <code>kern/init.c</code> to set up the necessary model specific registers (MSRs). Section 6.1.2 in Volume 2 of the AMD Architecture Programmer’s Manual and the reference on SYSENTER in Volume 2B of the Intel reference manuals give good descriptions of the relevant MSRs. You can find an implementation of <code>wrmsr</code> to add to <code>inc/x86.h</code> for writing to these MSRs <a href="http://ftp.kh.edu.tw/Linux/SuSE/people/garloff/linux/k6mod.c">here</a>.</p><p>Finally, <code>lib/syscall.c</code> must be changed to support making a system call with <code>sysenter</code>. Here is a possible register layout for the <code>sysenter</code> instruction:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">eax                - syscall number</span><br><span class="line">edx, ecx, ebx, edi - arg1, arg2, arg3, arg4</span><br><span class="line">esi                - return pc</span><br><span class="line">ebp                - return esp</span><br><span class="line">esp                - trashed by sysenter</span><br></pre></td></tr></table></figure><p>GCC’s inline assembler will automatically save registers that you tell it to load values directly into. Don’t forget to either save (push) and restore (pop) other registers that you clobber, or tell the inline assembler that you’re clobbering them. The inline assembler doesn’t support saving <code>%ebp</code>, so you will need to add code to save and restore it yourself. The return address can be put into <code>%esi</code> by using an instruction like <code>leal after_sysenter_label, %%esi</code>.</p><p>Note that this only supports 4 arguments, so you will need to leave the old method of doing system calls around to support 5 argument system calls. Furthermore, because this fast path doesn’t update the current environment’s trap frame, it won’t be suitable for some of the system calls we add in later labs.</p><p>You may have to revisit your code once we enable asynchronous interrupts in the next lab. Specifically, you’ll need to enable interrupts when returning to the user process, which <code>sysexit</code> doesn’t do for you.</p>          </div><h3 id="用户模式起步"><a href="#用户模式起步" class="headerlink" title="用户模式起步"></a>用户模式起步</h3><p>一个用户程序在<code>lib/entry.S</code>的顶部启动。经过一些配置，这个代码调用位于<code>lib/libmain.c</code>的<code>libmain()</code>。修改<code>libmain()</code>，初始化全局指针<code>thisenv</code>，指向<code>envs[]</code>中当前进程的<code>struct Env</code> 。提示：参考<code>inc/env.h</code>并使用<code>sys_getenvid</code>。</p><p><code>libmain()</code>随后调用<code>umain</code>，这个函数定义在具体的用户程序中。在hello程序中为<code>user/hello.c</code>。注意到打印了”<code>hello, world</code>“后，<code>umain()</code>试图访问<code>thisenv-&gt;env_id</code>。而该指针还未初始化好，所以出现了错误。现在我们初始化了<code>thisenv</code>，因此不会出错。如果依然有错误，那么<code>UENVS</code>这个空间可能被设置为了用户不可读。</p><div class="note default">            <p><strong>练习8</strong>：在用户库中添加所需的代码，支持用户程序运行。你应当看到<code>user/hello</code>打印<code>hello, world</code>以及<code>i am environment 00001000</code>，然后尝试调用<code>sys_env_destroy()</code>退出（参考<code>lib/libmain.c</code>和<code>lib/exit.c</code>）。由于当前内核仅支持一个用户程序，因此内核会报告销毁了唯一的进程，并进入了内核监视器。现在，<code>make grade</code>应当能够通过<code>hello</code>测试。</p>          </div><p>练习8要求我们初始化全局指针<code>thisenv</code>并指向<code>envs</code>中的当前进程，我们需要做两件事情：</p><ul><li>找到当前进程</li><li>将当前进程对应的地址赋值给<code>thisenv</code></li></ul><p>第一个任务是靠<code>sys_getenvid()</code>和<code>ENVX</code>实现的，查阅<code>inc/env.h</code>，我们可以知道如何找到进程id，并根据id找到进程在<code>envs</code>数组中的位置。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// The environment index ENVX(eid) equals the environment's index in the</span></span><br><span class="line"><span class="comment">// 'envs[]' array.  The uniqueifier distinguishes environments that were</span></span><br><span class="line"><span class="comment">// created at different times, but share the same environment index.</span></span><br></pre></td></tr></table></figure><p>解决了第一个问题，第二个就很简单了，我们只需要修改<code>libmain.c</code>，加一句话即可。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">libmain(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// set thisenv to point at our Env structure in envs[].</span></span><br><span class="line">    <span class="comment">// LAB 3: Your code here.</span></span><br><span class="line">    thisenv = &amp;envs[ENVX(sys_getenvid())];</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="页错误与内存保护"><a href="#页错误与内存保护" class="headerlink" title="页错误与内存保护"></a>页错误与内存保护</h3><p>操作系统依赖于硬件实施内存保护。内核会通知硬件，哪些虚拟内存是有效的，哪些不是。当一个程序尝试访问无效内存或者无权限内存，处理器会在触发错误的指令处停止该语句，并带着相关信息进入内核。如果错误是可修复的，内核修复错误并继续执行程序；否则程序无法继续执行。</p><p>As an example of a fixable fault, consider an automatically extended stack. In many systems the kernel initially allocates a single stack page, and then if a program faults accessing pages further down the stack, the kernel will allocate those pages automatically and let the program continue. By doing this, the kernel only allocates as much stack memory as the program needs, but the program can work under the illusion that it has an arbitrarily large stack.</p><p>System calls present an interesting problem for memory protection. Most system call interfaces let user programs pass pointers to the kernel. These pointers point at user buffers to be read or written. The kernel then dereferences these pointers while carrying out the system call. There are two problems with this:</p><ol><li>内核页错误更严重. If the kernel page-faults while manipulating its own data structures, that’s a kernel bug, and the fault handler should panic the kernel (and hence the whole system). But when the kernel is dereferencing pointers given to it by the user program, it needs a way to remember that any page faults these dereferences cause are actually on behalf of the user program.</li><li>The kernel typically has more memory permissions than the user program. The user program might pass a pointer to a system call that points to memory that the kernel can read or write but that the program cannot. The kernel must be careful not to be tricked into dereferencing such a pointer, since that might reveal private information or destroy the integrity of the kernel.</li></ol><p>For both of these reasons the kernel must be extremely careful when handling pointers presented by user programs.</p><p>You will now solve these two problems with a single mechanism that scrutinizes all pointers passed from userspace into the kernel. When a program passes the kernel a pointer, the kernel will check that the address is in the user part of the address space, and that the page table would allow the memory operation.</p><p>Thus, the kernel will never suffer a page fault due to dereferencing a user-supplied pointer. If the kernel does page fault, it should panic and terminate.</p><div class="note default">            <p><strong>Exercise 9.</strong> </p><ul><li><p>Change <code>kern/trap.c</code> to panic if a page fault happens in kernel mode. Hint: to determine whether a fault happened in user mode or in kernel mode, check the low bits of the <code>tf_cs</code>.</p></li><li><p>Read <code>user_mem_assert</code> in <code>kern/pmap.c</code> and implement <code>user_mem_check</code> in that same file.</p></li><li><p>Change <code>kern/syscall.c</code> to sanity (理智) check arguments to system calls.</p></li><li><p>Boot your kernel, running <code>user/buggyhello</code>. The environment should be destroyed, and the kernel should <em>not</em> panic. You should see:</p></li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">[00001000] user_mem_check assertion failure for va 00000001</span><br><span class="line">[00001000] free env 00001000</span><br><span class="line">Destroyed the only environment - nothing more to do!</span><br></pre></td></tr></table></figure><ul><li>Finally, change <code>debuginfo_eip</code> in <code>kern/kdebug.c</code> to call <code>user_mem_check</code> on <code>usd</code>, <code>stabs</code>, and <code>stabstr</code>. If you now run <code>user/breakpoint</code>, you should be able to run backtrace from the kernel monitor and see the backtrace traverse into <code>lib/libmain.c</code> before the kernel panics with a page fault. What causes this page fault? You don’t need to fix it, but you should understand why it happens.</li></ul>          </div><p>练习9一共给了5个任务，我们依次完成：</p><h4 id="修改-kern-trap-c，当内核发生页错误时，panic"><a href="#修改-kern-trap-c，当内核发生页错误时，panic" class="headerlink" title="修改 kern/trap.c，当内核发生页错误时，panic"></a>修改 <code>kern/trap.c</code>，当内核发生页错误时，panic</h4><p>这里提示我们使用 <code>tf_cs</code>的低位对内核和用户模式进行判断，如果页错误在内核，就将内核中止。这段代码比较简单，在<code>page_fault_handler</code>中添加下面的代码即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">page_fault_handler(struct Trapframe *tf)&#123;</span><br><span class="line">    <span class="comment">// LAB 3: Your code here</span></span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span>(tf-&gt;tf_cs == GD_KT)&#123;</span><br><span class="line">        panic(<span class="string">"kernel mode page faults"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="实现user-mem-check"><a href="#实现user-mem-check" class="headerlink" title="实现user_mem_check"></a>实现<code>user_mem_check</code></h4><h5 id="函数功能-6"><a href="#函数功能-6" class="headerlink" title="函数功能"></a>函数功能</h5><p>Check that an environment is allowed to access the range of memory <code>[va, va+len)</code> with permissions <code>perm | PTE_P</code>. Normally <code>perm</code> will contain PTE_U at least, but this is not required. <code>va</code> and <code>len</code> need not be page-aligned; you must test every page that contains any of that range.  You will test either <code>len/PGSIZE</code>, <code>len/PGSIZE + 1</code>, or <code>len/PGSIZE + 2</code> pages.</p><h5 id="函数原型-3"><a href="#函数原型-3" class="headerlink" title="函数原型"></a>函数原型</h5><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span></span><br><span class="line">user_mem_check(struct Env *env, <span class="keyword">const</span> <span class="keyword">void</span> *va, <span class="keyword">size_t</span> len, <span class="keyword">int</span> perm)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// LAB 3: Your code here.</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="函数实现-1"><a href="#函数实现-1" class="headerlink" title="函数实现"></a>函数实现</h5><p>为了对<code>[va, va+len)</code>进行检查，我们需要完成以下工作：</p><ul><li>进行内存对齐，找到包含<code>[va, va+len)</code>的最小对齐内存</li><li>依次遍历，判断内存是否合法，如果不合法，记录非法内存所在地址<ul><li>内存是否在用户区域内？(<code>start &lt; ULIM</code>)</li><li>pte是否非空？</li><li>pte是否符合perm的要求？(<code>pte &amp; perm == perm</code>)</li></ul></li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span></span><br><span class="line">user_mem_check(struct Env *env, <span class="keyword">const</span> <span class="keyword">void</span> *va, <span class="keyword">size_t</span> len, <span class="keyword">int</span> perm)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 'va' and 'len' need not be page-aligned; </span></span><br><span class="line">    <span class="comment">// To make sure that all the memory can be covered, we need to align the </span></span><br><span class="line">    <span class="comment">// address. [start, [va, va + len], end];</span></span><br><span class="line">    <span class="keyword">void</span>* start = (<span class="keyword">void</span>*) ROUNDDOWN((<span class="keyword">uint32_t</span>)va, PGSIZE);</span><br><span class="line">    <span class="keyword">void</span>* end = (<span class="keyword">void</span>*) ROUNDUP((<span class="keyword">uint32_t</span>)(va+len), PGSIZE);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// You must test every page that contains any of that range.  </span></span><br><span class="line">    <span class="comment">// You will test either 'len/PGSIZE', 'len/PGSIZE + 1', or 'len/PGSIZE + 2' pages.</span></span><br><span class="line">    <span class="keyword">for</span>(; start &lt; end; start += PGSIZE)&#123;</span><br><span class="line">        <span class="keyword">pte_t</span> *pte = pgdir_walk(env-&gt;env_pgdir, (<span class="keyword">void</span> *)start, <span class="number">0</span>);</span><br><span class="line">        <span class="comment">// A user program can access a virtual address if (1) the address is below</span></span><br><span class="line">        <span class="comment">// ULIM, and (2) the page table gives it permission.  These are exactly</span></span><br><span class="line">        <span class="comment">// the tests you should implement here.</span></span><br><span class="line">        <span class="keyword">if</span>((<span class="keyword">uintptr_t</span>)start &gt;= ULIM || !pte || !(*pte &amp; PTE_P) || ((*pte &amp; perm) != perm))&#123;</span><br><span class="line">            <span class="comment">// If there is an error, set the 'user_mem_check_addr' variable to the first</span></span><br><span class="line">            <span class="comment">// erroneous virtual address.</span></span><br><span class="line">            user_mem_check_addr = (<span class="keyword">uintptr_t</span>)(start &lt; va ? va : start);</span><br><span class="line">            <span class="keyword">return</span> -E_FAULT;  <span class="comment">// Returns -E_FAULT if memory is invalid</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Returns 0 if the user program can access this range of addresses.</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="修改-kern-syscall-c，检查syscall的调用参数"><a href="#修改-kern-syscall-c，检查syscall的调用参数" class="headerlink" title="修改 kern/syscall.c，检查syscall的调用参数"></a>修改 <code>kern/syscall.c</code>，检查<code>syscall</code>的调用参数</h4><p>由于<code>sys_cputs</code>系统调用要对内存进行写操作，因此我们要检查内存是否有效，在<code>/kern/syscall.c</code>的<code>sys_cputs</code>函数中加入进行检查即可。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">user_mem_assert(curenv, (<span class="keyword">void</span>*)s, len, PTE_P|PTE_U);</span><br></pre></td></tr></table></figure><h4 id="运行并查看结果"><a href="#运行并查看结果" class="headerlink" title="运行并查看结果"></a>运行并查看结果</h4><p>现在你的程序应当能够通过所有的测试，并获得相应的分数。至此，Lab3已经全部完成。</p><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/20210609221753.png" width = "550" alt="图片名称" align=center /></p><h2 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h2><h3 id="其他几个重要但是不需要我们完成的函数"><a href="#其他几个重要但是不需要我们完成的函数" class="headerlink" title="其他几个重要但是不需要我们完成的函数"></a>其他几个重要但是不需要我们完成的函数</h3><h4 id="env-pop-tf-struct-Trapframe-tf"><a href="#env-pop-tf-struct-Trapframe-tf" class="headerlink" title="env_pop_tf(struct Trapframe *tf)"></a>env_pop_tf(struct Trapframe *tf)</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Restores the register values in the Trapframe with the 'iret' instruction.</span></span><br><span class="line"><span class="comment">// This exits the kernel and starts executing some environment's code.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// This function does not return.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">env_pop_tf(struct Trapframe *tf)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params"><span class="string">"\tmovl %0,%%esp\n"</span><span class="comment">// 将%esp指向tf地址处</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="string">"\tpopal\n"</span><span class="comment">// 弹出Trapframe结构中的tf_regs值到通用寄存器</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="string">"\tpopl %%es\n"</span><span class="comment">// 弹出Trapframe结构中的tf_es值到%es寄存器</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="string">"\tpopl %%ds\n"</span><span class="comment">// 弹出Trapframe结构中的tf_ds值到%ds寄存器</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="string">"\taddl $0x8,%%esp\n"</span> <span class="comment">/* skip tf_trapno and tf_errcode */</span></span></span></span><br><span class="line"><span class="function"><span class="params"><span class="string">"\tiret\n"</span><span class="comment">// 中断返回指令，具体动作如下：从Trapframe结构中依次弹出tf_eip,tf_cs,tf_eflags,tf_esp,tf_ss到相应寄存器</span></span></span></span><br><span class="line"><span class="function"><span class="params">: </span></span></span><br><span class="line"><span class="function"><span class="params">        : <span class="string">"g"</span> (tf)                      <span class="comment">// 输入操作数</span></span></span></span><br><span class="line"><span class="function"><span class="params">        : <span class="string">"memory"</span>)</span></span>;</span><br><span class="line">    panic(<span class="string">"iret failed"</span>);  <span class="comment">/* mostly to placate the compiler */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个函数将一个陷帧弹出，将其中的值恢复给寄存器，即恢复到<code>tf</code>描述的状态。</p><h4 id="SETGATE"><a href="#SETGATE" class="headerlink" title="SETGATE"></a><code>SETGATE</code></h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Set up a normal interrupt/trap gate descriptor.</span></span><br><span class="line"><span class="comment">// - istrap: 1 for a trap (= exception) gate, 0 for an interrupt gate.</span></span><br><span class="line">    <span class="comment">//   see section 9.6.1.3 of the i386 reference: "The difference between</span></span><br><span class="line">    <span class="comment">//   an interrupt gate and a trap gate is in the effect on IF (the</span></span><br><span class="line">    <span class="comment">//   interrupt-enable flag). An interrupt that vectors through an</span></span><br><span class="line">    <span class="comment">//   interrupt gate resets IF, thereby preventing other interrupts from</span></span><br><span class="line">    <span class="comment">//   interfering with the current interrupt handler. A subsequent IRET</span></span><br><span class="line">    <span class="comment">//   instruction restores IF to the value in the EFLAGS image on the</span></span><br><span class="line">    <span class="comment">//   stack. An interrupt through a trap gate does not change IF."</span></span><br><span class="line">    <span class="comment">//   为了正确设置gate，我们需要知道哪些是trap，哪些是interrupt，参考</span></span><br><span class="line"><span class="comment">// - sel: Code segment selector for interrupt/trap handler</span></span><br><span class="line"><span class="comment">// - off: Offset in code segment for interrupt/trap handler</span></span><br><span class="line"><span class="comment">// - dpl: Descriptor Privilege Level -</span></span><br><span class="line"><span class="comment">//  the privilege level required for software to invoke</span></span><br><span class="line"><span class="comment">//  this interrupt/trap gate explicitly using an int instruction.</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SETGATE(gate, istrap, sel, off, dpl)\</span></span><br><span class="line">&#123;\</span><br><span class="line">    (gate).gd_off_15_0 = (<span class="keyword">uint32_t</span>) (off) &amp; <span class="number">0xffff</span>;\</span><br><span class="line">    (gate).gd_sel = (sel);\</span><br><span class="line">    (gate).gd_args = <span class="number">0</span>;\</span><br><span class="line">    (gate).gd_rsv1 = <span class="number">0</span>;\</span><br><span class="line">    (gate).gd_type = (istrap) ? STS_TG32 : STS_IG32;\</span><br><span class="line">    (gate).gd_s = <span class="number">0</span>;\</span><br><span class="line">    (gate).gd_dpl = (dpl);\</span><br><span class="line">    (gate).gd_p = <span class="number">1</span>;\</span><br><span class="line">    (gate).gd_off_31_16 = (<span class="keyword">uint32_t</span>) (off) &gt;&gt; <span class="number">16</span>;\</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个函数需要注意的是其中的<code>sel</code>段，这个段设置了中断/陷入handler的代码段，同时，<code>off</code>设置了代码段偏移量</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev10.pdf">xv6 bookc</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/labs/lab3/">Lab3</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li><li id="fn:3"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">3.</span><span style="display: inline-block; vertical-align: top;"><a href="https://www.cnblogs.com/gatsby123/p/9838304.html">Lab3实验答案</a></span><a href="#fnref:3" rev="footnote"> ↩</a></li><li id="fn:4"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">4.</span><span style="display: inline-block; vertical-align: top;"><a href="https://yangminz.github.io/images/OperatingSys/Lab3Report.html">Yangminz Blog</a></span><a href="#fnref:4" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;且将杯酒对星河&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
      <category term="课程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>MIT6828-HW5-xv6 CPU alarm</title>
    <link href="http://yoursite.com/2021/04/02/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/MIT6828-HW5/"/>
    <id>http://yoursite.com/2021/04/02/计算机/操作系统/课程/MIT6828-HW5/</id>
    <published>2021-04-02T13:08:19.920Z</published>
    <updated>2021-04-21T03:38:20.086Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>本作业将实现对使用CPU的进程进行周期性提醒，通过该练习，我们可以实现一个基础的用户层面的中断/错误处理机制。</p><a id="more"></a><h2 id="目标"><a href="#目标" class="headerlink" title="目标"></a>目标</h2><p>添加一个新的<code>alarm(interval, handler)</code>系统调用。如果某个程序调用了<code>alarm(n, fn)</code>，那么经过$n$个CPU时钟周期后，内核调用函数<code>fn</code>，当<code>fn</code>返回后，应用会从其被中断的地方继续运行。创建一个<code>alarmtest.c</code>程序：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"types.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"stat.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"user.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">periodic</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">main(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">int</span> i;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="number">1</span>, <span class="string">"alarmtest starting\n"</span>);</span><br><span class="line">  alarm(<span class="number">10</span>, periodic);</span><br><span class="line">  <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; <span class="number">25</span>*<span class="number">500000</span>; i++)&#123;</span><br><span class="line">    <span class="keyword">if</span>((i % <span class="number">250000</span>) == <span class="number">0</span>)</span><br><span class="line">      write(<span class="number">2</span>, <span class="string">"."</span>, <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">exit</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">periodic()</span><br><span class="line">&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="number">1</span>, <span class="string">"alarm!\n"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个程序调用了<code>alarm(10, periodic);</code>系统调用，每10个ticks调用一次<code>periodic</code>函数，这个函数的输出如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">$ alarmtest</span><br><span class="line">alarmtest starting</span><br><span class="line">.....alarm!</span><br><span class="line">....alarm!</span><br><span class="line">.....alarm!</span><br><span class="line">......alarm!</span><br><span class="line">.....alarm!</span><br><span class="line">....alarm!</span><br><span class="line">....alarm!</span><br><span class="line">......alarm!</span><br><span class="line">.....alarm!</span><br><span class="line">...alarm!</span><br><span class="line">...$</span><br></pre></td></tr></table></figure><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><p>本文需要对CPU的执行时间进行精确跟踪，因此这里只考虑单个CPU的情况，因此调试时执行下面的语句：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">make CPUS=<span class="number">1</span> qemu  # 设置CPU数为<span class="number">1</span></span><br></pre></td></tr></table></figure><p>这样内核会以单CPU启动</p><h2 id="实现步骤"><a href="#实现步骤" class="headerlink" title="实现步骤"></a>实现步骤</h2><h3 id="框架搭建"><a href="#框架搭建" class="headerlink" title="框架搭建"></a>框架搭建</h3><h4 id="将alarmtest-c添加为用户程序"><a href="#将alarmtest-c添加为用户程序" class="headerlink" title="将alarmtest.c添加为用户程序"></a>将alarmtest.c添加为用户程序</h4><p>在<code>Makefile</code>中的<code>UPROGS</code>字段添加<code>_alarmtest</code></p><h4 id="系统调用框架编写"><a href="#系统调用框架编写" class="headerlink" title="系统调用框架编写"></a>系统调用框架编写</h4><p>在<code>user.h</code>中添加系统调用声明：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">alarm</span><span class="params">(<span class="keyword">int</span> ticks, <span class="keyword">void</span> (*handler)())</span></span>;</span><br></pre></td></tr></table></figure><p>更新<code>syscall.h</code>和<code>usys.S</code>，添加对<code>alarm</code>的支持字段。</p><h4 id="更改进程描述符"><a href="#更改进程描述符" class="headerlink" title="更改进程描述符"></a>更改进程描述符</h4><p>在<code>proc.h</code>中更改进程描述符，添加用于保存<code>alarm</code>的<code>interval</code>和<code>handler</code>字段</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> alarmticks;          <span class="comment">// 定时中断时长</span></span><br><span class="line"><span class="keyword">int</span> passedticks;         <span class="comment">// 自alarmhandler上次调用后经过的时间</span></span><br><span class="line"><span class="keyword">void</span> (*alarmhandler)();  <span class="comment">// 中断服务函数</span></span><br></pre></td></tr></table></figure><h4 id="编写系统调用"><a href="#编写系统调用" class="headerlink" title="编写系统调用"></a>编写系统调用</h4><p>系统调用作业中给我们提供了，代码如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span></span><br><span class="line">sys_alarm(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> ticks;</span><br><span class="line">    <span class="keyword">void</span> (*handler)();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(argint(<span class="number">0</span>, &amp;ticks) &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">if</span>(argptr(<span class="number">1</span>, (<span class="keyword">char</span>**)&amp;handler, <span class="number">1</span>) &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    myproc()-&gt;alarmticks = ticks;</span><br><span class="line">    myproc()-&gt;alarmhandler = handler;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>功能很简单，就是设置定时中断时长和中断服务函数。至此我们已经搭建好了框架，现在我们要修改<code>trap</code>，实现定时中断。我们先编译程序，现在程序能够使用<code>sys_alarm</code>系统调用，但是这个调用目前只是修改了进程结构体，没有实际地触发中断。执行<code>make qemu-gdb</code>打开调试，在<code>sys_alarm</code>处设置断点，然后继续运行至系统调用。系统调用是根据系统调用号触发的，这个序号保存在<code>eax</code>中</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">print myproc()-&gt;tf-&gt;eax</span><br><span class="line">23   # 系统调用入口为23</span><br></pre></td></tr></table></figure><p>在<code>gdb</code>中执行如下命令，可以查看进程的栈</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">x/4x myproc()-&gt;tf-&gt;esp</span><br><span class="line">0x2fac: 0x00000034  0x0000000a  0x00000090 0x00000000</span><br></pre></td></tr></table></figure><p>用户栈地址为<code>0x2fac</code>，其中保存了系统调用的参数<code>ticks</code>和<code>handler</code>，分别为<code>10</code>和<code>0x00000090</code>。</p><h3 id="需要解决的问题："><a href="#需要解决的问题：" class="headerlink" title="需要解决的问题："></a>需要解决的问题：</h3><p>为了实现这个系统调用，我们还需要考虑如下问题：</p><ul><li>由系统调用触发中断后，在哪里执行中断服务函数，在<code>trap()</code>中不太好，那么是不是得换个地方</li><li>如何执行中断服务函数？</li><li>中断后如何保护现场，如何恢复现场？</li></ul><h3 id="完整实现"><a href="#完整实现" class="headerlink" title="完整实现"></a>完整实现</h3><h4 id="中断是如何进入的"><a href="#中断是如何进入的" class="headerlink" title="中断是如何进入的"></a>中断是如何进入的</h4><p>中断是从<code>vector.S</code>进入的，启动内核后，我们可以在vector32处设置断点</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">break vector32</span><br><span class="line"></span><br><span class="line">.globl vector32</span><br><span class="line">vector32:</span><br><span class="line">  pushl $0</span><br><span class="line">  pushl $32</span><br><span class="line">  jmp alltraps</span><br></pre></td></tr></table></figure><p>32即对应定时器，在跳转到<code>alltraps</code>后，内核会进入<code>trapasm.S</code>，最终跳转至<code>trap</code>函数</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">  #include &quot;mmu.h&quot;</span><br><span class="line"></span><br><span class="line">  # vectors.S sends all traps here.</span><br><span class="line">.globl alltraps</span><br><span class="line">alltraps:</span><br><span class="line">  # Build trap frame.  将现场保存至内核堆栈中</span><br><span class="line">  pushl %ds</span><br><span class="line">  pushl %es</span><br><span class="line">  pushl %fs</span><br><span class="line">  pushl %gs</span><br><span class="line">  pushal</span><br><span class="line">  </span><br><span class="line">  # Set up data and per-cpu segments.</span><br><span class="line">  movw $(SEG_KDATA&lt;&lt;3), %ax</span><br><span class="line">  movw %ax, %ds</span><br><span class="line">  movw %ax, %es</span><br><span class="line">  movw $(SEG_KCPU&lt;&lt;3), %ax</span><br><span class="line">  movw %ax, %fs</span><br><span class="line">  movw %ax, %gs</span><br><span class="line"></span><br><span class="line">  # Call trap(tf), where tf=%esp</span><br><span class="line">  pushl %esp</span><br><span class="line">  call trap     # 进入trap中</span><br><span class="line">  addl $4, %esp</span><br></pre></td></tr></table></figure><h4 id="tick中断处理"><a href="#tick中断处理" class="headerlink" title="tick中断处理"></a>tick中断处理</h4><p>在每一次时钟中断后，应当更新进程计时器，判断是否达到了计时数，如果达到了，执行中断服务函数，并将计时器置0。在进程描述符中，我们使用<code>passedticks</code>记录已经经过的时间，所以我们在定时中断中的框架如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">case</span> T_IRQ0 + IRQ_TIMER:</span><br><span class="line">    <span class="keyword">if</span>(cpu-&gt;id == <span class="number">0</span>)&#123;</span><br><span class="line">      acquire(&amp;tickslock);</span><br><span class="line">      ticks++;</span><br><span class="line">      </span><br><span class="line">      <span class="comment">// You code here</span></span><br><span class="line">      <span class="comment">/**********************************************************/</span></span><br><span class="line">      <span class="keyword">if</span>(myproc() != <span class="number">0</span> &amp;&amp; (tf-&gt;cs &amp; <span class="number">3</span>) == <span class="number">3</span>) &#123;   <span class="comment">// 进程有效且来自用户空间</span></span><br><span class="line">          myproc() -&gt; passedticks++;</span><br><span class="line">          <span class="keyword">if</span>(myproc() -&gt; passedticks &gt;= myproc() -&gt; alarmticks)&#123; <span class="comment">//超时</span></span><br><span class="line">              <span class="comment">//执行handler函数</span></span><br><span class="line">              tf-&gt;eip = myproc()-&gt;alarmhandler();  <span class="comment">//这样对吗</span></span><br><span class="line">              <span class="comment">//更新计时器</span></span><br><span class="line">              myproc() -&gt; passedticks = <span class="number">0</span>;</span><br><span class="line">          &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">/**********************************************************/</span></span><br><span class="line">        </span><br><span class="line">      wakeup(&amp;ticks);</span><br><span class="line">      release(&amp;tickslock);</span><br><span class="line">    &#125;</span><br><span class="line">    lapiceoi();</span><br><span class="line">    <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev10.pdf">xv6 book</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-alarm.html">In-class: xv6 CPU alarm (mit.edu)</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本作业将实现对使用CPU的进程进行周期性提醒，通过该练习，我们可以实现一个基础的用户层面的中断/错误处理机制。&lt;/p&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
      <category term="课程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/"/>
    
    
  </entry>
  
  <entry>
    <title>地址</title>
    <link href="http://yoursite.com/2021/03/30/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E5%9C%B0%E5%9D%80/"/>
    <id>http://yoursite.com/2021/03/30/计算机/操作系统/地址/</id>
    <published>2021-03-30T03:12:47.229Z</published>
    <updated>2021-04-04T10:12:21.314Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><blockquote><p>我迷失在田野里，渐渐忘记了回家的路</p></blockquote><a id="more"></a><h2 id="几种不同的地址"><a href="#几种不同的地址" class="headerlink" title="几种不同的地址"></a>几种不同的地址</h2><div class="table-container"><table><thead><tr><th>名称</th><th>概念</th></tr></thead><tbody><tr><td>逻辑地址</td><td>程序产生的段内偏移，由段选择符和段内偏移组成的地址。由操作系统决定，和物理地址一一对应的地址，C语言中的指针即为逻辑地址<br />逻辑地址就是段+页偏移</td></tr><tr><td>线性地址</td><td>段基址+段内偏移量构成的地址，如果未开启分页，那么线性地址就是物理地址，否则还需二次转换。<br />线性地址就是段偏移，但是还未进行页转换的地址<br />虚拟地址就是线性地址（linux内核明确表示）</td></tr><tr><td>物理地址</td><td>真实物理内存中的地址，即内存地址寄存器中的地址物理地址</td></tr></tbody></table></div><h3 id="物理地址"><a href="#物理地址" class="headerlink" title="物理地址"></a>物理地址</h3><p>物理地址就是硬件支持的地址空间，起始地址为0，一直到$\textrm{MAX}_{\textrm{sys}}$，如下所示：</p><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/physicalmemory.svg" width = "150" alt="图片名称" align=center /></p><p>每个物理内存单元的物理地址编号是唯一的，给写程序造成了不便。</p><h3 id="逻辑地址"><a href="#逻辑地址" class="headerlink" title="逻辑地址"></a>逻辑地址</h3><p>CPU运行进程时看到的地址，起始地址为0，一直到$\textrm{MAX}_{\textrm{proc}}$</p><h4 id="逻辑地址的作用"><a href="#逻辑地址的作用" class="headerlink" title="逻辑地址的作用"></a>逻辑地址的作用</h4><ul><li>避免直接将物理地址暴露，从而导致访问任意内存程序崩溃</li><li>可以用相邻的虚拟地址映射到不相邻的物理地址</li><li>可以使用一系列的虚拟地址访问大于可用物理内存的内存缓冲区（虚拟内存）</li><li>不同进程间使用虚拟地址相互隔离</li></ul><p>例如，如果同一时刻由两个进程P1和P2，那么两者的逻辑地址范围是多少？假设逻辑地址最大为65535。实际上，P1和P2可以有一样范围的地址，都为0-65535，操作系统会确保其逻辑地址指向不同的物理地址。</p><p>从这个问题中我们也可以看出，操作系统帮我们屏蔽了底层的物理实现。</p><h4 id="逻辑地址的生成过程"><a href="#逻辑地址的生成过程" class="headerlink" title="逻辑地址的生成过程"></a>逻辑地址的生成过程</h4><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/programloading.svg" width = "550" alt="图片名称" align=center /></p><h4 id="生成时机和限制"><a href="#生成时机和限制" class="headerlink" title="生成时机和限制"></a>生成时机和限制</h4><ul><li>编译时（最不灵活，写死的，但是比较简单）<ul><li>假设起始地址已知</li><li>如果起始地址改变，必须重新编译</li></ul></li><li>加载时（加载程序可以选择加载位置，但是加载好了执行过程中不能变）<ul><li>如编译时起始位置未知，编译器需要生成可重定位的代码</li><li>加载时生成绝对地址，在可执行文件中有一个重定位表，根据表修改绝对地址</li></ul></li><li>执行时（执行过程中可以移动代码，最灵活，需要虚拟地址支持）<ul><li>执行时代码可移动（前两种情况不能随意改动）</li><li>需要地址转换（映射）硬件支持</li></ul></li></ul><h2 id="地址转换"><a href="#地址转换" class="headerlink" title="地址转换"></a>地址转换</h2><h3 id="虚拟地址（线性地址）到物理地址转换步骤"><a href="#虚拟地址（线性地址）到物理地址转换步骤" class="headerlink" title="虚拟地址（线性地址）到物理地址转换步骤"></a>虚拟地址（线性地址）到物理地址转换步骤</h3><p>这里讨论一个二级页表情况下VA到PA的转换过程，已知一个虚拟地址为<strong>0x01EB6338</strong>，CR3寄存器的值为<strong>0xAC0F1000</strong>，求物理地址。</p><h4 id="第一步：写出二进制地址并拆分"><a href="#第一步：写出二进制地址并拆分" class="headerlink" title="第一步：写出二进制地址并拆分"></a>第一步：写出二进制地址并拆分</h4><p>虚拟地址的二进制为：$0000,0001 ,1110 ,1011 ,0110 ,0011 ,0011 ,1000$</p><p>按照线性地址2级分页进行划分，地址可以被拆分为：</p><ul><li>Dir：0000,0001,11</li><li>Table：10 ,1011 ,0110</li><li>Offset：0011 ,0011 ,1000</li></ul><p>换算成十六进制可得到如下结果：</p><ul><li>页目录索引：7</li><li>页表索引：0x2B6</li><li>偏移量：0x338</li></ul><h4 id="第二步：根据CR3寄存器的物理地址确定页目录表的基地址"><a href="#第二步：根据CR3寄存器的物理地址确定页目录表的基地址" class="headerlink" title="第二步：根据CR3寄存器的物理地址确定页目录表的基地址"></a>第二步：根据CR3寄存器的物理地址确定页目录表的基地址</h4><p>CR3中存放的物理地址就是页目录表的基地址</p><h4 id="第三步：计算页表项的地址"><a href="#第三步：计算页表项的地址" class="headerlink" title="第三步：计算页表项的地址"></a>第三步：计算页表项的地址</h4><p>页表地址存放在Page Directory Entry的第7项中，即</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">　[<span class="number">0xAC0F1000</span> + <span class="number">4</span> * <span class="number">7</span>] = [<span class="number">0xAC0F101C</span>] = <span class="number">0xAC0F101C</span>: pte</span><br></pre></td></tr></table></figure><p>0xAC0F101C这个地址保存了pte的值，假设这个值为<strong>0x2F103841</strong>，那么其中PTE为<strong>0x2F103000</strong>，而页表属性为0x381。</p><h4 id="第四步：计算页面的物理地址"><a href="#第四步：计算页面的物理地址" class="headerlink" title="第四步：计算页面的物理地址"></a>第四步：计算页面的物理地址</h4><p>页表的地址为<strong>0x2F103000</strong>，存放在第0x2B6项，所以虚拟地址所在页的物理地址为：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[0x2F103000 + 0x2B6 * 4] = [0x2F103AD8] = 0x2F103AD8: pa</span><br></pre></td></tr></table></figure><p>这里得到的pa是未经偏移的物理地址，假设为<strong>0x7095e847</strong>，那么页面物理地址为<strong>0x7095E000</strong>，属性为0x847</p><h4 id="最后一步：计算偏移量"><a href="#最后一步：计算偏移量" class="headerlink" title="最后一步：计算偏移量"></a>最后一步：计算偏移量</h4><p>最终物理地址为0x7095E000+0x338 = 7095E338</p><h3 id="地址生成过程的硬件解释"><a href="#地址生成过程的硬件解释" class="headerlink" title="地址生成过程的硬件解释"></a>地址生成过程的硬件解释</h3><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/CPUandMem.svg" width = "480" alt="图片名称" align=center /></p><p>CPU与内存连接的结构图如上图所示，我们从一条指令执行过程出发，分析虚拟地址到物理地址的转换过程。</p><ul><li>CPU当前正在执行一条指令<ul><li>ALU：需要逻辑地址指向的内存内容</li><li>MMU：根据页表进行逻辑到物理的转换</li><li>CPU控制器：给总线发送物理地址请求</li></ul></li><li>内存<ul><li>根据控制信号是读还是写，向总线发送或接收内容</li></ul></li><li><strong>操作系统</strong><ul><li>建立逻辑地址LA到物理地址PA的映射，地址转换是MMU硬件完成的，而操作系统只负责提供映射表</li></ul></li></ul><h2 id="寻址方式"><a href="#寻址方式" class="headerlink" title="寻址方式"></a>寻址方式</h2><p>现代CPU采用虚拟寻址的方式，需要将虚拟地址翻译成物理地址，这样才能访问真实的物理内存。完成虚拟地址转换为物理地址的的硬件是CPU中的MMU。转换过程为：</p><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/addresstrans.svg" width = "550" alt="图片名称" align=center /></p><p>其中，C语言中的指针就是虚拟地址中的Offset部分，如果关闭了段选，那么线性地址就是虚拟地址。更具体的带有多级页表的转换方式如下：</p><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/20210223224451.png" width = "650" alt="图片名称" align=center /></p><h3 id="地址检查"><a href="#地址检查" class="headerlink" title="地址检查"></a>地址检查</h3><p>在地址转换过程中，操作系统会对地址进行检查。例如在段选过程中，操作系统会判断逻辑地址是否大于段长度寄存器，如果是，那么会触发内存异常。如果不是，那么地址会和段基址寄存器相加，生成线性地址。操作系统负责设置段长度和段基址。</p><h2 id="Page-Table-Entry"><a href="#Page-Table-Entry" class="headerlink" title="Page Table Entry"></a>Page Table Entry</h2><h3 id="PDE与PTE"><a href="#PDE与PTE" class="headerlink" title="PDE与PTE"></a>PDE与PTE</h3><p>PDE与PTE的大小都为32位，具体格式如下：</p><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/20210302113437.png" width = "750" alt="图片名称" align=center /></p><h4 id="PDE（页目录表）"><a href="#PDE（页目录表）" class="headerlink" title="PDE（页目录表）"></a>PDE（页目录表）</h4><p>MMU通过<code>%cr3</code>寄存器获得PD位置，即完成不同进程之间PD的切换</p><h4 id="PTE（页表）"><a href="#PTE（页表）" class="headerlink" title="PTE（页表）"></a>PTE（页表）</h4><p>页表的格式如下：前20位存储物理地址的基地址（因为物理地址是按页分配的，一个页大小为4096，正好是$2^{12}$，因此页的起始地址都是4096的倍数，所以低12位为0，只需存储高20位即可），后12位是对于指向的内存的描述。MMU将会把这20位的虚拟地址替换掉。PTE由MMU加载并存储在内存中。</p><h4 id="标志位解析"><a href="#标志位解析" class="headerlink" title="标志位解析"></a>标志位解析</h4><div class="table-container"><table><thead><tr><th>标志位</th><th>全称</th><th>功能</th></tr></thead><tbody><tr><td>P</td><td>Present</td><td>对于大多数操作系统，如果present为0，代表PDE或PTE数据无效</td></tr><tr><td>W</td><td>Writable</td><td>内存是否可写</td></tr><tr><td>U(PTE_U)</td><td>User</td><td>用户是否可访问</td></tr></tbody></table></div><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;我迷失在田野里，渐渐忘记了回家的路&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
    
  </entry>
  
  <entry>
    <title>极大似然估计</title>
    <link href="http://yoursite.com/2021/03/27/%E6%95%B0%E5%AD%A6/%E6%A6%82%E7%8E%87%E8%AE%BA/%E6%9E%81%E5%A4%A7%E4%BC%BC%E7%84%B6%E4%BC%B0%E8%AE%A1/"/>
    <id>http://yoursite.com/2021/03/27/数学/概率论/极大似然估计/</id>
    <published>2021-03-27T07:05:34.934Z</published>
    <updated>2021-03-27T07:51:50.495Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>根据已知的样本结果信息，反推最有可能导致该结果的模型参数。</p><a id="more"></a><h2 id="什么是极大似然估计"><a href="#什么是极大似然估计" class="headerlink" title="什么是极大似然估计"></a>什么是极大似然估计</h2><p>一句话，一个参数估计方法，就是确定了模型（比如正态分布），但是参数未知（正态分布的均值和方差未知），有了数据，我需要计算均值$\mu$和方差$\theta$。为了求极大似然估计，我们先要了解似然函数</p><h3 id="似然（likelihood）函数"><a href="#似然（likelihood）函数" class="headerlink" title="似然（likelihood）函数"></a>似然（likelihood）函数</h3><p>考虑函数$P(x|\theta)$，这个函数有两个输入，$x$表示具体样本，$\theta$表示模型参数，那么</p><ul><li>当$\theta$已知，该函数退化为概率函数，即样本$x$出现的概率</li><li>当$x$已知，该函数退化为似然函数，即对于不同模型参数，出现$x$这个样本的概率</li></ul><p>最大似然估计就是第二个问题，已知$x$，求$\theta$，使$P(x|\theta)$最大</p><h3 id="极大似然估计的特点"><a href="#极大似然估计的特点" class="headerlink" title="极大似然估计的特点"></a>极大似然估计的特点</h3><h2 id="极大似然估计的例子"><a href="#极大似然估计的例子" class="headerlink" title="极大似然估计的例子"></a>极大似然估计的例子</h2><p>假设一个袋子里有黑球和白球，现在往出放回地摸球，一共摸了十次，有7次黑球3次白球，问黑球所占比例最有可能是多少？如果我们将这个事件视为二项分布，且假设黑球出现的概率为$\theta$，那么现在模型已知（二项分布）、样本已知（10次里7次黑球），求模型参数（$\theta$的值）。假设样本$x_0=\{1,1,1,0,1,1,0,1,0,1\}$。那么似然函数可以写为：</p><script type="math/tex; mode=display">f(x_0|\theta)=\theta^3(1-\theta)\theta^2(1-\theta)\theta(1-\theta)\theta=\theta^7(1-\theta)^3=f(\theta)</script><p>上面的$f(\theta)$就是我们的似然函数，最大似然估计就是求这个函数的最大值，即$\frac{\textrm{d}f(\theta)}{\textrm{d}\theta}=0$。</p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;根据已知的样本结果信息，反推最有可能导致该结果的模型参数。&lt;/p&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="机器学习" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0/"/>
    
    
  </entry>
  
  <entry>
    <title>MIT6828-HW3-xv6 system calls</title>
    <link href="http://yoursite.com/2021/03/27/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/MIT6828-HW3/"/>
    <id>http://yoursite.com/2021/03/27/计算机/操作系统/课程/MIT6828-HW3/</id>
    <published>2021-03-27T05:36:01.256Z</published>
    <updated>2021-03-15T13:10:16.921Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/hint.css/2.4.1/hint.min.css"><p>系统调用是内核中的一系列实现系统功能的子函数，本文将针对XV6中的一些系统调用进行管理。本章的配置过程如：<a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-boot.html">作业1：XV6启动</a></p><a id="more"></a><h2 id="第一部分：系统调用追踪"><a href="#第一部分：系统调用追踪" class="headerlink" title="第一部分：系统调用追踪"></a>第一部分：系统调用追踪</h2><p>修改xv6内核，对于每一个系统调用，打印函数名和返回值以及参数，当xv6启动时，可以看到如下内容：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">...</span><br><span class="line">fork -&gt; 2</span><br><span class="line">exec -&gt; 0</span><br><span class="line">open -&gt; 3</span><br><span class="line">close -&gt; 0</span><br><span class="line">$write -&gt; 1</span><br><span class="line"> write -&gt; 1</span><br></pre></td></tr></table></figure><p>这个是init fork，并执行了sh，sh确保只有两个文件描述符被打开，然后sh写了<code>$</code>，修改<code>syscall.c</code>下的<code>syscall()</code>，如下功能：</p><ul><li>打印系统调用名称</li><li>打印返回值</li><li>打印系统调用参数</li></ul><p><code>syscall()</code>原型如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">syscall(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">int</span> num;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">curproc</span> = <span class="title">myproc</span>();</span></span><br><span class="line"></span><br><span class="line">  num = curproc-&gt;tf-&gt;eax;    <span class="comment">// 系统调用的序号被保存在eax寄存器中</span></span><br><span class="line">  <span class="keyword">if</span>(num &gt; <span class="number">0</span> &amp;&amp; num &lt; NELEM(syscalls) &amp;&amp; syscalls[num]) &#123;</span><br><span class="line">    curproc-&gt;tf-&gt;eax = syscalls[num]();     <span class="comment">//系统调用返回值保存在这里</span></span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    cprintf(<span class="string">"%d %s: unknown sys call %d\n"</span>,</span><br><span class="line">            curproc-&gt;pid, curproc-&gt;name, num);</span><br><span class="line">    curproc-&gt;tf-&gt;eax = <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们需要打印系统调用名称、返回值及参数，<code>struct proc</code>结构体描述了进程的信息，具体如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> &#123;</span></span><br><span class="line">  uint sz;                     <span class="comment">// Size of process memory (bytes)</span></span><br><span class="line">  <span class="keyword">pde_t</span>* pgdir;                <span class="comment">// Page table</span></span><br><span class="line">  <span class="keyword">char</span> *kstack;                <span class="comment">// Bottom of kernel stack for this process</span></span><br><span class="line">  <span class="keyword">enum</span> procstate state;        <span class="comment">// Process state</span></span><br><span class="line">  <span class="keyword">int</span> pid;                     <span class="comment">// Process ID</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">parent</span>;</span>         <span class="comment">// Parent process</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> *<span class="title">tf</span>;</span>        <span class="comment">// Trap frame for current syscall</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">context</span> *<span class="title">context</span>;</span>     <span class="comment">// swtch() here to run process</span></span><br><span class="line">  <span class="keyword">void</span> *chan;                  <span class="comment">// If non-zero, sleeping on chan</span></span><br><span class="line">  <span class="keyword">int</span> killed;                  <span class="comment">// If non-zero, have been killed</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">ofile</span>[<span class="title">NOFILE</span>];</span>  <span class="comment">// Open files</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">inode</span> *<span class="title">cwd</span>;</span>           <span class="comment">// Current directory</span></span><br><span class="line">  <span class="keyword">char</span> name[<span class="number">16</span>];               <span class="comment">// Process name (debugging)</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>从这个结构体中，我们能够得到进程的名称，但是只知道进程名称是不行的，我们需要知道函数名称。我们可以看到，在syscall中有一个比较重要的结构体tf，即trapframe，这个结构体保存了CPU寄存器中的值，内容如下</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> &#123;</span></span><br><span class="line">  <span class="comment">// registers as pushed by pusha</span></span><br><span class="line">  uint edi;</span><br><span class="line">  uint esi;</span><br><span class="line">  uint ebp;</span><br><span class="line">  uint oesp;      <span class="comment">// useless &amp; ignored</span></span><br><span class="line">  uint ebx;</span><br><span class="line">  uint edx;</span><br><span class="line">  uint ecx;</span><br><span class="line">  uint eax;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// rest of trap frame</span></span><br><span class="line">  ushort gs;</span><br><span class="line">  ushort padding1;</span><br><span class="line">  ushort fs;</span><br><span class="line">  ushort padding2;</span><br><span class="line">  ushort es;</span><br><span class="line">  ushort padding3;</span><br><span class="line">  ushort ds;</span><br><span class="line">  ushort padding4;</span><br><span class="line">  uint trapno;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// below here defined by x86 hardware</span></span><br><span class="line">  uint err;</span><br><span class="line">  uint eip;</span><br><span class="line">  ushort cs;</span><br><span class="line">  ushort padding5;</span><br><span class="line">  uint eflags;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// below here only when crossing rings, such as from user to kernel</span></span><br><span class="line">  uint esp;</span><br><span class="line">  ushort ss;</span><br><span class="line">  ushort padding6;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>记得在Lab1里有过使用eip寄存器函数追踪的方法，但是那个过于复杂，由于系统调用比较固定，我们直接写一个函数表用查表法实现即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">char</span> *syacall_name[] = &#123;</span><br><span class="line">    [SYS_fork] = <span class="string">"fork"</span>,</span><br><span class="line">    [SYS_exit] = <span class="string">"exit"</span>,</span><br><span class="line">    [SYS_wait] = <span class="string">"wait"</span>,</span><br><span class="line">    [SYS_pipe] = <span class="string">"pipe"</span>,</span><br><span class="line">    [SYS_read] = <span class="string">"read"</span>,</span><br><span class="line">    [SYS_kill] = <span class="string">"kill"</span>,</span><br><span class="line">    [SYS_exec] = <span class="string">"exec"</span>,</span><br><span class="line">    [SYS_fstat] = <span class="string">"fstat"</span>,</span><br><span class="line">    [SYS_chdir] = <span class="string">"chdir"</span>,</span><br><span class="line">    [SYS_dup] = <span class="string">"dup"</span>,</span><br><span class="line">    [SYS_getpid] = <span class="string">"getpid"</span>,</span><br><span class="line">    [SYS_sbrk] = <span class="string">"sbrk"</span>,</span><br><span class="line">    [SYS_sleep] = <span class="string">"sleep"</span>,</span><br><span class="line">    [SYS_uptime] = <span class="string">"uptime"</span>,</span><br><span class="line">    [SYS_open] = <span class="string">"open"</span>,</span><br><span class="line">    [SYS_write] = <span class="string">"write"</span>,</span><br><span class="line">    [SYS_mknod] = <span class="string">"mknod"</span>,</span><br><span class="line">    [SYS_unlink] = <span class="string">"unlink"</span>,</span><br><span class="line">    [SYS_link] = <span class="string">"link"</span>,</span><br><span class="line">    [SYS_mkdir] = <span class="string">"mkdir"</span>,</span><br><span class="line">    [SYS_close] = <span class="string">"close"</span>,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>在<code>syscall()</code>中添加下面一句：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cprintf(<span class="string">"%s-&gt;%d\n"</span>,syacall_name[num],curproc-&gt;tf-&gt;eax);</span><br></pre></td></tr></table></figure><h2 id="第二部分：date-System-Call"><a href="#第二部分：date-System-Call" class="headerlink" title="第二部分：date System Call"></a>第二部分：date System Call</h2><p>第二部分要求我们完成一个date 系统调用，获取当前UTC时间并返回。</p><h3 id="实现准备"><a href="#实现准备" class="headerlink" title="实现准备"></a>实现准备</h3><p>首先，我们需要一个读取硬件时间的函数，<code>cmostime()</code>，这个函数定义在<code>lapic.c</code>。其次我们需要一个描述时间的结构体<code>struct rtcdate</code>（位于<code>date.h</code>），我们需要将一个<code>rtcdate</code>对象指针传递给<code>cmostime()</code>。我们可以参考其他系统调用的实现，输入</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep -n uptime *.[chS]</span><br></pre></td></tr></table></figure><p>查看和uptime系统调用有关的所有内容，并参考其完成<code>date</code>系统调用。根据<code>grep -n uptime *.[chS]</code>的结果，将该添加的函数声明及宏定义添加了</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">syscall.c:106:extern int sys_date(void);</span><br><span class="line">syscall.c:130:[SYS_date]    sys_date,</span><br><span class="line">syscall.c:156:  [SYS_date] = &quot;date&quot;, </span><br><span class="line">syscall.h:23:#define SYS_date   22</span><br><span class="line">user.h:26:int date(struct rtcdate*);</span><br><span class="line">usys.S:32:SYSCALL(date)</span><br></pre></td></tr></table></figure><p>其次，我们创建一个<code>date.c</code>文件，这个文件中包含两部分，第一，这个文件包含一个main函数，即我们编译后可以产生可执行二进制文件<code>date</code>，输入命令<code>date</code>后，就会调用这个文件；第二，这个文件包含了<code>date</code>的实现（这个是错误的）。</p><p>然而，当我在<code>date.c</code>中添加了<code>date</code>函数定义后，我遇到了如下错误：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">/home/duan/Code/OS/xv6-public/usys.S:32: multiple definition of `date&apos;</span><br><span class="line">date.o:/home/duan/Code/OS/xv6-public/date.c:6: first defined here</span><br></pre></td></tr></table></figure><p>提示我在文件<code>usys.S</code>中，<code>date</code>已经定义过了，打开<code>usys.S</code>，里面内容如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">#define SYSCALL(name) \</span><br><span class="line">  .globl name; \</span><br><span class="line">  name: \</span><br><span class="line">    movl $SYS_ ## name, %eax; \</span><br><span class="line">    int $T_SYSCALL; \</span><br><span class="line">    ret</span><br><span class="line"></span><br><span class="line">SYSCALL(fork)</span><br><span class="line">SYSCALL(exit)</span><br><span class="line">...</span><br><span class="line">SYSCALL(date)</span><br></pre></td></tr></table></figure><p>将<code>date</code>带入宏定义并展开，得到结果如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">.globl date;</span><br><span class="line">date:</span><br><span class="line">    movl $SYS_date, %eax;</span><br><span class="line">    int $T_SYSCALL;</span><br><span class="line">    ret</span><br></pre></td></tr></table></figure><p>说明我们在<code>usys.S</code>中，已经通过汇编的方式，定义了<code>date</code>这个函数，这个函数的内容是将<code>SYS_date</code>移到<code>eax</code>，然后通过中断的方式调用<code>sys_date</code>，因此我们不需要再定义<code>date</code>，只需要编写好<code>sys_date</code>即可。但是这个时候我又疑惑了，我们的<code>date()</code>是有参数的，输入的是<code>struct rtcdate*</code>，但是<code>sys_</code>开头的系统调用函数都是没有参数的</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">int</span> <span class="title">sys_link</span><span class="params">(<span class="keyword">void</span>)</span></span>;     <span class="comment">// 系统调用全都没有参数</span></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">int</span> <span class="title">sys_mkdir</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">int</span> <span class="title">sys_mknod</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">int</span> <span class="title">sys_open</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br></pre></td></tr></table></figure><p>那么该如何处理输入参数呢？</p><h3 id="解决输入参数的问题"><a href="#解决输入参数的问题" class="headerlink" title="解决输入参数的问题"></a>解决输入参数的问题</h3><p>后来我经过了一番寻找，终于找到了一个类似的需要输入参数的系统调用，即<code>sys_mkdir</code>。而<code>mkdir.c</code>中对<code>mkdir</code></p><h3 id="实现步骤"><a href="#实现步骤" class="headerlink" title="实现步骤"></a>实现步骤</h3><h4 id="sys-date实现"><a href="#sys-date实现" class="headerlink" title="sys_date实现"></a>sys_date实现</h4><p>在<code>sysproc.c</code>中实现<code>sys_date()</code>函数，练习系统调用的编写过程。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> </span><br><span class="line">sys_date(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">  <span class="comment">// Your code</span></span><br><span class="line">  <span class="keyword">char</span> *r;</span><br><span class="line">  acquire(&amp;tickslock);</span><br><span class="line">  <span class="keyword">if</span>(argptr(<span class="number">0</span>, &amp;r, <span class="keyword">sizeof</span>(struct rtcdate)) &lt; <span class="number">0</span> )&#123;    <span class="comment">// 如果输入的第一个参数有误，那么释放锁，然后返回</span></span><br><span class="line">    release(&amp;tickslock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  cmostime((struct rtcdate*)r);</span><br><span class="line">  release(&amp;tickslock);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="在Makefile中添加调用"><a href="#在Makefile中添加调用" class="headerlink" title="在Makefile中添加调用"></a>在Makefile中添加调用</h4><p>为了使date命令能够在xv6的shell中使用，需要在<code>Makefile</code>的<code>UPROGS</code>定义中添加<code>_date</code>。</p><h4 id="date可执行程序实现"><a href="#date可执行程序实现" class="headerlink" title="date可执行程序实现"></a>date可执行程序实现</h4><p>在<code>date.c</code>中加入如下程序</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"types.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"user.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"date.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">main(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span><br><span class="line">&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">rtcdate</span> <span class="title">r</span>;</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (date(&amp;r)) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="number">2</span>, <span class="string">"date failed\n"</span>);</span><br><span class="line">    <span class="built_in">exit</span>();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// your code to print the time in any format you like...</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">exit</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h2><p>最后实现的结果如下，运行<code>make qemu</code>打开操作系统后，输入<code>date</code>，将显示系统时间（命令行中的777不知道是啥，好像是在qemu和shell切换时会产生）</p><p><img src="https://raw.githubusercontent.com/sqduan/hexoimg/master/1615813640(1).png" width = "350" alt="图片名称" align=center /></p><h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><div id="footnotes"><hr><div id="footnotelist"><ol style="list-style:none; padding-left: 0;"><li id="fn:1"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">1.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/xv6/book-rev10.pdf">xv6 book</a></span><a href="#fnref:1" rev="footnote"> ↩</a></li><li id="fn:2"><span style="display: inline-block; vertical-align: top; padding-right: 10px;">2.</span><span style="display: inline-block; vertical-align: top;"><a href="https://pdos.csail.mit.edu/6.828/2018/homework/xv6-syscall.html">Homework: xv6 system calls (mit.edu)</a></span><a href="#fnref:2" rev="footnote"> ↩</a></li></ol></div></div>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;系统调用是内核中的一系列实现系统功能的子函数，本文将针对XV6中的一些系统调用进行管理。本章的配置过程如：&lt;a href=&quot;https://pdos.csail.mit.edu/6.828/2018/homework/xv6-boot.html&quot;&gt;作业1：XV6启动&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="计算机" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/"/>
    
      <category term="操作系统" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/"/>
    
      <category term="课程" scheme="http://yoursite.com/categories/%E8%AE%A1%E7%AE%97%E6%9C%BA/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%AF%BE%E7%A8%8B/"/>
    
    
  </entry>
  
</feed>
